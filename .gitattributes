# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# cmd 批处理必须 CRLF(LF/UTF-8 会让中文多行块解析炸裂 → 双击闪退)
build_exe.bat text eol=crlf

launch_card_editor.bat text eol=crlf

launch_map_importer.bat text eol=crlf
