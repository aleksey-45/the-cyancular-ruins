refactor(pvp): 加对手副本访问器,共享基类再吸收 3 函数 —— 3.4 收口

★ 引入 `_replica_for(role) -> Node2D`(基类默认返回 null,**子类必须覆写**):两个客户端
  除了"对手 1 个固定 vs N 个按 role"之外别无异同,把这条差异**收在一个口上**,下面那些
  消费快照/事件的函数就能一起上提,而不是各写两份。

吸收(判据同前:剔注释含行尾注释后逐字相同,或只差"取哪个副本"):
  · `_on_hit_event`(对手被打 → 副本闪一下);
  · `_on_beam_fired`(对手激光光束视觉副本,逐点锚到射手副本)—— 连 `const LaserVisual` 一起上提;
  · `_on_match_sync`(进场拉取应答,17 行)—— 两个子类仅差一处 `push_warning` 前缀。
    它要调 `_apply_peer_names`/`_apply_peer_hues`,而这两个的形状**本就**随对手数量而定,
    故在基类声明为「必须覆写」的桩(body 是 push_error),子类各自实现。

行数:pvp_client 555→318、royale_game 482→291、基类 0→239。**重复净减 189 行**。

★ 3.4 到此收口 —— 剩下**仍有差异**的函数是**刻意保留**的,它们差的不是重复而是**第二根结构轴**:
  · `_process` / `_on_snapshot_world` / `_on_round_state`:差在 **per-role 的 ID 标签与血条记账**
    (1v1 是固定的 `_id_self`/`_id_opp`/`_hp_bar`,大乱斗是 `_id_labels`/`_hp_bars` 字典),
    以及 `_on_round_state` 的真语义差(退场定时器 5.0s vs 6.0s)。要合得先抽"标签/血条管理器",
    那是**重新设计**而不是去重 —— 与本项"合并重复的事件消费层"不是一个动作,故不做。
  · `_apply_peer_names` / `_apply_peer_hues`:上面那两个桩的实现,形状随对手数量而定(刻意)。
  · `_on_kill_event`:仅差一行回退文案(`"对手"` vs `"玩家%d" % victim`)。**统一会改 1v1 的
    用户可见文字**,不擅自改,留作待定。
  · `_ready`:两个模式各建各的世界/副本/HUD,差异最大(56/75 行)。
  · `_replica_for`:基类桩 + 两个子类实现(本就是设计的一环)。

验证:启动 0 报错 + 两个对局场景真加载 0 报错 + kh_l1/l5/l6 + match_sync + preview_visibility +
room_sweep/pvp_room/pvp_match/pvp_reconcile/pvp_twin/move_feel 六个多进程 + royale_probe/
royale_bound/royale_c2 三个端到端 + replica_ghost + 4 客户端真对局 soak + 命名 lint,全绿。
