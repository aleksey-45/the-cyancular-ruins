refactor(pvp): 共享基类再吸收两函数,并修一处**因缺字段声明导致的基类编译失败**(3.4 第二步)

吸收(判据同前:剔掉注释**含行尾注释**后代码逐字相同):
  · `_on_bullet_spawn`(25 行)—— 零新依赖(只用已上提的 `_world` 与 class_name);
  · `_refresh_input_lock`(2 行)—— 两个子类仅差一个 `_match_ended` 维度,故连 `_match_ended` 一起上提。
    ★ 这是本步的**唯一行为差异**:1v1 在 MATCH_OVER 之后那几秒输入现在也被锁(此前没有),
      与大乱斗同款、且与 `_match_ended` 的语义一致(那段时间正在回菜单)—— 属有意统一,已在代码注释里写明。

★ 途中踩到并修掉的一个真问题(值得记):把 `_refresh_input_lock` 追加进基类时**漏了它引用的
  `_menu_open`** —— 基类自己编译不过 → 所有 `extends PvpMatchClient` 的脚本在**子进程**里报
  `Could not resolve class "PvpMatchClient"`(而类缓存条目本身是对的:class→path 完全正确,
  只是那个 path 指向的脚本编译失败)。两个大乱斗探针因此超时。
  · 误判方向:`--import` **不编译**全部脚本(它只 import 资源),所以"import 无报错"不代表脚本能编译;
  · 可靠的单文件检查是 `--check-only --script <path>` —— 但它**不实例化 autoload**,故 `NetBus`/
    `GameParameters` 一律假报「Identifier not found」;它只对**脚本内未声明的标识符**(如 `_menu_open`)
    是真信号。最可靠的仍是**真加载场景**(`--quit-after 30 <scene>`)。

★ 探针连带:`kh_l6` 的「输入锁单一收口」检查原先只在 `pvp_client` 里找 `func _refresh_input_lock(`,
  搬走后它红得对。改成**两处都查**(定义可在 pvp_client 或基类;函数体判据在定义所在的那个文件上做),
  并把「散落 `set_controls_locked`」的扫描**跨两个文件**(只查一边的话,另一边散落一份就没人管)。
  反证:往**基类**的锁函数体外塞一处 `set_controls_locked` → 咬红,且消息报出
  `pvp_match_client.gd/_correct_local_spawn`(证明跨文件扫描真的生效);还原转绿。

行数:pvp_client 394→367、royale_game 368→341、基类 111→152。

验证:启动 0 报错 + 大厅 + **两个对局场景真加载**各 0 报错 + kh_l1/l3/l4/l5/l6 + match_sync +
preview_visibility + rollback_fidelity + grenade_player_hit + room_sweep/pvp_room/pvp_match/
pvp_reconcile/pvp_twin/move_feel 六个多进程 + royale_probe/royale_bound/royale_c2 三个端到端 +
4 客户端真对局 soak + 命名 lint,全绿。
