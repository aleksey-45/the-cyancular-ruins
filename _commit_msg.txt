refactor(ui): UiFactory 补齐 5 个口,收掉三个页面各手抄的控件包装(3.7)

新增(全部逐字搬自页面里的手抄实现,只是把各自的**版式数值**提成参数):
  · `apply_font_recursive(root)` ← `_apply_pixel_font`(matchmaking/royale_lobby 各一份,逐字相同);
  · `hue_preview_color(hue_deg)` ← `_hue_preview_color`(同上,两份逐字相同);
  · `check_row(text, initial, label_w, on_toggle)` ← `matchmaking._opt_check` 与 `settings_menu._check`
    —— 两者**同形状**,只差标签列宽(440 vs 320),那本就是各页版式调参,故走参数;
  · `slider_row(text, initial, label_w, on_change)` ← `settings_menu._slider_row`;
  · `line_edit(parent, pos, size, placeholder, initial)` ← `_make_line_edit`(两份差 size:240×36 / 250×40,
    同样走参数 —— 逐字搬会改观感)。

★ 形参顺序刻意把 `label_w` 放在 **callable 之前**:两个页面都用**块体 lambda** 作最后实参,
  callable 之后再跟实参要跨过 lambda 体写逗号,极易写错(本轮第一版就写错、解析不过)。

行数:matchmaking 538→493、royale_lobby 601→581、settings_menu 216→180、ui_factory 272→351
(三个页面共减 101 行手抄,工厂增 79 行 —— 净减重复,且注释不再抄三遍)。

★ 途中踩到并修的两个坑(都是我自己的按行段删函数脚本):
  ① 该脚本"走到下一段后接 `func` 的注释才停"的启发式,**遇到注释与 `func` 之间夹着 `const` 就失效**
     → `_hue_preview_color` 的删除把 `const OPT_LABEL_W := 440.0` 一起吞了(缺常量 → 5 处解析错误)。
     已补回;另一处 `const CHECK_LABEL_W` 因在 settings_menu 里同形,提前防护 + 删后核查,未被吞。
  ② 删完必查"被删的行里有没有 `-const`/`-var`"(`git diff | grep '^-'`),这是唯一能发现 ① 的办法。

验证:启动 0 报错 + **三个页面场景真加载各 0 报错**(matchmaking / royale_lobby / settings_menu)
+ kh_l1/l5/l6 + match_sync + pvp_room/pvp_match 两个多进程 + royale_bound/royale_c2 端到端,全绿。
★ 视觉未实测:`menu_autotest` 是截图探针(必须真实渲染),留给用户跑;本次所有数值都按参数原样传,
预期零观感变化。
