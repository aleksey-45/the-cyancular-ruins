# KH 合并 L1+L2 实施计划（基础设施 + 反馈层）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 KH_v1_1_3 的基础设施（Settings / NetBusExt / Sfx / RunOptions / LocalServer / PvpSession）与打击反馈层（命中 X / 击杀播报 / 骷髅连杀 / 受击红闪 / 爆炸内圈免疫掩护 / 归因 meta）搬进 main，其余保持 main 不变。

**Architecture:** 逐文件搬运 + 机械路径改写（`Scenes/`→`scenes/`、`Globals/`→`core/`、`Tests/`→`tests/`），手工合并只在 `explosion.gd` / `bullet_base.gd` / `enemy_base.gd` / `combat_component.gd` / `post_process.gd` 五处做加法——这几处 main 领先，**只加不改**。

**Tech Stack:** Godot 4.7.1 标准版（非 mono）· GDScript · 无单测框架，用 `extends SceneTree`（`-s`）或场景式（`.tscn`）探针

---

## Global Constraints

- **规格来源**：`docs/superpowers/specs/2026-09-10-kh-merge-design.md`（本计划的每一步都不得偏离它）。
- **KH 来源目录**：`E:\Workspace\godot\the-cyancular-ruins-KH_v1_1_3`（= `origin/KH_v1_1_3`，已 fetch；二选一都行）。下文记作 `$KH`。
- **路径映射**（机械改写，逐字节对齐 main 布局）：`Globals/*.gd`→`core/*.gd`；`Globals/gameParameters.gd|enemyParams.gd|playerParams.gd`→`core/game_parameters.gd|enemy_params.gd|player_params.gd`；`Scenes/Enemies|Player|Weapons|Effects/*`→`scenes/enemies|player|weapons|effects/*`；`Scenes/hud.gd`→`ui/hud.gd`；`Scenes/post_process.gd`→`render/post_process.gd`；`Scenes/*`→`scenes/*`；`Shaders/*`→`shaders/*`；`Tests/*`→`tests/*`；`map/*`→`maps/*`；`editor/*`→`level_editor/*`。
- **搬进来的文件里所有 `res://` 字面量必须同步改写**（`res://Scenes/...`→`res://scenes/...`、`res://Globals/...`→`res://core/...`、`res://Tests/...`→`res://tests/...`、`res://Shaders/...`→`res://shaders/...`）。
- **目录命名规范**（main 已整改，不得倒退）：目录全小写；脚本文件名 snake_case；`class_name` 保持 Pascal；场景 `.tscn` 保持 PascalCase。
- **autoload 由 2 个增至 4 个**，顺序固定：`GameParameters`、`NetBus`、`NetBusExt`、`Settings`。
- **Godot 可执行文件**：`"D:\Program Files\Godot_v4.7.1-stable_win64\Godot_v4.7.1-stable_win64_console.exe"`（下文记作 `$GODOT`）。
- **`-s` 阶段 autoload 尚未实例化**：凡要断言 autoload 的探针必须是**场景式**（`.tscn` + `--path . res://tests/xxx.tscn`），不能用 `-s`。
- **测试由代理代跑**（本任务一次性授权）；但真机手感/GUI 表现仍由用户验。
- **提交纪律**：**每个任务结束各做一次本地提交**（用户 2026-09-10 裁决：由「一层一个提交」改为「一任务一个提交」，以契合逐任务评审的 `BASE..HEAD` diff 机制），全程不 `git push`。层内不额外汇总提交。用户最后会 `git reset --soft <基线>` 把全部提交压回暂存区自行组织。
- **提交范围**：只 `git add` 本任务实际改动的文件；**绝不允许**把用户既有的未提交改动（`core/net_bus.gd`、`docs/pvp-networking.md`）一起 `git add` 进去（不要用 `git add -A` / `git add .`）。
- **本层的四个守卫点**（来自规格 §5，本层只涉及第 2 条）：`bullet_base` 撞砖碎片保留 main 的「无条件播放」语义——KH 版把它塞进 `if apply_damage` 会让 PvP 客户端视觉副本撞墙无反馈，**禁止照抄**。

---

### Task 1: Sfx 8bit 程序合成音效

**Files:**
- Create: `core/sfx.gd`（从 `$KH/Globals/sfx.gd` 照搬，121 行）

**Interfaces:**
- Produces: `class_name Sfx`（`extends RefCounted`，静态类，**不是** autoload）；`Sfx.play(kind: String, pitch := 1.0, volume_db := 0.0) -> void`；`Sfx._stream(kind: String) -> AudioStreamWAV`（探针用）；音效 kind 全集：`shoot / shoot_heavy / shotgun / hit / hurt / jump / explosion / kill / switch / deny / reload / ui / teleport`

- [ ] **Step 1: 复制文件并改写引用**

```bash
cp "$KH/Globals/sfx.gd" core/sfx.gd
grep -n 'res://' core/sfx.gd   # 预期：无输出（该文件不含任何 res:// 字面量，无需改写）
```

- [ ] **Step 2: 校验解析（headless 启动，Sfx 是 class_name 会被全局类缓存收录）**

Run:
```bash
"$GODOT" --headless --path . --import 2>&1 | grep -iE 'sfx|parse error|error' ; echo "exit=$?"
```
Expected: 无 `Parse Error`；`core/sfx.gd` 出现在全局类缓存里。

- [ ] **Step 3: 断言音效流可生成**

Run:
```bash
"$GODOT" --headless --path . -s res://tests/laser_weapon_smoke.gd 2>&1 | tail -3
```
Expected: 该既有探针仍正常结束（用作「新增 class_name 未打断既有解析」的回归信号）。

- [ ] **Step 4: 手动最小验证**

在下一步的 `tests/kh_l1_probe.gd` 里会断言 `Sfx._stream("kill") != null && Sfx._stream("hit") != null`，届时一并验证，本步不单独建文件。

---

### Task 2: Settings autoload（照搬 + 删 old_ui / sp_difficulty）

**Files:**
- Create: `core/settings.gd`（从 `$KH/Globals/settings.gd` 照搬 184 行后删裁）

**Interfaces:**
- Produces: autoload 节点名 `Settings`；成员 `master_volume`、`sfx_volume`、`wheel_switch`、`reload_enabled`、`sp_disabled_weapons: Array[int]`、`pvp_show_trajectories`、`pvp_round_full_heal`、`pvp_show_enemy_hp`、`pvp_disabled_weapons: Array[int]`、`pvp_color_hue`、`royale_match_min`、`pvp_show_minimap`、`pvp_minimap_show_enemy`；方法 `save()`、`load_settings()`、`get_binding(action)`、`get_binding_names(action)`、`set_binding(action, event)`、`reset_bindings()`；常量 `SAVE_PATH = "user://settings.cfg"`、`REMAPPABLE_ACTIONS`
- **明确不产出**：`old_ui`、`sp_difficulty`，以及任何其它难度相关物（`difficulty` / `DENSITY` / `DIFFICULTY_NAMES` 若在此文件出现也要删）。
  - 注：`_apply_binding` 是 **KH 与 main 双方都存在、双方都零调用** 的私有死代码，**不在删除范围，保留原样**（本任务是照搬，不扩删范围）。

- [ ] **Step 1: 复制并改写路径**

```bash
cp "$KH/Globals/settings.gd" core/settings.gd
grep -n 'res://' core/settings.gd   # 预期：无输出（只有 user:// 与 InputMap，无需改写）
```

- [ ] **Step 2: 删除老版 UI 开关**

删掉 `var old_ui: bool = false	# 老版大厅 UI`（第 24 行）、`cf.set_value("ui", "old_ui", old_ui)`、`old_ui = bool(cf.get_value("ui", "old_ui", false))`。

```bash
grep -n 'old_ui' core/settings.gd   # 预期：无输出
```

- [ ] **Step 3: 删除难度**

删掉 `var sp_difficulty: int = 1`（第 29 行）、`cf.set_value("single", "difficulty", sp_difficulty)`、`sp_difficulty = int(cf.get_value("single", "difficulty", 1))`。

```bash
grep -n 'difficulty' core/settings.gd   # 预期：无输出
```

- [ ] **Step 4: 清理重复注释**

第 68-71 行有重复的「键位重映射」注释块（KH 自己留的），删成一块。

- [ ] **Step 5: 手工核对该文件以 KEEP 开头的项一个没少**

```bash
grep -nE 'master_volume|sfx_volume|wheel_switch|reload_enabled|sp_disabled_weapons|pvp_show_trajectories|pvp_round_full_heal|pvp_show_enemy_hp|pvp_disabled_weapons|pvp_color_hue|royale_match_min|pvp_show_minimap|pvp_minimap_show_enemy|REMAPPABLE_ACTIONS' core/settings.gd | wc -l
```
Expected: ≥ 14 个匹配行（每个保留项至少一处声明/读写）。

> **注意（不是 bug）**：开发机上 `user://settings.cfg` 是本机既有存档，里面可能残留 `ui/old_ui`、`single/difficulty` 键。删掉读写后这些键被忽略，`save()` 下次会整文件重写、不再带它们。探针**不得**断言「默认值」（存档会覆盖），只断言属性存在性与源码。

---

### Task 3: NetBusExt autoload（扩展协议收口）

**Files:**
- Create: `core/net_bus_ext.gd`（从 `$KH/Globals/net_bus_ext.gd` 照搬，110 行）

**Interfaces:**
- Consumes: `NetBus`（autoload，先于本节点注册）
- Produces: autoload 节点名 `NetBusExt`
  - **RPC 方法（15）**：`player_options`、`suicide_request`、`royale_create`、`royale_join`、`royale_leave`、`royale_list`、`royale_start`、`ai_duel`、`royale_start_ai`、`match_options`、`peer_hues`、`hit_confirm`、`beam_fired`、`royale_rooms`、`royale_room_state`
  - **信号（15）**：`local_match_options`、`local_peer_hues`、`local_hit_confirm`、`player_options_received`、`local_beam_fired`、`suicide_requested`、`royale_create_requested`、`royale_join_requested`、`royale_leave_requested`、`royale_list_requested`、`royale_start_requested`、`ai_duel_requested`、`royale_start_ai_requested`、`local_royale_rooms`、`local_royale_room_state`
  - ⚠️ **注意命名陷阱**：`ai_duel_requested` / `royale_start_ai_requested` 是**信号**不是方法（对应 RPC 是 `ai_duel` / `royale_start_ai`）。用 `has_method` 断言它们会假失败。
- **设计不变量**：`core/net_bus.gd` 的 RPC 方法列表**一字不改**（改动会与原作者云大厅全部失联）；所有新增协议只进本文件

- [ ] **Step 1: 复制并改写路径**

```bash
cp "$KH/Globals/net_bus_ext.gd" core/net_bus_ext.gd
grep -n 'res://' core/net_bus_ext.gd   # 预期：无输出；若有，按 Global Constraints 的映射改写
```

- [ ] **Step 2: 确认 net_bus.gd 未被改动（守卫点）**

```bash
git diff --stat core/net_bus.gd
```
Expected: 只显示**用户既有的**未提交改动（`ENet_CHANNELS`），**不得**出现本计划新增的行。

- [ ] **Step 3: 断言 RPC 齐备（延后到 Task 8 的场景探针）**

本步不单独建文件；Task 8 的 `tests/kh_l1_probe.gd` 会逐个 `has_method` 断言上面列出的 13 个 RPC。

---

### Task 4: RunOptions（只留禁用武器）

**Files:**
- Create: `core/run_options.gd`（从 `$KH/Globals/run_options.gd` 照搬 19 行后删裁）

**Interfaces:**
- Produces: `class_name RunOptions`（静态类）；`static var disabled_weapons: Array[int]`；`static func reset() -> void`
- **明确不产出**：`difficulty`、`DIFFICULTY_NAMES`、`difficulty_mult()`

- [ ] **Step 1: 复制**

```bash
cp "$KH/Globals/run_options.gd" core/run_options.gd && cat core/run_options.gd
```

- [ ] **Step 2: 删除难度相关的一切**

删掉 `difficulty` 静态变量、`DIFFICULTY_NAMES` 常量、`difficulty_mult()` 方法（若存在）；`reset()` 里对应的重置行一并删。`disabled_weapons` 与它的重置**保留**。

- [ ] **Step 3: 驱动「已删干净」的断言（先失败）**

```bash
grep -c 'difficulty' core/run_options.gd
```
Expected: `0`。若 >0，回到 Step 2。

---

### Task 5: LocalServer（本机一键起服）

**Files:**
- Create: `core/local_server.gd`（从 `$KH/Globals/local_server.gd` 照搬，108 行）

**Interfaces:**
- Produces: `class_name LocalServer`（`extends RefCounted`，**不是** autoload，Windows 专属，被 `scenes/matchmaking.gd` 使用——该接线在 L4）

- [ ] **Step 1: 复制并改写路径**

```bash
cp "$KH/Globals/local_server.gd" core/local_server.gd
grep -nE 'res://|C:/Godot|C:\\\\Godot' core/local_server.gd
```
Expected: 若有 `res://` 字面量按映射改写；若含硬编码的 Godot 路径（`C:/Godot/...`）改成本机 `D:/Program Files/Godot_v4.7.1-stable_win64/...`。

- [ ] **Step 2: 校验类可解析**

由 Task 8 的场景探针断言 `LocalServer != null`（`class_name` 解析成功即通过），本步不单独建文件。

---

### Task 6: PvpSession 补两个字段

**Files:**
- Modify: `core/pvp_session.gd`

**Interfaces:**
- Produces: `PvpSession.disabled_weapons: Array[int]`、`PvpSession.royale: bool`；`PvpSession.reset()` 一并清这两项

- [ ] **Step 1: 写入（照 `$KH/Globals/pvp_session.gd` 的差异）**

在 `static var spawn: Vector2i = Vector2i(-1, -1)` 之后加：

```gdscript
static var disabled_weapons: Array[int] = []   # 本局生效的禁用武器(服务器 match_options 下发)
static var royale: bool = false                # 大乱斗局:N 人限时死斗
```

在 `reset()` 内 `spawn = Vector2i(-1, -1)` 之后加：

```gdscript
	disabled_weapons = []
	royale = false
```

- [ ] **Step 2: 核对差异与 KH 完全一致（除注释措辞）**

```bash
diff --strip-trailing-cr core/pvp_session.gd "$KH/Globals/pvp_session.gd"
```
Expected: 仅注释文案不同（KH 注释提到 RoyaleServer 分支名），无逻辑差异。

---

### Task 7: project.godot 与 export_presets

**Files:**
- Modify: `project.godot`（`[autoload]` 段）
- Modify: `export_presets.cfg`（两处 `exclude_filter`）

**Interfaces:**
- Produces: autoload 顺序 `GameParameters` → `NetBus` → `NetBusExt` → `Settings`

- [ ] **Step 1: 改 `[autoload]`**

把

```ini
[autoload]

GameParameters="*res://core/game_parameters.gd"
NetBus="*res://core/net_bus.gd"
```

改成

```ini
[autoload]

GameParameters="*res://core/game_parameters.gd"
NetBus="*res://core/net_bus.gd"
NetBusExt="*res://core/net_bus_ext.gd"
Settings="*res://core/settings.gd"
```

- [ ] **Step 2: 排除 DevTools（两处 preset 都要改，第 13 行与第 87 行）**

`exclude_filter="*.md"` → `exclude_filter="*.md,DevTools/*"`

```bash
grep -c 'exclude_filter="\*.md,DevTools/\*"' export_presets.cfg
```
Expected: `2`

- [ ] **Step 3: 确认 include_filter 保持 main 的**

```bash
grep -n 'include_filter' export_presets.cfg
```
Expected: `include_filter="maps/*.cyrm,data/enemies.json"`（**不得**变成 KH 的 `map/*.cyrm,editor/enemies.json`）

---

### Task 8: L1 验收探针 + 提交

**Files:**
- Create: `tests/kh_l1_probe.gd`
- Create: `tests/kh_l1_probe.tscn`

**Interfaces:**
- Consumes: Task 1–7 的全部产出
- Produces: 探针 `tests/kh_l1_probe.tscn`，成功打印 `KH L1 PROBE: ALL-OK` 并 `quit(0)`

- [ ] **Step 1: 写探针脚本 `tests/kh_l1_probe.gd`**

```gdscript
extends Node

# KH 合并 L1 探针(场景模式:autoload 需已实例化,不能用 -s 跑)。
# 跑法: Godot_console --headless --path . res://tests/kh_l1_probe.tscn

const REQUIRED_AUTOLOADS := ["GameParameters", "NetBus", "NetBusExt", "Settings"]
const NETBUS_EXT_RPCS := [
	# 客户端→服务器请求
	"player_options", "suicide_request",
	"royale_create", "royale_join", "royale_leave", "royale_list", "royale_start",
	"ai_duel", "royale_start_ai",
	# 服务器→客户端
	"match_options", "peer_hues", "hit_confirm", "beam_fired",
	"royale_rooms", "royale_room_state",
]

# 注意:下面这些是**信号**不是方法(对应 RPC 见 NETBUS_EXT_RPCS)。
# 用 has_method 断言它们会假失败,必须用 has_signal。
const NETBUS_EXT_SIGNALS := [
	"local_match_options", "local_peer_hues", "local_hit_confirm", "player_options_received",
	"local_beam_fired", "suicide_requested",
	"royale_create_requested", "royale_join_requested", "royale_leave_requested",
	"royale_list_requested", "royale_start_requested",
	"ai_duel_requested", "royale_start_ai_requested",
	"local_royale_rooms", "local_royale_room_state",
]


func _ready() -> void:
	var failures: Array[String] = []

	# 1) 四个 autoload 就位
	for n in REQUIRED_AUTOLOADS:
		if get_tree().root.get_node_or_null(NodePath(n)) == null:
			failures.append("autoload 缺失: %s" % n)

	# 2) Settings 保留项都在、已删项都不在(不断言默认值:本机 user://settings.cfg 会覆盖)
	for prop in ["master_volume", "sfx_volume", "wheel_switch", "reload_enabled",
			"sp_disabled_weapons", "pvp_show_trajectories", "pvp_round_full_heal",
			"pvp_show_enemy_hp", "pvp_disabled_weapons", "pvp_color_hue",
			"royale_match_min", "pvp_show_minimap", "pvp_minimap_show_enemy"]:
		if not _has_prop(Settings, prop):
			failures.append("Settings 缺属性: %s" % prop)
	if _has_prop(Settings, "old_ui"):
		failures.append("Settings.old_ui 未删除")
	if _has_prop(Settings, "sp_difficulty"):
		failures.append("Settings.sp_difficulty 未删除")
	if Settings.REMAPPABLE_ACTIONS.is_empty():
		failures.append("Settings.REMAPPABLE_ACTIONS 为空")
	Settings.save()             # 存写不崩
	Settings.load_settings()    # 读回不崩

	# 3) 源码级:难度/老版 UI 不得残留
	var ro_src := _read_res("res://core/run_options.gd")
	if ro_src == "":
		failures.append("core/run_options.gd 读不到")
	else:
		if "difficulty" in ro_src:
			failures.append("core/run_options.gd 仍含 difficulty")
		if "disabled_weapons" not in ro_src:
			failures.append("core/run_options.gd 缺 disabled_weapons")
	var st_src := _read_res("res://core/settings.gd")
	if "old_ui" in st_src:
		failures.append("core/settings.gd 仍含 old_ui")
	if "sp_difficulty" in st_src:
		failures.append("core/settings.gd 仍含 sp_difficulty")

	# 4) NetBusExt 扩展 RPC 与信号齐
	for m in NETBUS_EXT_RPCS:
		if not NetBusExt.has_method(m):
			failures.append("NetBusExt 缺 RPC: %s" % m)
	for s in NETBUS_EXT_SIGNALS:
		if not NetBusExt.has_signal(s):
			failures.append("NetBusExt 缺信号: %s" % s)

	# 5) pvp_session 两个新字段 + reset 清得掉
	if PvpSession.royale:
		failures.append("PvpSession.royale 重置后应为 false")
	if PvpSession.disabled_weapons.size() != 0:
		failures.append("PvpSession.disabled_weapons 重置后应为空")
	PvpSession.royale = true
	PvpSession.disabled_weapons.append(3)
	PvpSession.reset()
	if PvpSession.royale or PvpSession.disabled_weapons.size() != 0:
		failures.append("PvpSession.reset() 未清 royale/disabled_weapons")

	# 6) Sfx 程序合成流可生成(不依赖音频设备)
	if Sfx._stream("kill") == null or Sfx._stream("hit") == null:
		failures.append("Sfx 音效流生成失败")

	# 7) LocalServer 类可解析
	if LocalServer == null:
		failures.append("LocalServer 未解析")

	if failures.is_empty():
		print("KH L1 PROBE: ALL-OK")
		get_tree().quit(0)
	else:
		print("KH L1 PROBE: FAIL | " + "; ".join(failures))
		get_tree().quit(1)


func _has_prop(o: Object, prop: String) -> bool:
	for p in o.get_property_list():
		if str(p["name"]) == prop:
			return true
	return false


func _read_res(path: String) -> String:
	if not ResourceLoader.exists(path):
		return ""
	var f := FileAccess.open(path, FileAccess.READ)
	return f.get_as_text() if f != null else ""
```

- [ ] **Step 2: 写场景 `tests/kh_l1_probe.tscn`**

```
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://tests/kh_l1_probe.gd" id="1"]

[node name="KhL1Probe" type="Node"]
script = ExtResource("1")
```

（照 main 既有的 `tests/pvp_reconcile_smoke.tscn` 写法；`uid` 由 Godot 首次导入时补，不必手填。）

- [ ] **Step 3: 跑探针——此时应当全绿**

Run:
```bash
"$GODOT" --headless --path . res://tests/kh_l1_probe.tscn 2>&1 | tail -5
```
Expected: `KH L1 PROBE: ALL-OK`，退出码 0。

- [ ] **Step 4: 反证探针真的会失败（防止假绿）**

临时把 `core/settings.gd` 里加回一行 `var old_ui: bool = false`，重跑 Step 3。

Expected: 输出含 `Settings.old_ui 未删除`，退出码 1。**验完立刻删掉该行并重跑确认恢复 ALL-OK。**

- [ ] **Step 5: 全项目 headless 启动自检**

Run:
```bash
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -iE 'error|parse|script' | head -20
```
Expected: 无 `Parse Error` / `SCRIPT ERROR`。

- [ ] **Step 6: 提交（本任务自己的产出）**

```bash
git add tests/kh_l1_probe.gd tests/kh_l1_probe.tscn
git commit -m "test(kh-merge): L1 验收探针——autoload/设置项/已删字段/NetBusExt RPC 与信号/RunOptions/大乱斗字段/音效流"
```

> Task 1–7 各自已在结束时提交了自己的文件，本步只提交探针。

---

### Task 8b: 探针加固（用户 2026-09-10 裁定补做）

> 背景：Task 8 的评审提出两条失效模式，用户裁定补掉。**只动探针，不碰任何游戏代码。**
> 这是对「Task 8 探针与 brief 逐字一致」的**有意增强**（增强 ≠ 削弱：断言只增不减）。

**Files:**
- Modify: `tests/kh_l1_probe.gd`

**Interfaces:**
- 不新增/改变任何对外接口；仅让两条断言真正生效

- [ ] **Step 1: 给 `settings.gd` 的源码读取补空值守卫**

现状（约 `tests/kh_l1_probe.gd:62-66`）只有「不含某子串」的断言——若 `_read_res` 返回 `""`，两条都会**空转通过**却仍打印 ALL-OK。改成与 `run_options.gd` 那条对称：

```gdscript
	var st_src := _read_res("res://core/settings.gd")
	if st_src == "":
		failures.append("core/settings.gd 读不到")
	else:
		if "old_ui" in st_src:
			failures.append("core/settings.gd 仍含 old_ui")
		if "sp_difficulty" in st_src:
			failures.append("core/settings.gd 仍含 sp_difficulty")
```

- [ ] **Step 2: 把恒假的 `LocalServer == null` 换成真检查**

`class_name` 标识符与 `null` 比较在编译期即恒假，那句永远不触发。改用全局类缓存查询（若 `class_name` 被摘掉/文件缺失，这条会真的失败）：

```gdscript
	# LocalServer 是 class_name(非 autoload):查全局类缓存,验它真的注册成功了
	var found_local_server := false
	for entry in ProjectSettings.get_global_class_list():
		if str(entry.get("class", "")) == "LocalServer":
			found_local_server = true
			break
	if not found_local_server:
		failures.append("LocalServer 未注册进全局类缓存(应来自 res://core/local_server.gd)")
```

删掉原来的 `if LocalServer == null: failures.append("LocalServer 未解析")` 两行。

- [ ] **Step 3: 文件头注释里的运行命令补 `--quit-after 300`**

把头部注释的跑法改为：

```
# 跑法: Godot_console --headless --path . --quit-after 300 res://tests/kh_l1_probe.tscn
```

原因：本探针在别处直接引用了 `Settings` / `NetBusExt` 等 autoload 标识符，**若某个 autoload 从 `project.godot` 被删，整个脚本会编译失败** → 场景加载成无脚本根节点 → 命令会**无输出挂死**。`--quit-after 300` 保证这种情况下最坏也只是超时退出，而不是挂死。

- [ ] **Step 4: 跑探针，确认仍全绿**

Run:
```bash
"$GODOT" --headless --path . --quit-after 300 res://tests/kh_l1_probe.tscn 2>&1 | tail -3
```
Expected: `KH L1 PROBE: ALL-OK`，退出码 0

- [ ] **Step 5: 反证新守卫真的会红（两条都要验）**

5a. **空值守卫**：把 `_read_res("res://core/settings.gd")` 临时改成 `_read_res("res://core/__no_such_file__.gd")`，重跑。

Expected: 输出含 `core/settings.gd 读不到`，退出码 1。**验完改回。**

5b. **LocalServer 守卫**：临时把 `ProjectSettings.get_global_class_list()` 的结果过滤条件改成 `== "NoSuchClass"`，重跑。

Expected: 输出含 `LocalServer 未注册进全局类缓存`，退出码 1。**验完改回。**

两条都改回后重跑 Step 4 确认恢复 `ALL-OK`，并确认 `git status` 里 `tests/kh_l1_probe.gd` 之外无改动。

- [ ] **Step 6: 提交**

```bash
git add tests/kh_l1_probe.gd
git commit -m "test(kh-merge): L1 探针加固——settings.gd 源码读取补空值守卫(原会空转通过)、LocalServer 改查全局类缓存(原断言恒假)、运行命令补 --quit-after 防编译失败挂死"
```

- [ ] **Step 7: 全项目自检不回归**

Run:
```bash
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'
```
Expected: `0`

---

## ⚠️ L2 实际执行顺序（控制者 2026-09-10 修正：编译依赖）

任务**编号保持不变**（便于账本对照），但**执行顺序**必须是：

```
Task 11 → Task 10 → Task 9 → Task 12 → Task 13 → Task 14 → Task 14b
```

**原因**：`core/explosion.gd`（Task 9）与 `scenes/weapons/bullet_base.gd`（Task 12）都要调用 `CombatFeedback.hit_marker()`，而 `CombatFeedback` 这个 `class_name` 由 **Task 11** 创建。GDScript 里引用未声明的全局类名是**解析错误**——若按编号顺序先做 Task 9，`core/explosion.gd` 会直接编译不过、整个项目起不来。

Task 10（受击红闪）不依赖 `CombatFeedback`，与 Task 11 无先后关系；放在 11 之后只是顺路。Task 11、Task 10 都只依赖 L1 的产出。

---

### Task 9: 爆炸内圈免疫掩护 + 归因 meta

**Files:**
- Test: `tests/explosion_falloff_probe.gd`（从 `$KH/Tests/explosion_falloff_probe.gd` 照搬）
- Modify: `core/explosion.gd`

**Interfaces:**
- Produces: `Explosion.cover_multiplier(d: float, radius: float, blocked: bool) -> float`（`static`，纯函数，`-s` 可测）；`apply_aoe` 对敌人写 `last_damager` meta、对玩家写 `last_damager` + `last_damager_time` meta，并调 `CombatFeedback.hit_marker()`
- 不变量：**任意距离合成伤害随距离不增**（`_falloff(d) × cover_multiplier(d)` 单调不增）

- [ ] **Step 1: 先把探针搬过来（TDD：此时必失败）**

```bash
cp "$KH/Tests/explosion_falloff_probe.gd" tests/explosion_falloff_probe.gd
```
探针内的 `res://Tests/...` 注释按映射改为 `res://tests/...`（仅注释，无功能影响）。

> **⚠️ 执行中发现的平台事实（2026-09-10，Task 9 实现者实测；原计划此处的「直接照搬 `-s` 跑」是错的）**
> KH 这份探针在本项目**无法原样运行**：它静态引用全局类 `Explosion`，而 `core/explosion.gd` 引用了 autoload `GameParameters`；`-s` 脚本在加载期解析全局类时拿不到 autoload 标识符 → **恒报 `Compile Error: Identifier not found: GameParameters`**，与 `cover_multiplier` 是否存在无关（`--import` 刷缓存无效，已实测；与 `tests/grenade_smoke.gd:70` 既有注释是同一回事）。
> 因此照搬时**必须**做项目惯例的适配：`_init` → `_initialize`，改为运行时 `load("res://core/explosion.gd")` 再经变量调用（`Exp.` 前缀），并加一句"类未实现则报 FAIL 并退出"的保护（否则 RED 会因运行期报错中断函数而**挂死**）。
> **断言、常量、分组必须与 KH 一字不差**——适配只许改"怎么拿到那个类"，不许改"验什么"。

- [ ] **Step 2: 跑探针，确认失败**

Run:
```bash
"$GODOT" --headless --path . -s res://tests/explosion_falloff_probe.gd 2>&1 | tail -5
```
Expected: FAIL —— 报 `Invalid call. Nonexistent function 'cover_multiplier'` 或 `SMOKE FAILED`。

- [ ] **Step 3: 实现 `cover_multiplier`**

在 `core/explosion.gd` 末尾（`_falloff` 之后）追加：

```gdscript


# 掩护衰减系数:内圈(≤INNER_FRACTION 半径)免疫遮挡——贴脸目标被墙棱角判"无视线"
# 扣 25% 会出现"爆心比开阔边缘伤害低"的倒挂(实测 bug);爆心贴脸时掩护不该生效。
# 纯函数(-s 可测);apply_aoe 的敌人/玩家分支共用,保证"任意距离伤害随距离不增"。
static func cover_multiplier(d: float, radius: float, blocked: bool) -> float:
	if d <= radius * INNER_FRACTION:
		return 1.0
	return BLOCKED_FRACTION if blocked else 1.0
```

- [ ] **Step 4: 跑探针，确认通过**

Run:
```bash
"$GODOT" --headless --path . -s res://tests/explosion_falloff_probe.gd 2>&1 | tail -3
```
Expected: `SMOKE OK`，退出码 0。

- [ ] **Step 5: 敌人分支改用 `cover_multiplier` 并加归因**

把该分支的

```gdscript
		# 遮挡(墙后)= 部分掩体:伤害/击退保留 BLOCKED_FRACTION;无遮挡全额
		var blocked := has_grid and not _has_los(center, e as Node2D, grid)
		var wmult := Water.water_mult((e as Node2D).global_position, grid)  # 目标在水里:×水格 decay
		var dmg := _falloff(d, radius, max_damage) * (BLOCKED_FRACTION if blocked else 1.0) * wmult
		if dmg <= 0:
			continue
		# set_velocity=true:爆炸击退覆盖原速度,严格沿爆心→目标径向(不叠加鸟自身飞行速度带偏)
		e.hurt(int(dmg), _outward_dir(center, (e as Node2D).global_position),
				_falloff(d, radius, max_knockback) * (BLOCKED_FRACTION if blocked else 1.0) * wmult, true)
```

改成

```gdscript
		# 遮挡(墙后)= 部分掩体;内圈免疫遮挡(见 cover_multiplier 注释)
		var blocked := has_grid and not _has_los(center, e as Node2D, grid)
		var cover := cover_multiplier(d, radius, blocked)
		var wmult := Water.water_mult((e as Node2D).global_position, grid)  # 目标在水里:×水格 decay
		var dmg := _falloff(d, radius, max_damage) * cover * wmult
		if dmg <= 0:
			continue
		# set_velocity=true:爆炸击退覆盖原速度,严格沿爆心→目标径向(不叠加鸟自身飞行速度带偏)
		e.hurt(int(dmg), _outward_dir(center, (e as Node2D).global_position),
				_falloff(d, radius, max_knockback) * cover * wmult, true)
		# 击杀归因 + 命中标记(单机:玩家榴弹炸到敌人;服务器进程无 CombatFeedback 实例则空转)
		if shooter != null and is_instance_valid(shooter) and shooter != e:
			e.set_meta("last_damager", shooter)
		CombatFeedback.hit_marker()
```

- [ ] **Step 6: 玩家分支改用 `cover_multiplier` 并加归因**

把该分支的

```gdscript
			var blocked := has_grid and not _has_los(center, pp, grid)
			var mult := BLOCKED_FRACTION if blocked else 1.0
			mult *= Water.water_mult(pp.global_position, grid)  # 目标在水里:×0.25
			# 击退随距离衰减传入玩家(独立击退向量结算);ignore_iframes=true 穿透无敌帧
			pp.take_hit(center, int(_falloff(d, radius, max_damage) * mult), true,
					_falloff(d, radius, max_knockback) * mult)
			# PvP 击杀计分统一在 MatchHost(对方死亡都算),此处不需记射手 meta
```

改成

```gdscript
			# 同敌人:内圈免疫遮挡衰减
			var blocked := has_grid and not _has_los(center, pp, grid)
			var mult := cover_multiplier(d, radius, blocked)
			mult *= Water.water_mult(pp.global_position, grid)  # 目标在水里:×0.25
			# 击退随距离衰减传入玩家(独立击退向量结算);ignore_iframes=true 穿透无敌帧
			pp.take_hit(center, int(_falloff(d, radius, max_damage) * mult), true,
					_falloff(d, radius, max_knockback) * mult)
			# 击杀归因(大乱斗 RoyaleHost 读 last_damager 判击杀分);1v1 MatchHost 不读,无行为变化
			if shooter != null and shooter != pp:
				pp.set_meta("last_damager", shooter)
				pp.set_meta("last_damager_time", Time.get_ticks_msec())   # 归因时效
```

> **1v1 计分规则不变**（规格 §2 归因口径说明）：`MatchHost` 仍是「对方死亡都算」，这两行 meta 只是给反馈层/大乱斗读的附加信息。

- [ ] **Step 7: 跑探针 + 既有爆炸冒烟回归**

Run:
```bash
"$GODOT" --headless --path . -s res://tests/explosion_falloff_probe.gd 2>&1 | tail -3
"$GODOT" --headless --path . -s res://tests/grenade_smoke.gd 2>&1 | tail -5
```
Expected: 前者 `SMOKE OK`；后者输出与改动前一致（`grenade_smoke` 有一处**既存**失败「墙后敌人保留 75% 伤(26)」，改动前后都失败即正常，见规格不动）。

---

### Task 10: 受击红闪（post_process + shader + combat_component）

**Files:**
- Modify: `render/post_process.gd`
- Modify: `shaders/post_process.gdshader`
- Modify: `scenes/player/combat_component.gd`

**Interfaces:**
- Produces: `PostProcess.flash_hit(strength := 1.0) -> void`；shader uniform `hit_red : hint_range(0,1)`；`combat.take_hit` 每次命中调 `flash_hit` 并给小伤害震屏
- Consumes: `render/post_process.gd` 已在 `_ready` 里 `add_to_group("post_process")`（main 既有，无需改）

- [ ] **Step 1: `render/post_process.gd` 加红闪状态与方法**

在 `var _mat: ShaderMaterial = null` 之后加：

```gdscript
var _hit_red: float = 0.0   # 受击红闪当前强度(每帧衰减,见 flash_hit)
```

在文件末尾 `set_downed()` 之后追加：

```gdscript


# 受击红闪:strength 0-1(伤害越大越红),约 0.25s 衰减完。
# combat.take_hit(单机)与 PvP hit_event→take_hit 都走这里,不用重复接线。
func flash_hit(strength: float = 1.0) -> void:
	_hit_red = maxf(_hit_red, clampf(strength, 0.0, 1.0))

func _process(delta: float) -> void:
	if _hit_red > 0.0:
		_hit_red = maxf(_hit_red - delta * 4.0, 0.0)   # 衰减放慢:微红可感知地停留 ~0.25s
		if _mat != null:
			_mat.set_shader_parameter("hit_red", _hit_red)
```

- [ ] **Step 2: shader 加 uniform 与混合**

在 `uniform float desat : hint_range(0.0, 1.0) = 0.0;` 之后加：

```glsl
// 受击红闪:0=无,1=纯红罩。由 PostProcess.flash_hit() 置峰后每帧衰减。
uniform float hit_red : hint_range(0.0, 1.0) = 0.0;
```

在 `COLOR = texture(screen_tex, uv);` 之后、`if (desat > 0.0)` 之前加：

```glsl
	if (hit_red > 0.0) {
		// 画面微红一瞬间:向红色偏移(保留暗部细节,不做纯色覆盖);0.6 = 可感知但不糊画面
		COLOR.rgb = mix(COLOR.rgb, vec3(0.85, 0.05, 0.05), hit_red * 0.6);
	}
```

- [ ] **Step 3: `combat_component.take_hit` 接线**

在 `took_hit.emit(source_pos, damage)` 之后加 `Sfx.play("hurt")`。

在爆炸/直击击退结算之后、`# 大伤害反馈` 注释之前，把

```gdscript
	# 大伤害反馈:一次扣血 >25% 最大血 → 相机震动(幅度随伤害比例增强)
	var hit_ratio := float(damage) / float(max_hp)
	if hit_ratio > 0.25:
		var cam: Camera2D = body.get_viewport().get_camera_2d()
		if cam != null and cam.has_method("shake"):
			cam.shake(PlayerParams.hit_cam_shake * (hit_ratio / 0.25), PlayerParams.hit_cam_shake_time)
```

改成

```gdscript
	# 受击反馈(每次命中):画面微红一瞬间 + 小幅屏幕震动。
	# 大伤害(>25% 最大血)原有的强震保持不变(下面 hit_ratio 分支)。
	var tree := body.get_tree()
	if tree != null:
		var pp := tree.get_first_node_in_group("post_process")
		if pp != null and pp.has_method("flash_hit"):
			pp.flash_hit(clampf(float(damage) / float(max_hp) * 2.0, 0.5, 1.0))
	# 大伤害反馈:一次扣血 >25% 最大血 → 相机震动(幅度随伤害比例增强)
	var hit_ratio := float(damage) / float(max_hp)
	var cam: Camera2D = body.get_viewport().get_camera_2d()
	if cam != null and cam.has_method("shake"):
		if hit_ratio > 0.25:
			cam.shake(PlayerParams.hit_cam_shake * (hit_ratio / 0.25), PlayerParams.hit_cam_shake_time)
		else:
			cam.shake(6.0, 0.15)   # 小伤害也给一点震感
```

> **改名冲突提示**：`var tree` 在同函数后段（`_downed()`/`revive()` 里也有 `var tree`）可能重名——若报 `already declared`，把新加的那个改名为 `fb_tree`。以实际报错为准。

- [ ] **Step 4: 验证 shader 编译与启动**

Run:
```bash
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -iE 'shader|error|parse' | head -20
```
Expected: 无 shader 编译错误、无 `Parse Error`。

- [ ] **Step 5: 交互验证留给 L6 之后的真机实测**

单机受击红闪需要 GUI 实测（headless 不渲染）；本步只保证编译与启动无误，真机验证写进交付说明。

---

### Task 11: CombatFeedback（命中 X / 击杀播报 / 骷髅连杀）+ 单机接线

**Files:**
- Create: `scenes/effects/combat_feedback.gd`（从 `$KH/Scenes/Effects/combat_feedback.gd` 照搬，261 行）
- Test: `tests/feedback_probe.gd` + `tests/feedback_probe.tscn`（从 `$KH/Tests/` 照搬）
- Modify: `scenes/level_0.gd`（`_ready` 里挂载）
- Modify: `scenes/enemies/enemy_base.gd`（`_begin_death` 播报）

**Interfaces:**
- Produces: `class_name CombatFeedback`（`extends CanvasLayer`，layer 131）；静态 `spawn(host)`、`hit_marker()`、`kill(who: String)`、`reset_streak()`、`notify_enemy_killed(victim: Node)`、`enemy_display_name(victim) -> String`；`static var current`；常量 `ENEMY_NAMES`
- Consumes: `Sfx.play("kill")`（Task 1）、`last_damager` meta（Task 9/12）

- [ ] **Step 1: 先搬探针（TDD：此时必失败）**

```bash
cp "$KH/Tests/feedback_probe.gd" tests/feedback_probe.gd
cp "$KH/Tests/feedback_probe.tscn" tests/feedback_probe.tscn
```

把探针里的 `res://Scenes/Effects/combat_feedback.gd` 改成 `res://scenes/effects/combat_feedback.gd`；`.tscn` 里的脚本路径同理。

- [ ] **Step 2: 跑探针，确认失败**

Run:
```bash
"$GODOT" --headless --path . res://tests/feedback_probe.tscn 2>&1 | tail -5
```
Expected: FAIL（`combat_feedback.gd` 不存在 → `load()` 返回 null 后调用即报错）。

- [ ] **Step 3: 搬入实现**

```bash
cp "$KH/Scenes/Effects/combat_feedback.gd" scenes/effects/combat_feedback.gd
grep -n 'res://' scenes/effects/combat_feedback.gd
```
Expected: 只有 `res://assets/fonts/less_perfect_dos_vga.ttf`（main 已有同文件，**不改**）。

- [ ] **Step 4: 单机挂载**

在 `scenes/level_0.gd` 的 `_ready()` 里（`RenderingServer.set_default_clear_color("b0e5f6")` 之后加一行即可）：

```gdscript
	CombatFeedback.spawn(self)
```

- [ ] **Step 5: 敌人死亡播报**

在 `scenes/enemies/enemy_base.gd` 的 `_begin_death()` 里，`died.emit()` 之后加：

```gdscript
	# 击杀播报(CombatFeedback):只有玩家造成的死亡才出「击杀 XXX」文字+音效
	# (子弹/爆炸命中时写入的 last_damager meta 归因;溺水等环境死安静销毁)
	CombatFeedback.notify_enemy_killed(self)
```

> **守卫点**：**不要**照抄 KH 的 `enemy_base.gd`——那版同时删掉了 main 的 `_players_frame()` 每帧缓存（性能优化）。本步只加这 3 行。

- [ ] **Step 6: 跑探针，确认通过**

Run:
```bash
"$GODOT" --headless --path . res://tests/feedback_probe.tscn 2>&1 | tail -3
```
Expected: `FEEDBACK PROBE: ALL-OK(击杀播报/X 标记/归因/音效 全部通过)`，退出码 0。

- [ ] **Step 7: 既有敌人冒烟回归**

Run:
```bash
"$GODOT" --headless --path . -s res://tests/enemy_logic_smoke.gd 2>&1 | tail -3
```
Expected: `SMOKE OK`。

---

### Task 12: 子弹命中归因 + 音效（保持 main 的碎片语义）

**Files:**
- Modify: `scenes/weapons/bullet_base.gd`
- Modify: `tests/feedback_probe.gd`（新增「写入方」断言段——见 Step 6）

**Interfaces:**
- Produces: `BulletBase._register_player_hit(target: Node) -> void`；命中敌人播 `Sfx.play("hit")`；爆炸播 `Sfx.play("explosion")`
- **不变量（守卫点）**：撞可破坏砖时 `TileHitFx.spawn` 仍是**无条件**播放（视觉副本也要有即时反馈），`TileDefs.damage_tile` 仍只在 `apply_damage=true` 时执行

- [ ] **Step 1: 加归因方法**

在 `_direct_hit()` 之后加：

```gdscript


# 玩家子弹命中实体的统一收尾:击杀归因 meta(敌死时 CombatFeedback 读它播「击杀 XXX」)
# + 命中 X 标记。headless 服务器进程无 CombatFeedback 实例 → hit_marker 空操作,无副作用。
func _register_player_hit(target: Node) -> void:
	var who := shooter
	if who == null and is_instance_valid(source):
		who = source
	if who != null and who != target:
		target.set_meta("last_damager", who)
	CombatFeedback.hit_marker()
```

- [ ] **Step 2: 两个命中敌人的分支各加两行**

在 `_direct_hit()` 里 `hit.hurt(direct_hit_damage, dir)` 之后加 `_register_player_hit(hit)`。

在 `_collide`（命中敌人）的两处 `hurt` 调用之后各加：

```gdscript
			_register_player_hit(hit)
			Sfx.play("hit")
```

- [ ] **Step 3: 爆炸音效**

`_explode()` 的第一行加 `Sfx.play("explosion")`。

- [ ] **Step 4: 守卫点自检——撞砖分支必须保持 main 原样**

Run:
```bash
grep -n -A3 'TileHitFx.spawn' scenes/weapons/bullet_base.gd
```
Expected: 输出形如

```
				TileHitFx.spawn(get_viewport(), pos, tex)   # 纯反馈:命中可破坏砖就播
				if apply_damage:
					TileDefs.damage_tile(cell, hit_damage, "bullet")
```

**若出现 `if apply_damage:` 把 `TileHitFx.spawn` 包起来的形态（KH 版），说明照抄错了，改回上面这版。**

- [ ] **Step 5: 回归**

Run:
```bash
"$GODOT" --headless --path . -s res://tests/enemy_logic_smoke.gd 2>&1 | tail -3
"$GODOT" --headless --path . -s res://tests/grenade_smoke.gd 2>&1 | tail -5
```
Expected: `enemy_logic_smoke` = `SMOKE OK`；`grenade_smoke` 与 Task 9 的基线一致（同一处既存失败）。

- [ ] **Step 6: 补「写入方」断言（闭环 Task 11 评审的 Important：归因链路此前零覆盖）**

> **为什么必须做**：Task 11 的 `feedback_probe` 是**自己造 `last_damager` meta** 再验"读端"的谓词。main 里**没有任何代码写这个 meta**，所以那条 GREEN **不可能**因"写入方缺失或写错形状"而变红——归因链路端到端零覆盖。本条补上**写端**。

在 `tests/feedback_probe.gd` 的 `_ready()` 末尾（`_finish(failures)` 之前）加一段：

```gdscript
	# ── 写入方覆盖(Task 12 闭环):真实 BulletBase._register_player_hit 必须落 last_damager ──
	var bullet_scene: PackedScene = load("res://scenes/weapons/bullet.tscn")
	if bullet_scene == null:
		failures.append("bullet.tscn 载入失败,无法验证写入方")
	else:
		var shooter := _make_player()
		# 正例:射手 ≠ 目标 → 必须写 meta 且值就是射手
		var b: Node = bullet_scene.instantiate()
		add_child(b)
		b.set("shooter", shooter)
		var victim_ok := _victim_killed_by(null)     # 注意:这个 helper 不设 meta
		b.call("_register_player_hit", victim_ok)
		if not victim_ok.has_meta("last_damager"):
			failures.append("_register_player_hit 未写入 last_damager(写端缺失/写错)")
		elif victim_ok.get_meta("last_damager") != shooter:
			failures.append("last_damager 写的不是射手")
		# 反例:射手 == 目标 → 不得写 meta(自伤不应归因给自己)
		var self_hit := _victim_killed_by(null)
		b.set("shooter", self_hit)
		b.call("_register_player_hit", self_hit)
		if self_hit.has_meta("last_damager"):
			failures.append("射手==目标时不应写 last_damager")
		b.queue_free()
```

> `_victim_killed_by(null)` 是既有 helper，传 null 时**不设** meta，正好用作"干净目标"。若 `bullet.tscn` 的根节点脚本不是 `BulletBase`（如已改为其它基类），以实际类型为准调整 `call(...)` 的目标。

- [ ] **Step 7: 跑探针确认新断言真的生效（红→绿）**

先跑一次确认**绿**：

```bash
"$GODOT" --headless --path . --quit-after 600 res://tests/feedback_probe.tscn 2>&1 | tail -3
```
Expected: `FEEDBACK PROBE: ALL-OK(...)`

再做**反证**：临时把 `_register_player_hit` 里那两行 `target.set_meta("last_damager", who)` 注释掉，重跑。

Expected: 输出含 `_register_player_hit 未写入 last_damager`，退出码 1。**验完立刻恢复并重跑确认 ALL-OK。**

- [ ] **Step 8: 提交**

```bash
git add scenes/weapons/bullet_base.gd tests/feedback_probe.gd
git commit -m "feat(kh-merge): L2 子弹命中归因+音效——BulletBase._register_player_hit 写 last_damager 与命中X;Sfx hit/explosion;探针补写入方断言(闭环归因链路零覆盖)"
```

---

### Task 13: 搬入三个 PvP 视觉件（本层仅保证解析）

**Files:**
- Create: `scenes/effects/bullet_trail.gd`（`$KH/Scenes/Effects/bullet_trail.gd`，26 行）
- Create: `scenes/effects/minimap.gd`（`$KH/Scenes/Effects/minimap.gd`，114 行）
- Create: `scenes/player/enemy_hp_bar.gd`（`$KH/Scenes/Player/enemy_hp_bar.gd`，22 行）

**Interfaces:**
- Produces: `class_name BulletTrail`（`static attach(bullet: Node2D, color: Color)`）；`class_name Minimap`（`setup(local_provider, enemy_provider)`、`setup_multi(local_provider, others_provider)`）；`class_name EnemyHpBar`（`ratio: float`）
- Consumes: `Settings.pvp_minimap_show_enemy`（Task 2）、`Water.is_liquid` / `MazeGenerator.wrap_to_range`（main 已有）
- **接线在 L6**：本层只搬入并保证解析通过，不挂到任何场景

> **⚠️ 供 L6 / L5 的硬性约束（控制者 2026-09-10 裁定）**
> `CombatFeedback` 的**唯一挂载点**是 `scenes/level_0.gd` 的 `_ready()` 顶部（Task 11 已落地，位于 `pvp_mode`/`menu_demo` 等早退**之前**），因此它**自动覆盖单机 / PvP / 大乱斗**三种模式（三者都经 `Level0`）。
> 所以 **L6 的 `pvp_client.gd` 与 L5 的 `royale_game.gd` 都不要再调 `CombatFeedback.spawn()`**。
> 原因：`spawn()` 的幂等判据是 `current != null`，而 `current` 要到 `_ready` 才赋值、`add_child` 又是 `call_deferred` —— **同一帧内两次 `spawn()` 会建出两个实例**（两个 X 标记/两行播报重叠）。
> 这与 KH 的做法不同（KH 把 spawn 放在单机路径末尾、由各客户端场景自己挂），是**有意的简化**：单一挂载点从构造上消除了双实例隐患。

- [ ] **Step 1: 复制三个文件**

```bash
cp "$KH/Scenes/Effects/bullet_trail.gd" scenes/effects/bullet_trail.gd
cp "$KH/Scenes/Effects/minimap.gd"     scenes/effects/minimap.gd
cp "$KH/Scenes/Player/enemy_hp_bar.gd" scenes/player/enemy_hp_bar.gd
grep -n 'res://' scenes/effects/bullet_trail.gd scenes/effects/minimap.gd scenes/player/enemy_hp_bar.gd
```
Expected: 无输出（三者都不含 `res://` 字面量）。

- [ ] **Step 2: 解析自检**

Run:
```bash
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -iE 'error|parse' | head -20
```
Expected: 无 `Parse Error`。

---

### Task 14: L2 验收 + 提交

**Files:**
- 无新增；跑既有 + 新增探针

- [ ] **Step 1: 跑全量既有冒烟（回归）**

Run:
```bash
"$GODOT" --headless --path . -s res://tests/enemy_logic_smoke.gd 2>&1 | tail -3
"$GODOT" --headless --path . -s res://tests/water_probe.gd 2>&1 | tail -3
"$GODOT" --headless --path . -s res://tests/explosion_falloff_probe.gd 2>&1 | tail -3
"$GODOT" --headless --path . res://tests/feedback_probe.tscn 2>&1 | tail -3
"$GODOT" --headless --path . res://tests/kh_l1_probe.tscn 2>&1 | tail -3
```
Expected: 全部 `SMOKE OK` / `ALL-OK` / `KH L1 PROBE: ALL-OK`。

- [ ] **Step 2: 全项目启动 + 导入自检（路径改写兜底）**

Run:
```bash
"$GODOT" --headless --path . --import 2>&1 | grep -iE 'case mismatch|hides a global|Parse Error' | head -20
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -iE 'error|parse' | head -20
```
Expected: 两条都无输出（零 case mismatch / 零解析错误）。

- [ ] **Step 3: 收尾提交（仅当仍有未提交的产出时）**

Task 9–13 各自已在结束时提交。本任务通常无文件改动 → **无提交是正常的**；若上面某步补了文件（例如探针修正），才执行：

```bash
git add <本任务实际改动的文件>
git commit -m "chore(kh-merge): L2 验收修正"
```

- [ ] **Step 4: 交付说明（写给用户）**

在回复里列明：
- 本层改了哪些文件、跑通了哪些探针（贴输出）
- **需要用户真机实测的点**：
  1. **★ 必测（端到端归因链的唯一证明）**：单机真机**打鸟**——用枪打死一只飞鸟/跳鸟，确认屏幕中央真的出现「击杀 飞鸟/跳鸟」+ 像素骷髅 + 音效。原因：Task 11 的探针是**自造 `last_damager` meta** 验读端；真实写入方（Task 12 的 `_register_player_hit` / Task 9 的爆炸）在探针里只能验到"函数会写 meta"这一层，**真实世界里的命中→归因→死亡→播报这条链唯有真机跑一次才算验过**（Task 11 评审的 Important 结论）。**这条不通过，L2 不算完成。**
  2. 受击红闪与震屏手感（每次中弹微红 + 小震；大伤害强震不变）
  3. 连杀计数（6s 窗口内连续击杀应显示 x2、x3；自己被击杀清零）
  4. 爆炸贴脸 vs 开阔边缘的伤害倒挂是否消失（内圈免疫掩护）
- **已知不做**：三个 PvP 视觉件未接线（L6）；AI 补位未接界面；大乱斗未做（L5）；8bit 音效听感（L1 遗留）

---

### Task 14b: L2 视觉验收（截图，控制者读图判定）

> **用户 2026-09-10 裁定新增**：L2 的四个视觉特征（受击红闪 / 命中 X / 击杀播报 / 骷髅连杀）headless 验不了（`--headless` 不渲染）。改为**带真实渲染跑一次探针、把每个状态定格成 PNG**，由控制者（能直接看图）判对错，而不是"看着代码觉得对"。
> 本任务**同时**产出数值断言（可 CI）与 PNG（可肉眼审）——两条腿，缺一不可。

**Files:**
- Create: `tests/kh_visual_probe.gd`
- Create: `tests/kh_visual_probe.gd.uid`（Godot 导入时生成，随行提交）
- Create: `tests/kh_visual_probe.tscn`（照 `tests/feedback_probe.tscn` 的 6 行写法）
- 产物（**不入库**）：`res://.superpowers/sdd/_visual_*.png`（该目录已被 `.superpowers/sdd/.gitignore` 忽略）

**Interfaces:**
- Consumes: `PostProcess.flash_hit(strength)`（Task 10）、`CombatFeedback.spawn/hit_marker/kill`（Task 11）
- Produces: 5 张 PNG + 终端逐项 `[VISUAL] ...` 断言行；成功打印 `KH VISUAL PROBE: ALL-OK` 并 `quit(0)`

**跑法（关键：不能加 `--headless`，加了就不渲染、`get_image()` 返回 null）**：

```bash
"$GODOT" --path . --quit-after 600 res://tests/kh_visual_probe.tscn
```

- [ ] **Step 1: 写探针 `tests/kh_visual_probe.gd`**

要点（骨架如下；窗口尺寸以 `get_viewport().get_visible_rect().size` 实取，不要写死）：

```gdscript
extends Node

# L2 视觉验收探针(必须带真实渲染,--headless 下 get_image() 返回 null):
#   Godot_console --path . --quit-after 600 res://tests/kh_visual_probe.tscn
# 把受击红闪 / 命中 X / 击杀播报 / 连杀分别定格成 PNG 供控制者读图,同时打数值断言。

const OUT_DIR := "res://.superpowers/sdd"

var _failures: Array[String] = []
var _pp: PostProcess = null
var _vp: SubViewport = null


func _ready() -> void:
	var win := get_viewport().get_visible_rect().size
	# 世界视口:一块纯色中灰,铺满(便于看红闪偏色)
	_vp = SubViewport.new()
	_vp.size = Vector2i(win)
	_vp.render_target_update_mode = SubViewport.UPDATE_ALWAYS
	add_child(_vp)
	var bg := ColorRect.new()
	bg.color = Color(0.22, 0.26, 0.30)
	bg.size = win
	_vp.add_child(bg)

	# 后处理(受击红闪挂在它身上)
	_pp = PostProcess.new()
	_pp.world_viewport = _vp
	add_child(_pp)

	# 打击反馈层
	CombatFeedback.spawn(self)

	await get_tree().process_frame
	await get_tree().process_frame

	# 0) 基线
	var base := await _shot("_visual_0_baseline.png")
	var base_px := _center(base)

	# 1) 受击红闪:flash_hit(1.0) 后中心像素应明显偏红
	_pp.flash_hit(1.0)
	await get_tree().process_frame
	var flash := await _shot("_visual_1_hitflash.png")
	var flash_px := _center(flash)
	_check(flash_px.r > base_px.r + 0.15, "受击红闪未让画面变红(base.r=%.2f flash.r=%.2f)" % [base_px.r, flash_px.r])
	_check(flash_px.r > flash_px.b, "受击红闪偏色不对(r 应 > b)")
	print("[VISUAL] 红闪 基线中心=(%.2f,%.2f,%.2f) 闪后=(%.2f,%.2f,%.2f)" % [base_px.r, base_px.g, base_px.b, flash_px.r, flash_px.g, flash_px.b])

	# 让红闪衰减掉,免得污染后面几张
	await get_tree().create_timer(0.6).timeout

	# 2) 命中 X 标记:调后应可见,且在中心
	CombatFeedback.hit_marker()
	await get_tree().process_frame
	await get_tree().create_timer(0.06).timeout    # X 存活 0.22s,取中段
	await _shot("_visual_2_hitmarker.png")
	_check(CombatFeedback.current != null and CombatFeedback.current._marker.visible, "命中 X 标记未显示")

	# 等 X 消失
	await get_tree().create_timer(0.4).timeout
	_check(not CombatFeedback.current._marker.visible, "命中 X 标记未按时消失")

	# 3) 击杀播报(含骷髅 + x1)
	CombatFeedback.kill("测试鸟")
	await get_tree().process_frame
	await get_tree().create_timer(0.12).timeout
	await _shot("_visual_3_kill.png")
	var txt: String = CombatFeedback.current._kill_label.text
	_check("测试鸟" in txt, "击杀播报文本里没有受害者名(实际:%s)" % txt)

	# 4) 连杀:窗口内再杀一只 → x2
	CombatFeedback.kill("第二只")
	await get_tree().process_frame
	await get_tree().create_timer(0.12).timeout
	await _shot("_visual_4_streak.png")
	_check(CombatFeedback.current._streak_label.text == "x2", "连杀计数不对(实际:%s)" % CombatFeedback.current._streak_label.text)

	_finish()


# 截图:等两帧确保画完,存 PNG 并返回 Image(供数值断言)
func _shot(name: String) -> Image:
	await get_tree().process_frame
	await get_tree().process_frame
	var img := get_viewport().get_texture().get_image()
	if img == null:
		_failures.append("截图 %s 失败(get_image 返回 null——是不是误加了 --headless?)" % name)
		return Image.new()
	img.save_png(OUT_DIR.path_join(name))
	print("[VISUAL] 已存 ", OUT_DIR.path_join(name), " ", img.get_width(), "x", img.get_height())
	return img


func _center(img: Image) -> Color:
	if img.get_width() == 0:
		return Color.BLACK
	return img.get_pixel(img.get_width() / 2, img.get_height() / 2)


func _check(ok: bool, msg: String) -> void:
	if not ok:
		_failures.append(msg)


func _finish() -> void:
	if _failures.is_empty():
		print("KH VISUAL PROBE: ALL-OK")
		get_tree().quit(0)
	else:
		print("KH VISUAL PROBE: FAIL | " + "; ".join(_failures))
		get_tree().quit(1)
```

- [ ] **Step 2: 跑探针（**不带 `--headless`**）**

Run:
```bash
"$GODOT" --path . --quit-after 600 res://tests/kh_visual_probe.tscn 2>&1 | tail -12
```
Expected: 5 行 `[VISUAL] 已存 ...` + `KH VISUAL PROBE: ALL-OK`，退出码 0

- [ ] **Step 3: 确认产物存在且仓库没被污染**

```bash
ls -la .superpowers/sdd/_visual_*.png
git status --short          # 不应出现任何 _visual_*.png
```
Expected: 5 个 PNG；`git status` 只有本任务的两个源文件（未跟踪）

- [ ] **Step 4: 提交**

```bash
git add tests/kh_visual_probe.gd tests/kh_visual_probe.gd.uid tests/kh_visual_probe.tscn
git commit -m "test(kh-merge): L2 视觉验收探针——冻结受击红闪/命中X/击杀播报/连杀四态为 PNG + 数值断言(红闪偏色、X 显隐、播报文本、连杀计数)"
```

- [ ] **Step 5: 把 5 张 PNG 的路径写给控制者**

在回报里列出绝对路径，**并明确说明**：控制者需读图判定四件事——(1) 第 2 张整幅明显偏红；(2) 第 3 张屏幕中心有白色 X（四段斜线、中心留缺口）；(3) 第 4 张中央有「击杀 测试鸟」+ 像素骷髅 + `x1`；(4) 第 5 张骷髅右侧是 `x2`。任一张与描述不符即为视觉缺陷，需回报控制者。

---

## 附：给执行者的关键提醒

1. **`-s` 与场景探针的区别**：`-s` 阶段 autoload 未实例化。`kh_l1_probe` / `feedback_probe` 必须用 `.tscn` 跑；`explosion_falloff_probe` 是纯静态函数探针，`-s` 跑。
2. **不要照抄 `enemy_base.gd` / `bullet_base.gd` 整文件**——KH 版把 main 的性能优化与撞砖碎片语义改差了（守卫点）。这两处只按本计划的步骤做加法。
3. **`user://settings.cfg` 是本机既有存档**：不要断言 Settings 的默认值；改动后第一次 `save()` 会重写并清掉废弃键。
4. **用户既有的未提交改动**（`core/net_bus.gd` 的 `ENet_CHANNELS`、`docs/pvp-networking.md`）**不属于本计划**，不许动、不许一起提交。
5. **KH 的 `AGENTS.md` 有命名漂移**：它把 AI 相关的接口写成 `NetBusExt.ai_duel_requested` / `royale_start_ai_requested`，实际这两个是**信号**，RPC 方法名是 `ai_duel` / `royale_start_ai`。后续 L5 写 AI/大乱斗接线时以**源码**为准，别信文档。
