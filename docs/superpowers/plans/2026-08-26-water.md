# 水(Water)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 2D 平台 demo 中实现水体:分层渲染 + 水面晃动、浮力/游泳物理、溺水、FlyBird 避水、爆炸衰减 25%、子弹水中阻力、水粒子效果。

**Architecture:** 新增静态 `Water` 助手(判水/水面线/没顶)供玩家/敌人/子弹/特效共用;主角加 `SwimComponent`,敌人逻辑进 `EnemyBase`;渲染在 Level0 增加 `WaterLayer` + `WaterSurfaceLayer` 两个 TileMapLayer 分层铺图,水面层每帧 sin 晃动;`WaterFx` 粒子组件运行期挂到主角/敌人。Spec:`docs/superpowers/specs/2026-08-26-water-design.md`。

**Tech Stack:** Godot 4.7.1 标准版(GDScript),TileMapLayer + CPUParticles2D,无 mono。

## Global Constraints

- 运行 Godot 用绝对路径(不在 PATH):`"D:/Program Files/Godot_v4.7.1-stable_win64/Godot_v4.7.1-stable_win64_console.exe"`。
- **测试由用户自己跑,不要代跑**;headless `-s` 探针在复杂改动下实现者可代跑自检,但最终确认交给用户。
- 瓦片 64px/格,碰撞子格 32px。纹理 **21=水体、22=水面**,两者 `type="liquid"`,`explosion_decay=0.25`。
- `-s` 阶段 autoload 未实例化:静态可测代码(**Water.gd、probe**)不得引用 autoload(用本地 `const TILE_TS := 64`),避免连带预加载。
- 每次改动前先 `git status` 看用户是否并发改过相关文件,别用旧快照覆盖。
- 地图里只存纹理 21;水面(22)由渲染自动派生。编辑器调色板扩到 22。

---
## 文件结构

- **Create** `Globals/water.gd` — Water 静态助手。
- **Create** `Scenes/Player/swim_component.gd` — 主角水中物理组件。
- **Create** `Scenes/Effects/water_fx.gd` — 水粒子组件。
- **Create** `Tests/water_probe.gd` — 水逻辑 `-s` 冒烟探针(用户跑)。
- **Modify** `Globals/tile_defs.json` — 加 21/22。
- **Modify** `Globals/tile_defs.gd` — MAX_TEXTURE 22 + `explosion_decay_of`。
- **Modify** `Globals/maze_generator.gd` — 纹理字符 L/M。
- **Modify** `Globals/gameParameters.gd` — 水参数。
- **Modify** `Globals/playerParams.gd` — 游泳参数。
- **Modify** `Globals/enemyParams.gd` — 敌人游泳/溺水参数。
- **Modify** `Globals/explosion.gd` — 目标在水中爆炸 ×decay。
- **Modify** `Scenes/level_0.gd` + `Scenes/Level0.tscn` — 分层渲染 + 晃动。
- **Modify** `Scenes/Player/player.gd` + `Scenes/Player/Player.tscn` — Swim 接线 + WaterFx。
- **Modify** `Scenes/Enemies/enemy_base.gd` — 水物理 + 溺水 + 钩子 + WaterFx。
- **Modify** `Scenes/Enemies/enemy_fly_base.gd` — FlyBird 避水。
- **Modify** `Scenes/Enemies/enemy_jump_bird.gd` + `enemy_black_bird.gd` — 游泳钩子覆写。
- **Modify** `Scenes/Weapons/bullet_base.gd` + `Scenes/Enemies/enemy_bullet.gd` — 子弹阻力。
- **Modify** `editor/structure-editor.html` — 调色板扩到 22;重跑 `node editor/sync-tiles.js`。

---

### Task 1: 瓦片定义与纹理字符(21/22 + explosion_decay)

**Files:**
- Modify: `Globals/tile_defs.json`(加 21/22)
- Modify: `Globals/tile_defs.gd`(MAX_TEXTURE、explosion_decay_of)
- Modify: `Globals/maze_generator.gd`(L/M 字符)
- Test: `Tests/water_probe.gd`(新建,本任务先建骨架)

**Interfaces:**
- Produces: `TileDefs.explosion_decay_of(texture:int)->float`(逐格,回落全局);`MazeGenerator._texture_char_to_value("L")==21`、`_value_to_texture_char(21)=="L"`。

- [ ] **Step 1: 写探针骨架(先跑通"失败")**

Create `Tests/water_probe.gd`:
```gdscript
extends SceneTree
# 水逻辑冒烟。用户自跑:
#   "D:/Program Files/Godot_v4.7.1-stable_win64/Godot_v4.7.1-stable_win64_console.exe" --headless --path . -s res://tests/water_probe.gd
# 成功打印 WATER OK 退出 0。-s 阶段 autoload 未实例化,静态代码不得引 autoload。

var _failed := false
func _check(cond: bool, msg: String) -> void:
	if not cond:
		_failed = true
		push_error("FAIL: " + msg)
func _check_eq(a, b, msg: String) -> void:
	if a != b:
		_failed = true
		push_error("FAIL: %s (got %s want %s)" % [msg, a, b])
func _check_approx(a: float, b: float, tol: float, msg: String) -> void:
	if absf(a - b) > tol:
		_failed = true
		push_error("FAIL: %s (got %s want %s)" % [msg, a, b])

func _init() -> void:
	MazeGenerator.current_grid = _make_grid()
	TileDefs.load_defs()

	_check_eq(MazeGenerator._texture_char_to_value("L"), 21, "L->21")
	_check_eq(MazeGenerator._texture_char_to_value("M"), 22, "M->22")
	_check_eq(MazeGenerator._value_to_texture_char(21), "L", "21->L")
	_check_eq(TileDefs.type_of(21), "liquid", "tex21 type liquid")
	_check_approx(TileDefs.explosion_decay_of(21), 0.25, 1e-6, "tex21 decay 0.25")
	_check_approx(TileDefs.explosion_decay_of(15), TileDefs.explosion_decay(), 1e-6, "落叶回落全局 0.75")

	if _failed:
		quit(1)
	else:
		print("WATER OK")
		quit(0)

# 10×10 合成网格:y=5 整行水(tex21 全砖),y=6 整行实体墙,其余空气。
func _make_grid() -> Array[Array]:
	var grid: Array[Array] = []
	for y in range(10):
		var row: Array[int] = []
		for x in range(10):
			if y == 5:
				row.append(MazeGenerator.pack(21, 15))
			elif y == 6:
				row.append(MazeGenerator.SOLID)
			else:
				row.append(MazeGenerator.EMPTY)
		grid.append(row)
	return grid
```

- [ ] **Step 2: 跑探针确认失败**

Run: `"D:/Program Files/Godot_v4.7.1-stable_win64/Godot_v4.7.1-stable_win64_console.exe" --headless --path . -s res://tests/water_probe.gd`
Expected: FAIL 输出(字符/decay 断言未实现)→ 退出 1。

- [ ] **Step 3: 加 21/22 到 tile_defs.json**

Append to `Globals/tile_defs.json` `"tiles"` object (after `"20"`):
```json
	"21": { "name": "水", "type": "liquid", "hp": 1, "bullet_destroyable": false, "explosion_destroyable": false, "elastic": false, "explosion_decay": 0.25 },
	"22": { "name": "水面", "type": "liquid", "hp": 1, "bullet_destroyable": false, "explosion_destroyable": false, "elastic": false, "explosion_decay": 0.25 }
```

- [ ] **Step 4: tile_defs.gd 扩展**

`Globals/tile_defs.gd`:
- `const MAX_TEXTURE: int = 20` → `const MAX_TEXTURE: int = 22`。
- 在 `explosion_decay()` 后新增:
```gdscript
# 逐格爆炸衰减(水 0.25);缺省回落全局 explosion_decay()。
static func explosion_decay_of(texture: int) -> float:
	return float(tile(texture).get("explosion_decay", explosion_decay()))
```

- [ ] **Step 5: maze_generator.gd 纹理字符**

`Globals/maze_generator.gd` `_texture_char_to_value` 的 match 里,`"K","k": return 20` 之后加:
```gdscript
			"L", "l": return 21
			"M", "m": return 22
```
`_value_to_texture_char` 改为:
```gdscript
static func _value_to_texture_char(v: int) -> String:
	if v >= 10 and v <= 22:
		return "ABCDEFGHIJKLM"[v - 10]
	return str(v)
```

- [ ] **Step 6: 跑探针确认通过**

Run: 同上命令。
Expected: `WATER OK` 退出 0。

- [ ] **Step 7: 提交**

```bash
git add Globals/tile_defs.json Globals/tile_defs.gd Globals/maze_generator.gd Tests/water_probe.gd
git commit -m "feat: 瓦片 21/22 水定义(explosion_decay 0.25)+ L/M 纹理字符"
```

---

### Task 2: Water 静态助手

**Files:**
- Create: `Globals/water.gd`
- Test: `Tests/water_probe.gd`(追加断言)

**Interfaces:**
- Produces: `Water.is_liquid(texture:int)->bool`;`Water.is_in_water(pos:Vector2)->bool`;`Water.surface_y_at(pos:Vector2)->float`;`Water.submerged(center:Vector2, surface_y:float)->bool`;`Water.feet_offset(body:Node)->float`(世界脚底偏移,回退 24)。全部静态、**不引 autoload**。

- [ ] **Step 1: 写失败断言(先加到 probe)**

Append to `Tests/water_probe.gd` `_init()`(在 `_check_approx(... "落叶回落全局 0.75")` 之后):
```gdscript
	var surf := Vector2(5 * 64 + 32, 5 * 64 + 32)
	_check(Water.is_in_water(surf), "水格判水")
	_check(not Water.is_in_water(Vector2(5 * 64 + 32, 4 * 64 + 32)), "水面上方不是水")
	_check_eq(Water.surface_y_at(Vector2(5 * 64 + 32, 5 * 64 + 32)), 5 * 64, "水面线=第5行顶")
	_check_eq(Water.surface_y_at(Vector2(5 * 64 + 32, 4 * 64 + 32)), 4 * 64 + 32, "不在水里回传 pos.y")
	_check(Water.submerged(Vector2(5 * 64 + 32, 6 * 64), 5 * 64), "中心在水面线下=没顶")
	_check(not Water.submerged(Vector2(5 * 64 + 32, 5 * 64), 5 * 64), "中心在水面线=浮着不算")
```

- [ ] **Step 2: 跑探针确认失败**

Run: 同 Task 1 命令。Expected: FAIL(`Water` 未定义)。

- [ ] **Step 3: 写 Water.gd**

Create `Globals/water.gd`:
```gdscript
class_name Water
extends RefCounted

# 水判定/水面线/没顶判定。静态、不引 autoload(-s 可直接测)。
# 依赖 MazeGenerator.current_grid(level_0 赋值)与本地 TILE_TS。
# 约定:脚底(中心+半身)在水格 = 在水中;浮力弹簧把「身体中心」拉回水面线(半没入)。

const TILE_TS: int = 64


static func is_liquid(texture: int) -> bool:
	return TileDefs.type_of(texture) == "liquid"


static func is_in_water(pos: Vector2) -> bool:
	var grid := MazeGenerator.current_grid
	if grid.is_empty():
		return false
	var c := MazeGenerator.cell_of(pos, TILE_TS, grid[0].size(), grid.size())
	var v: int = grid[c.y][c.x]
	return v != 0 and is_liquid(v / 16)


# 所在列向上扫到最顶 liquid 格,返回其顶边 y(px);点不在水里时返回 pos.y(不硬拉)。
static func surface_y_at(pos: Vector2) -> float:
	var grid := MazeGenerator.current_grid
	if grid.is_empty():
		return pos.y
	var cols := grid[0].size()
	var rows := grid.size()
	var c := MazeGenerator.cell_of(pos, TILE_TS, cols, rows)
	if not is_in_water(pos):
		return pos.y
	var top := c.y
	var y := c.y
	while true:
		var above := posmod(y - 1, rows)
		var v: int = grid[above][c.x]
		if v != 0 and is_liquid(v / 16):
			top = above
			y = above
		else:
			break
	return float(top * TILE_TS)


# 没顶判定:中心低于水面线(浮着中心≈水面线,不算)。
static func submerged(center: Vector2, surface_y: float) -> bool:
	return center.y > surface_y


# 实体脚底相对原点偏移(世界 px):取活动碰撞箱底边;找不到回退 24。
static func feet_offset(body: Node) -> float:
	var offset := 24.0
	for child in body.get_children():
		if not (child is CollisionShape2D):
			continue
		if (child as CollisionShape2D).disabled:
			continue
		if child is CollisionPolygon2D:
			var cp := child as CollisionPolygon2D
			var pts := cp.polygon
			if pts.size() > 0:
				var maxy := cp.to_global(pts[0]).y
				for p in pts:
					maxy = maxf(maxy, cp.to_global(p).y)
				offset = maxf(offset, maxy - body.global_position.y)
		else:
			var shape := (child as CollisionShape2D).shape
			if shape is RectangleShape2D:
				var rs := shape as RectangleShape2D
				var hh := rs.size.y * 0.5
				offset = maxf(offset, (child as CollisionShape2D).to_global(Vector2(0, hh)).y - body.global_position.y)
	return offset
```

- [ ] **Step 4: 跑探针确认通过**

Run: 同上。Expected: `WATER OK`。

- [ ] **Step 5: 提交**

```bash
git add Globals/water.gd Tests/water_probe.gd
git commit -m "feat: Water 静态助手(判水/水面线/没顶/脚底偏移)"
```

---

### Task 3: Level0 渲染分层 + 水面晃动

**Files:**
- Modify: `Scenes/level_0.gd`
- Modify: `Scenes/Level0.tscn`
- Test: headless 启动(实现者自检 + 用户跑)

**Interfaces:**
- Consumes: `Water.is_liquid(tex)`, `Level0.water_layer` / `Level0.water_surface_layer`(静态,供 `_process` 晃动)。
- Produces: 两个 TileMapLayer 铺图;水面层 position.x 每帧 sin 晃动。

- [ ] **Step 1: Level0.tscn 加两个 TileMapLayer**

In `Scenes/Level0.tscn`, after the `WallLayer` node:
```
[node name="WaterLayer" type="TileMapLayer" parent="WorldViewport"]

[node name="WaterSurfaceLayer" type="TileMapLayer" parent="WorldViewport"]
```

- [ ] **Step 2: _create_wall_tileset 扩到 22 纹理**

`Scenes/level_0.gd` `_create_wall_tileset()`:
- `for i in range(20):` → `for i in range(22):`
- 注释 `# 20 块源砖(两行 32×32,每行 10 块)` → `# 22 块源砖(两行 32×32 + 第3行两块水)`
- `var atlas_img := Image.create(16 * ts, 20 * ts, ...)` → `Image.create(16 * ts, 22 * ts, ...)`
- `for tex in range(20):` → `for tex in range(22):`

- [ ] **Step 3: _ready 接线两个水层**

In `_ready()`, after `_paint_maze(wl, grid)`:
```gdscript
	Level0.water_layer = $WorldViewport/WaterLayer
	Level0.water_surface_layer = $WorldViewport/WaterSurfaceLayer
	Level0.water_layer.tile_set = tile_set
	Level0.water_surface_layer.tile_set = tile_set
	_paint_water(grid)
```

- [ ] **Step 4: _paint_maze 跳过水格 + 新增 _paint_water**

`_paint_maze`:在 `var v: int = row[x]` 之后加一行跳过水:
```gdscript
				var v: int = row[x]
				if v == MazeGenerator.EMPTY:
					continue
				if Water.is_liquid(MazeGenerator.texture_of(v)):
					continue  # 水由 _paint_water 分层铺
```
Add `_paint_water`(atlas 行 20=水体 tex21、行 21=水面 tex22;上方非 liquid → 水面层):
```gdscript
func _paint_water(grid: Array[Array]) -> void:
	const BODY_ROW := 20   # 纹理 21(水体)的 atlas 行
	const SURF_ROW := 21   # 纹理 22(水面)的 atlas 行
	var cols := grid[0].size()
	var rows := grid.size()
	var wl: TileMapLayer = Level0.water_layer
	var sl: TileMapLayer = Level0.water_surface_layer
	for ty in range(-1, 2):
		for tx in range(-1, 2):
			var ox := tx * cols
			var oy := ty * rows
			for y in range(rows):
				var row: Array = grid[y]
				for x in range(cols):
					var v: int = row[x]
					if v == MazeGenerator.EMPTY:
						continue
					if not Water.is_liquid(MazeGenerator.texture_of(v)):
						continue
					var above: int = grid[posmod(y - 1, rows)][x]
					var is_surface := above == 0 or not Water.is_liquid(MazeGenerator.texture_of(above))
					var target := sl if is_surface else wl
					target.set_cell(Vector2i(x + ox, y + oy), 0,
							Vector2i(MazeGenerator.shape_of(v), SURF_ROW if is_surface else BODY_ROW))
```

- [ ] **Step 5: _process 加水面晃动**

Add member `var _water_sway_time: float = 0.0`(与 `_dirty_chunks` 声明同区)。`_process` 开头加:
```gdscript
func _process(delta: float) -> void:
	_water_sway_time += delta
	if Level0.water_surface_layer != null:
		Level0.water_surface_layer.position.x = roundf(
				sin(_water_sway_time * GameParameters.water_sway_speed) * GameParameters.water_sway_amp)
	if not _dirty_chunks.is_empty():
		...
```

- [ ] **Step 6: gameParameters.gd 加水参数**

`Globals/gameParameters.gd`(物理基础段后):
```gdscript
# ── 水 ──
const water_sway_amp: float = 2.0        # 水面晃动幅度(px,整数取整)
const water_sway_speed: float = 0.8      # 水面晃动角速度(rad/s)
const water_bullet_drag: float = 2.0     # 子弹水中速度阻力系数(exp(-drag·Δt))
const water_fx_move_threshold: float = 40.0   # 实体移动速度低于此值不发射水粒子
const water_fx_splash_band: float = 20.0     # 脚底距水面线 ≤ 此值算溅水花
const water_fx_splash_interval: float = 0.15
const water_fx_bubble_interval: float = 0.12
```

- [ ] **Step 7: headless 自检 + 提交**

Run(headless 启动 90 帧,看脚本报错;实现者自检,最终由用户确认):
`"D:/Program Files/Godot_v4.7.1-stable_win64/Godot_v4.7.1-stable_win64_console.exe" --headless --path . --quit-after 90`
Expected: 无 `SCRIPT ERROR`。

```bash
git add Scenes/level_0.gd Scenes/Level0.tscn Globals/gameParameters.gd
git commit -m "feat: 水体/水面分层渲染 + 水面 sin 晃动"
```

---

### Task 4: 主角游泳(SwimComponent)

**Files:**
- Create: `Scenes/Player/swim_component.gd`
- Modify: `Scenes/Player/Player.tscn`(加 Swim 节点)
- Modify: `Scenes/Player/player.gd`(接线 + 水中分支)
- Modify: `Globals/playerParams.gd`(游泳参数)
- Test: 用户游戏测试(进水里按上下左右游、出水跳回)

**Interfaces:**
- Produces: `SwimComponent.update(parent:CharacterBody2D, delta:float, move_mult:Vector2=Vector2.ONE)->bool`(返回是否在水中,已在水中设好 parent.velocity);`SwimComponent.in_water:bool`。
- Consumes: `Water.is_in_water` / `Water.surface_y_at` / `Water.feet_offset`, `PlayerParams.player_swim_*`。

- [ ] **Step 1: playerParams.gd 加游泳参数**

`Globals/playerParams.gd`(冲刺段后):
```gdscript
# ── 游泳 ──
const player_swim_speed: float = 420.0    # 水中水平移速(≈0.6×700)
const player_swim_up: float = -480.0      # 上浮速度
const player_swim_down: float = 420.0     # 下沉速度
const player_swim_accel: float = 6.0      # 水中水平缓动系数
const player_buoyancy_k: float = 6.0      # 浮力弹簧刚度(回水面)
const player_max_float: float = 300.0     # 上浮最大速度
const player_max_sink: float = 200.0      # 下沉最大速度
const player_water_damp: float = 4.0      # 水中垂直速度阻尼
```

- [ ] **Step 2: 写 SwimComponent**

Create `Scenes/Player/swim_component.gd`:
```gdscript
class_name SwimComponent
extends Node

# 主角水中物理:浮力弹簧回水面 + 上浮/下沉 + 水平游泳。根每帧显式调用 update(),
# 返回是否在水中(据此跳过普通垂直/跳跃/下蹲/冲刺逻辑)。不写 _physics_process。

var in_water: bool = false


func _approach(current: float, target: float, rate: float, delta: float) -> float:
	return lerp(current, target, 1.0 - exp(-rate * delta))


func update(parent: CharacterBody2D, delta: float, move_mult: Vector2 = Vector2.ONE) -> bool:
	var grid := MazeGenerator.current_grid
	var feet := Vector2(parent.global_position.x, parent.global_position.y + Water.feet_offset(parent))
	in_water = not grid.is_empty() and Water.is_in_water(feet)
	if not in_water:
		return false
	var surface_y := Water.surface_y_at(parent.global_position)
	var horiz := Input.get_axis("left", "right")
	# 水平游泳(吃武器移动惩罚 move_mult.x)
	var target_vx := horiz * PlayerParams.player_swim_speed * move_mult.x
	parent.velocity.x = _approach(parent.velocity.x, target_vx, PlayerParams.player_swim_accel, delta)
	# 垂直:上/下覆盖,无输入弹簧回水面(中心贴水面线,半没入)
	if Input.is_action_pressed("up"):
		parent.velocity.y = PlayerParams.player_swim_up
	elif Input.is_action_pressed("down"):
		parent.velocity.y = PlayerParams.player_swim_down
	else:
		var target_vy := clampf((surface_y - parent.global_position.y) * PlayerParams.player_buoyancy_k,
				-PlayerParams.player_max_float, PlayerParams.player_max_sink)
		parent.velocity.y = _approach(parent.velocity.y, target_vy, PlayerParams.player_water_damp, delta)
	return true
```

- [ ] **Step 3: Player.tscn 加 Swim 节点**

Append to `Scenes/Player/Player.tscn`(Combat 节点后):
```
[node name="Swim" type="Node" parent="."]
script = ExtResource("...")
```
注:需要在文件头加一条 `[ext_resource type="Script" path="res://scenes/Player/swim_component.gd" id="6_swim"]`,`parent="."` 的 id 用 `6_swim`(或已存在未占用的 id)。

- [ ] **Step 4: player.gd 接线**

`Scenes/Player/player.gd`:
- `@onready var combat: CombatComponent = $Combat` 后加:
```gdscript
@onready var swim: SwimComponent = $Swim
```

- [ ] **Step 5: _physics_process 水中分支(守卫式小改,不动原块)**

`_physics_process` 中,`var mult := weapons.movement_multiplier()` 与 `var horizontal_input = Input.get_axis("left", "right")` 之后,原攀爬声明之前替换:
```gdscript
	# ---------- 水中(浮水/游泳):速度由 swim 设置,跳过攀爬/重力/跳跃/下蹲/冲刺 ----------
	var in_water := swim.update(self, delta, mult)
	var climbing := false
	var latched := false
	if not in_water:
		# ---------- 攀爬(梯子/锁链:攀附不受重力,按住上/下爬,锁链更快,下降更快) ----------
		climbing = climb.update(mult, delta, is_squat)
		latched = climb.is_latched()
	else:
		# 水中:清掉冲刺/下蹲残留,避免姿态锁死
		is_charge = false
		is_squat = false
```
随后对四个移动块的 `if` 条件各加 `and not in_water`(原块内容一律不动):
- 垂直块:`if not latched:` → `if not latched and not in_water:`
- 下蹲块:`if not latched:` → `if not latched and not in_water:`
- 冲刺输入:`if not latched and not is_charge and not is_squat:` → `if not latched and not is_charge and not is_squat and not in_water:`
- 水平速度:`if not climbing:` → `if not climbing and not in_water:`

其余代码(面朝/姿态/动画/击退/移动/弹性/回卷)保持不动。

- [ ] **Step 6: 姿态选择水中特例**

姿态 desired 计算块,把原:
```gdscript
	var desired: Pose = Pose.STAND
	if is_charge:
		desired = Pose.CHARGE
	elif is_squat:
```
改为:
```gdscript
	var desired: Pose = Pose.STAND
	if in_water:
		desired = Pose.MOVE if absf(velocity.x) > 1.0 else Pose.STAND
	elif is_charge:
		desired = Pose.CHARGE
	elif is_squat:
```
(其余 `elif` 链原样不动。)

- [ ] **Step 7: headless 自检 + 提交**

Run: headless 启动命令(Task 3 同款)。Expected: 无 SCRIPT ERROR。

```bash
git add Scenes/Player/swim_component.gd Scenes/Player/Player.tscn Scenes/Player/player.gd Globals/playerParams.gd
git commit -m "feat: 主角水中游泳(上浮/下沉/水平游/浮力弹簧)"
```

---

### Task 5: 敌人水物理 + 溺水 + 游泳钩子

**Files:**
- Modify: `Globals/enemyParams.gd`(shared 参数)
- Modify: `Scenes/Enemies/enemy_base.gd`(水物理/溺水/钩子/WaterFx)
- Modify: `Scenes/Enemies/enemy_jump_bird.gd`、`Scenes/Enemies/enemy_black_bird.gd`(钩子覆写)
- Test: 用户游戏测试(落水浮起、朝玩家游、5s 后每 1s 扣 5)

**Interfaces:**
- Produces: `EnemyBase._water_swim_dir()->Vector2`(默认 ZERO);`EnemyBase._apply_water(delta)`;`_in_water/_water_time/_drown_tick`。
- Consumes: `Water.*`, `EnemyParams.shared.bird_swim_speed` 等。

- [ ] **Step 1: enemyParams.gd shared 加参数**

`Globals/enemyParams.gd` `class shared` 内加:
```gdscript
	# ── 水 ──
	const bird_swim_speed: float = 260.0   # 落水鸟朝玩家游的速度
	const bird_buoyancy_k: float = 6.0     # 浮力弹簧刚度(回水面)
	const bird_max_float: float = 260.0    # 上浮最大速度
	const bird_max_sink: float = 160.0     # 下沉最大速度
	const bird_water_damp: float = 5.0     # 水中垂直速度阻尼
	const drown_delay: float = 5.0         # 没顶累计超过此值开始扣血
	const drown_interval: float = 1.0      # 扣血间隔(秒)
	const drown_damage: int = 5            # 每次扣血
```

- [ ] **Step 2: EnemyBase 加字段与钩子**

`Scenes/Enemies/enemy_base.gd`:
- 字段区(`var _turn_cooldown` 附近)加:
```gdscript
var _in_water: bool = false
var _water_time: float = 0.0     # 没顶累计(秒)
var _drown_tick: float = 0.0     # 扣血倒计时
```
- 行为钩子区(默认 `_anim_update` 旁)加:
```gdscript
# 落水时水平游泳方向(基类默认零=漂着;JumpBird/BlackBird 覆写为朝玩家)。
func _water_swim_dir() -> Vector2:
	return Vector2.ZERO
```
- (WaterFx 的 `add_child(WaterFx.new())` 由 Task 9 统一加,本任务不接线。)

- [ ] **Step 3: EnemyBase._physics_process 插 _apply_water**

`_physics_process` 中 `if is_dead: velocity *= exp(...)` 块之后、`move_and_collide(knock_velocity * delta)` 之前插入:
```gdscript
	_apply_water(delta)
```
(尸体也走水物理:腐烂前短暂上浮,可接受。)

- [ ] **Step 4: 写 _apply_water + _approach**

`_apply_hit` 附近加:
```gdscript
func _approach(current: float, target: float, rate: float, delta: float) -> float:
	return lerp(current, target, 1.0 - exp(-rate * delta))


# 落水浮力:弹簧把身体中心拉回水面线(半没入);水平朝 _water_swim_dir 游;没顶累计溺水。
func _apply_water(delta: float) -> void:
	var feet := Vector2(global_position.x, global_position.y + Water.feet_offset(self))
	_in_water = Water.is_in_water(feet)
	if not _in_water:
		_water_time = 0.0
		_drown_tick = 0.0
		return
	var surface_y := Water.surface_y_at(global_position)
	var target_vy := clampf((surface_y - global_position.y) * EnemyParams.shared.bird_buoyancy_k,
			-EnemyParams.shared.bird_max_float, EnemyParams.shared.bird_max_sink)
	velocity.y = _approach(velocity.y, target_vy, EnemyParams.shared.bird_water_damp, delta)
	var dir := _water_swim_dir()
	velocity.x = _approach(velocity.x, dir.x * EnemyParams.shared.bird_swim_speed,
			EnemyParams.shared.bird_water_damp, delta)
	if Water.submerged(global_position, surface_y):
		_water_time += delta
		if _water_time > EnemyParams.shared.drown_delay:
			_drown_tick -= delta
			if _drown_tick <= 0.0:
				_drown_tick = EnemyParams.shared.drown_interval
				hurt(EnemyParams.shared.drown_damage, Vector2.ZERO)
	else:
		_water_time = 0.0
		_drown_tick = 0.0
```

- [ ] **Step 5: JumpBird / BlackBird 覆写钩子**

`Scenes/Enemies/enemy_jump_bird.gd` 末尾加:
```gdscript
# 落水:朝玩家水平游(不朝岸边,保持追击感)。
func _water_swim_dir() -> Vector2:
	return toroidal_dir_to_player()
```
`Scenes/Enemies/enemy_black_bird.gd` 末尾加同样覆写:
```gdscript
func _water_swim_dir() -> Vector2:
	return toroidal_dir_to_player()
```

- [ ] **Step 6: headless 自检 + 提交**

Run: headless 启动命令。Expected: 无 SCRIPT ERROR。

```bash
git add Globals/enemyParams.gd Scenes/Enemies/enemy_base.gd Scenes/Enemies/enemy_jump_bird.gd Scenes/Enemies/enemy_black_bird.gd
git commit -m "feat: 敌人落水浮力+朝玩家游泳+溺水(5s后每1s扣5)"
```

---

### Task 6: FlyBird 避水

**Files:**
- Modify: `Scenes/Enemies/enemy_fly_base.gd`
- Test: 用户游戏测试(鸟飞越水面不降、不落水)

**Interfaces:**
- Consumes: `Water.is_liquid(tex)`。

- [ ] **Step 1: 改 _bird_can_pass**

`_bird_can_pass` 的格子循环:
```gdscript
	for gy in range(y0, y1 + 1):
		for gx in range(x0, x1 + 1):
			if TileDefs.is_blocked(grid[posmod(gy, rows)][posmod(gx, cols)]):
				return false
```
改为:
```gdscript
	for gy in range(y0, y1 + 1):
		for gx in range(x0, x1 + 1):
			var v := grid[posmod(gy, rows)][posmod(gx, cols)]
			if TileDefs.is_blocked(v) or Water.is_liquid(v / 16):
				return false
```
(只挡自身飞行箱压到的水格;正下方是水不挡,可飞越大湖面。)

- [ ] **Step 2: headless 自检 + 提交**

Run: headless 启动命令。Expected: 无 SCRIPT ERROR。

```bash
git add Scenes/Enemies/enemy_fly_base.gd
git commit -m "feat: FlyBird 飞行箱压到水格视为障碍"
```

---

### Task 7: 爆炸在水中衰减 25%

**Files:**
- Modify: `Globals/explosion.gd`
- Test: `Tests/water_probe.gd`(追加断言)

**Interfaces:**
- Produces: `Explosion.water_mult(pos:Vector2, grid:Array[Array])->float`(公开静态,目标在水里返回水格 decay 0.25,否则 1.0)。

- [ ] **Step 1: 写失败断言(probe 追加)**

Append to probe `_init()`(Task 2 断言后):
```gdscript
	var g := MazeGenerator.current_grid
	_check_approx(Explosion.water_mult(Vector2(5 * 64 + 32, 5 * 64 + 32), g), 0.25, 1e-6, "目标在水里 ×0.25")
	_check_approx(Explosion.water_mult(Vector2(5 * 64 + 32, 4 * 64 + 32), g), 1.0, 1e-6, "目标在岸上 ×1.0")
```

- [ ] **Step 2: 跑探针确认失败**

Run: probe 命令。Expected: FAIL(`water_mult` 未定义)。

- [ ] **Step 3: 加 water_mult + apply_aoe 接入**

`Globals/explosion.gd` 加公开静态(放在 `_falloff` 前;用本地 TILE_TS 保证 -s 可测):
```gdscript
# 目标所在格是水 → 爆炸伤害/击退按该水格 explosion_decay(0.25)保留,否则 1.0。
static func water_mult(pos: Vector2, grid: Array[Array]) -> float:
	if grid.is_empty():
		return 1.0
	var ts := 64  # 不引 autoload,-s 可测
	var c := MazeGenerator.cell_of(pos, ts, grid[0].size(), grid.size())
	var v: int = grid[c.y][c.x]
	if v == 0:
		return 1.0
	var tex: int = v / 16
	if TileDefs.type_of(tex) == "liquid":
		return TileDefs.explosion_decay_of(tex)
	return 1.0
```
`apply_aoe`:
- 敌人分支里 `var blocked := ...` 之后加 `var wmult := water_mult((e as Node2D).global_position, grid)`,并把 `dmg` 与 `_falloff(...)*...` 击退都再乘 `* wmult`:
```gdscript
		var wmult := water_mult((e as Node2D).global_position, grid)
		var dmg := _falloff(d, radius, max_damage) * (BLOCKED_FRACTION if blocked else 1.0) * wmult
		if dmg <= 0:
			continue
		e.hurt(int(dmg), _outward_dir(center, (e as Node2D).global_position),
				_falloff(d, radius, max_knockback) * (BLOCKED_FRACTION if blocked else 1.0) * wmult, true)
```
- 玩家分支里 `var mult := BLOCKED_FRACTION if blocked else 1.0` 之后加:
```gdscript
			mult *= water_mult(p.global_position, grid)
```

- [ ] **Step 4: 跑探针确认通过 + 提交**

Run: probe 命令。Expected: `WATER OK`。

```bash
git add Globals/explosion.gd Tests/water_probe.gd
git commit -m "feat: 爆炸对水中目标伤害/击退 ×水格 decay(0.25)"
```

---

### Task 8: 子弹在水中受速度方向阻力

**Files:**
- Modify: `Scenes/Weapons/bullet_base.gd`
- Modify: `Scenes/Enemies/enemy_bullet.gd`
- Test: `Tests/water_probe.gd`(追加断言)

**Interfaces:**
- Produces: `BulletBase.water_drag_factor(in_water:bool, drag:float, delta:float)->float`(纯函数);`BulletBase._apply_water_drag(delta)`。

- [ ] **Step 1: 写失败断言(probe 追加)**

Append to probe `_init()`:
```gdscript
	var f := BulletBase.water_drag_factor(true, 2.0, 0.1)
	_check_approx(f, exp(-0.2), 1e-4, "水中阻力 exp(-0.2)")
	_check_approx(BulletBase.water_drag_factor(false, 2.0, 0.1), 1.0, 1e-6, "岸上无阻力")
```

- [ ] **Step 2: 跑探针确认失败**

Run: probe 命令。Expected: FAIL(`water_drag_factor` 未定义)。

- [ ] **Step 3: BulletBase 加阻力**

`Scenes/Weapons/bullet_base.gd`:
- 加纯函数 + 应用方法:
```gdscript
# 水中阻力系数(纯函数,probe 可测):在水里返回 exp(-drag·Δt),否则 1.0。
static func water_drag_factor(in_water: bool, drag: float, delta: float) -> float:
	return exp(-drag * delta) if in_water else 1.0


func _apply_water_drag(delta: float) -> void:
	if Water.is_in_water(global_position):
		velocity_vec *= exp(-GameParameters.water_bullet_drag * delta)
```
- `_physics_process` 里 `var step := velocity_vec * delta` 之前插:
```gdscript
	_apply_water_drag(delta)
```

- [ ] **Step 4: EnemyBullet 也调阻力**

`Scenes/Enemies/enemy_bullet.gd` `_physics_process` 里 `var step := velocity_vec * delta` 之前插:
```gdscript
	_apply_water_drag(delta)
```

- [ ] **Step 5: 跑探针确认通过 + 提交**

Run: probe 命令。Expected: `WATER OK`。

```bash
git add Scenes/Weapons/bullet_base.gd Scenes/Enemies/enemy_bullet.gd Tests/water_probe.gd
git commit -m "feat: 子弹在水中速度指数阻力(玩家+敌人)"
```

---

### Task 9: WaterFx 水粒子

**Files:**
- Create: `Scenes/Effects/water_fx.gd`
- Modify: `Scenes/Player/player.gd`(加 add_child)
- Modify: `Scenes/Enemies/enemy_base.gd`(加 add_child)
- Test: 用户游戏测试(水面溅水花、水体上浮气泡、静止不喷)

**Interfaces:**
- Produces: `WaterFx`(`class_name WaterFx extends Node2D`,自驱动 `_process`,读父实体速度/位置)。
- 接线:Task 4/5 已预留,本任务在 `player._ready`(weapons.equip("1") 后)与 `EnemyBase._ready`(_setup_contact_area() 后)各加 `add_child(WaterFx.new())`。

- [ ] **Step 1: 写 WaterFx**

Create `Scenes/Effects/water_fx.gd`:
```gdscript
class_name WaterFx
extends Node2D

# 水粒子:实体在水里移动时,水面溅水花 / 水体上浮气泡。运行期挂到主角与敌人。
# 自驱动 _process,读父实体(CharacterBody2D)速度+位置;粒子播进父实体所在视图。

static var _tex: Texture2D = null

static func _texture() -> Texture2D:
	if _tex == null:
		var img := Image.create(4, 4, false, Image.FORMAT_RGBA8)
		img.fill(Color.WHITE)
		_tex = ImageTexture.create_from_image(img)
	return _tex

var _next_emit: float = 0.0


func _process(delta: float) -> void:
	var parent := get_parent() as CharacterBody2D
	if parent == null:
		return
	var grid := MazeGenerator.current_grid
	if grid.is_empty():
		return
	var off := Water.feet_offset(parent)
	var feet := Vector2(parent.global_position.x, parent.global_position.y + off)
	if not Water.is_in_water(feet):
		return
	if parent.velocity.length() < GameParameters.water_fx_move_threshold:
		return  # 静止漂着不喷
	var surface_y := Water.surface_y_at(parent.global_position)
	# 按「中心相对水面线」分模式:中心贴水面(半没入/浮着)→ 溅水花;中心没入深 → 上浮气泡
	var depth := parent.global_position.y - surface_y
	_next_emit -= delta
	if _next_emit > 0.0:
		return
	var host := parent.get_parent()
	if host == null:
		return
	if depth <= GameParameters.water_fx_splash_band:
		_next_emit = GameParameters.water_fx_splash_interval
		_spawn(host, Vector2(parent.global_position.x, surface_y), true)
	else:
		_next_emit = GameParameters.water_fx_bubble_interval
		_spawn(host, Vector2(parent.global_position.x, parent.global_position.y + off), false)


func _spawn(host: Node, at: Vector2, splash: bool) -> void:
	var p := CPUParticles2D.new()
	p.texture = _texture()
	p.position = at
	p.amount = 8 if splash else 5
	p.lifetime = 0.4 if splash else 0.7
	p.one_shot = true
	p.explosiveness = 1.0
	p.direction = Vector2.UP
	p.spread = 40.0 if splash else 12.0
	p.gravity = Vector2(0, 700.0) if splash else Vector2(0, -30.0)
	p.initial_velocity_min = 60.0 if splash else 40.0
	p.initial_velocity_max = 160.0 if splash else 90.0
	p.scale_amount_min = 1.0 if splash else 0.5
	p.scale_amount_max = 2.5 if splash else 1.0
	p.color = Color(0.75, 0.88, 1.0, 0.9) if splash else Color(0.75, 0.88, 1.0, 0.6)
	host.add_child(p)
	host.get_tree().create_timer(p.lifetime + 0.1).timeout.connect(p.queue_free)
```

- [ ] **Step 2: 接线到主角与敌人**

`Scenes/Player/player.gd` `_ready()` 末尾 `weapons.equip("1")` 后加:
```gdscript
	add_child(WaterFx.new())
```
`Scenes/Enemies/enemy_base.gd` `_ready()` 末尾 `_setup_contact_area()` 后加:
```gdscript
	add_child(WaterFx.new())
```

- [ ] **Step 3: headless 自检 + 提交**

Run: headless 启动命令。Expected: 无 SCRIPT ERROR。

```bash
git add Scenes/Effects/water_fx.gd Scenes/Player/player.gd Scenes/Enemies/enemy_base.gd
git commit -m "feat: 水面溅水花/水体上浮气泡粒子(移动中触发)"
```

---

### Task 10: 编辑器调色板

**Files:**
- Modify: `editor/structure-editor.html`
- Regenerate: `editor/tile_defs.js`(由 `node editor/sync-tiles.js`)
- Test: `node editor/smoke.js`

- [ ] **Step 1: 调色板扩到 22**

`editor/structure-editor.html` `buildPalette()` 里 `for (var t = 0; t <= 20; t++)` → `for (var t = 0; t <= 22; t++)`。
(`setWallSwatchBg` 已按 `(t-1)/10` 取行,21→第3行0列、22→第3行1列,背景定位逻辑现成可用。)

- [ ] **Step 2: 重生成 tile_defs.js**

Run: `node editor/sync-tiles.js`
Expected: `editor/tile_defs.js` 出现 `"21"` / `"22"`(type liquid,explosion_decay 0.25)。

- [ ] **Step 3: 跑编辑器测试 + 提交**

Run: `node editor/smoke.js`
Expected: 全部通过。

```bash
git add editor/structure-editor.html editor/tile_defs.js
git commit -m "feat: 编辑器调色板加 21/22 水(可在地图里画水)"
```

---

## 收尾

- [ ] 全量验证:用户跑 `Tests/water_probe.gd` + headless 启动 + 进游戏实测(画一块水:水体/水面渲染、晃动、主角游、鸟浮水/朝玩家游/溺水扣血、FlyBird 飞越水面、水中爆炸衰减、子弹入水减速、水花/气泡)。
- [ ] 更新 `CLAUDE.md`:水系统(纹理 21/22、Water/SwimComponent/WaterFx、溺水规则、FlyBird 避水、爆炸衰减、子弹阻力)。

## Self-Review 记录

- **Spec 覆盖**:渲染(分层+晃动)→Task3;主角游泳→Task4;敌人浮水/朝玩家游/溺水→Task5;FlyBird 避水→Task6;爆炸衰减→Task7;子弹阻力→Task8;水粒子→Task9;瓦片/编辑器→Task1/10;参数→各 Task。全覆盖。
- **占位符**:无 TBD/TODO;每步含完整代码与命令。
- **类型一致**:`Water.feet_offset(body:Node)->float`、`SwimComponent.update(...)->bool`、`EnemyBase._water_swim_dir()->Vector2`、`Explosion.water_mult(...)->float`、`BulletBase.water_drag_factor(...)->float` 在各任务间引用一致。
