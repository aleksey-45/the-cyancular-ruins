# S686 霰弹枪实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增第 4 把武器 S686 霰弹枪(8 丸 ±8° 散布、短射程、中上后坐),作为半自动轻武器接入第 4 武器槽。

**Architecture:** 在 `WeaponBase.fire()` 加多弹丸支持(`pellet_count`/`spread_deg` @export,默认 1/0 行为不变),新场景 `shotgun_s686.tscn` 用 @export 配置霰弹参数,玩家 WEAPONS 注册表加 `"4"` 槽位 + project.godot 加键盘 4 输入动作。

**Tech Stack:** Godot 4.7.1 GDScript。测试靠 `Tests/enemy_logic_smoke.gd`(SceneTree 冒烟脚本,由用户运行)。

## Global Constraints

- 项目约定: 冒烟测试**由用户运行**(`Godot_v4.4.1... --headless --path . -s res://tests/enemy_logic_smoke.gd`),实现者不跑测试;可用 `Godot 4.7.1 --headless --import` 做构建解析检查。
- `.gd` 脚本首字母小写;`.tscn` 场景按根节点名大写(`ShotgunS686`)。
- 敌人无无敌帧(不改 `EnemyBase.hurt`)。
- 子弹基类脚本 `res://scenes/Weapons/bullet_base.gd`;武器基类 `res://scenes/Weapons/weapon_base.gd`。

---

### Task 1: WeaponBase 多弹丸支持

**Files:**
- Modify: `Scenes/Weapons/weapon_base.gd`(加 @export + 改 `fire()`)
- Test: `Tests/enemy_logic_smoke.gd`(追加多弹丸断言)

**Interfaces:**
- Produces: `WeaponBase.pellet_count: int`(默认 1)、`WeaponBase.spread_deg: float`(默认 0)、`fire()` 按 `pellet_count` 生成多颗子弹、每颗散布 `±spread_deg` 度。

- [ ] **Step 1: 加 @export 参数**

在 `weapon_base.gd` 的 `# ── 子弹 ──` 段后(第 32 行 `bullet_color` 之后)插入:

```gdscript
# ── 霰弹/多弹丸 ──
# 每次开火弹丸数(1=单发,与旧版一致;>1 为霰弹)
@export var pellet_count: int = 1
# 弹丸散布半角(度):每颗弹丸在瞄准方向 ±spread_deg 内随机角度
@export var spread_deg: float = 0.0
```

- [ ] **Step 2: 改 `fire()` 为多弹丸循环**

把 `weapon_base.gd:132-147` 的 `fire()` 替换为:

```gdscript
func fire() -> void:
	if not _player_ok():
		return
	fire_cd_timer = fire_cooldown
	var base_dir := _clamped_aim_dir()
	var spread := deg_to_rad(spread_deg)
	for i in range(pellet_count):
		var b: BulletBase = BULLET_SCENE.instantiate()
		var ang := base_dir.angle() + randf_range(-spread, spread)
		b.setup(Vector2.from_angle(ang), bullet_speed, bullet_range, bullet_size, bullet_color, self)
		b.global_position = muzzle.global_position
		get_viewport().add_child(b)
	if player != null and player.has_method("apply_recoil"):
		player.apply_recoil(recoil_push)
	_recoil_timer = RECOIL_TIME
	sprite.position = _base_sprite_pos - Vector2(1.0, 0.0) * recoil_kick
	var cam: Camera2D = get_viewport().get_camera_2d()
	if cam != null and cam.has_method("shake"):
		cam.shake(cam_shake, cam_shake_time)
```

- [ ] **Step 3: 冒烟测试加多弹丸断言**

在 `Tests/enemy_logic_smoke.gd` 第 168 行 `e.free()` 之后追加(复用上面的 `w` = 手枪实例):

```gdscript
	# 多弹丸(霰弹):fire() 按 pellet_count 生成多颗子弹
	w.pellet_count = 3
	w.spread_deg = 8.0
	var bullet_script := load("res://scenes/Weapons/bullet_base.gd")
	var b_before := 0
	for child in root.get_children():
		if child.get_script() == bullet_script:
			b_before += 1
	w.fire()
	var b_after := 0
	for child in root.get_children():
		if child.get_script() == bullet_script:
			b_after += 1
	_check(b_after - b_before == 3, "多弹丸开火生成 3 颗子弹")
	w.queue_free()
```

- [ ] **Step 4: 构建检查 + 提交**

运行(实现者): `"D:\Program Files\Godot_v4.7.1-stable_win64\Godot_v4.7.1-stable_win64.exe" --headless --path . --import` 应无 SCRIPT ERROR。

用户运行冒烟测试: `...Godot_v4.4.1... --headless --path . -s res://tests/enemy_logic_smoke.gd` 应 SMOKE OK(含新断言)。

提交:
```bash
git add Scenes/Weapons/weapon_base.gd Tests/enemy_logic_smoke.gd
git commit -m "feat: WeaponBase 多弹丸支持(pellet_count/spread_deg), 霰弹基础"
```

---

### Task 2: 填充 s686.tscn(用户已建空场景 + 贴图)

**Files:**
- Modify: `Scenes/Weapons/s686.tscn`(已有空场景,补 script/参数/Sprite/Muzzle)
- Test: `Tests/enemy_logic_smoke.gd`(追加场景加载断言)

**Interfaces:**
- Consumes: `WeaponBase.pellet_count`/`spread_deg`(Task 1)。
- Produces: 场景 `res://scenes/Weapons/s686.tscn`,根节点 `S686` 挂 `weapon_base.gd`,参数 `pellet_count=8, spread_deg=8.0, damage=4, bullet_range=400.0, recoil_push=450.0`。

- [ ] **Step 1: 填充场景文件**

`Scenes/Weapons/s686.tscn` 当前只有根节点 `S686`。整文件替换为(贴图区域用用户新增的霰弹枪贴图 `Rect2(3,19,59,14)`):

```
[gd_scene format=3 uid="uid://c45hq6uqtt6wu"]

[ext_resource type="Script" uid="uid://knjsw64jktvh" path="res://scenes/Weapons/weapon_base.gd" id="1_wb"]
[ext_resource type="Texture2D" uid="uid://cfbhunkx3526n" path="res://AssetBundle/Sprites/Weapons.png" id="2_wpn"]

[node name="S686" type="Node2D" unique_id=1871974505]
script = ExtResource("1_wb")
tier = 0
weapon_name = "S686"
full_auto = false
fire_cooldown = 0.5
bullet_speed = 1100.0
bullet_range = 400.0
bullet_size = 0.5
pellet_count = 8
spread_deg = 8.0
damage = 4
impact = 180.0
recoil_push = 450.0
recoil_kick = 11.0
cam_shake = 5.0
cam_shake_time = 0.1
move_penalty = 0.9
penalty_mode = 1
pitch_clamp_deg = 55.0

[node name="Sprite2D" type="Sprite2D" parent="."]
texture = ExtResource("2_wpn")
region_enabled = true
region_rect = Rect2(3, 19, 59, 14)

[node name="Muzzle" type="Marker2D" parent="."]
position = Vector2(56, 4)
```

> 注: Muzzle 位置先放右端默认,playtest 后按贴图枪口实际位置微调。

- [ ] **Step 2: 冒烟测试加场景加载断言**

在 Task 1 加的多弹丸断言之后追加:

```gdscript
	# 霰弹枪场景加载 + 参数
	var sg_scene: PackedScene = load("res://scenes/Weapons/s686.tscn")
	_check(sg_scene != null, "霰弹枪场景加载")
	var sg = sg_scene.instantiate()  # 无类型:访问自定义属性需要动态分派(项目惯例)
	_check(sg.pellet_count == 8 and is_equal_approx(sg.spread_deg, 8.0), "霰弹枪 8 丸 ±8°")
	_check(sg.damage == 4 and is_equal_approx(sg.bullet_range, 400.0), "霰弹枪单丸4伤/短射程")
	_check(sg.tier == 0, "霰弹枪轻武器")
	sg.queue_free()
```

- [ ] **Step 3: 构建检查 + 提交**

`--import` 无错误。用户冒烟测试 SMOKE OK。

提交:
```bash
git add Scenes/Weapons/shotgun_s686.tscn Tests/enemy_logic_smoke.gd
git commit -m "feat: S686 霰弹枪场景(8丸±8°/单丸4伤/射程400/后坐450)"
```

---

### Task 3: 第 4 武器槽(输入动作 + 玩家注册)

**Files:**
- Modify: `project.godot`(加 `4=` 输入动作)
- Modify: `Scenes/Player/player.gd`(WEAPONS 注册 + 切枪循环)

**Interfaces:**
- Consumes: `res://scenes/Weapons/shotgun_s686.tscn`(Task 2)。
- Produces: 玩家按键盘 `4` 切换到霰弹枪。

- [ ] **Step 1: project.godot 加输入动作 "4"**

在 `project.godot` 第 94 行 `3={...}` 块结束的 `}` 之后、空行之前插入:

```
4={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":52,"key_label":0,"unicode":52,"location":0,"echo":false,"script":null)
]
}
```

- [ ] **Step 2: player.gd 注册第 4 槽**

`Scenes/Player/player.gd:46-50` WEAPONS 字典加一行:

```gdscript
const WEAPONS: Dictionary = {
	"1": "res://scenes/Weapons/pistol_test.tscn",
	"2": "res://scenes/Weapons/rifle_test.tscn",
	"3": "res://scenes/Weapons/m82a1.tscn",
	"4": "res://scenes/Weapons/s686.tscn",
}
```

`Scenes/Player/player.gd:308` 切枪循环改:

```gdscript
	for slot in ["1", "2", "3", "4"]:
		if event.is_action_pressed(slot):
			_equip_weapon(WEAPONS[slot])
			return
```

- [ ] **Step 3: 构建检查 + 提交**

`--import` 无错误(project.godot 语法正确、玩家注册表可解析)。

提交:
```bash
git add project.godot Scenes/Player/player.gd
git commit -m "feat: 第4武器槽接入 S686(键盘4)"
```

---

### Task 4: 最终验证

- [ ] **Step 1: 用户冒烟测试**

用户运行 `Godot_v4.4.1... --headless --path . -s res://tests/enemy_logic_smoke.gd`,应 SMOKE OK(含多弹丸 + 场景加载断言)。

- [ ] **Step 2: 用户 playtest**

编辑器 playtest: 按 `4` 切到 S686,开火确认:
- 一次 8 颗弹丸 ±8° 散布
- 射程 ~400px 后子弹消失
- 中上后坐(玩家被推 + 枪口上跳 + 镜头抖动)
- 半自动(连按每发之间有 0.5s 间隔)
- 近距离对敌人单发 8 丸全中 = 32 伤害

- [ ] **Step 3: 发布/提交收尾(按用户指示)**

验证通过后,提交剩余未提交内容(Player 重构内容改动等),再按需发布。
