# KH 合并 L5 实施计划（大乱斗 Royale）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 KH 的**大乱斗**整套（lobby / game / hud / host / AI 补位服务端链路）搬进 main，并手工合并三个被 KH 改坏的文件（`room_manager` / `server_main` / `match_host`），全程不撒手 main 的 **C2 客户端预测**。

**Architecture:** 以 **main 为基底做加法**。`match_host.gd` 是唯一最高风险点——KH 版把 C2 四条（每 tick 1 包 / 快照在消费前 / `ack_seq`+`c2` / COUNTDOWN `reset_state`）全改坏了，本层**只做追加**（7 个扩展点），那四条一行都不许动。幸运的是 `RoyaleHost` 覆写的方法集（`_match_round_tick`/`_ready`/`_broadcast_round_state`/`_spawn_cell`/`_match_winner`/`_on_bullet_hit`/`_respawn_player`）**不碰** `_physics_process`/`_broadcast_snapshot`/`_on_input` → C2 自然存活。

**Tech Stack:** Godot 4.7.1 标准版 · GDScript · 探针用 `extends SceneTree`（`-s`）或场景式（`.tscn` + `--quit-after`，单位**帧**）

---

## Global Constraints

- **规格**：`docs/superpowers/specs/2026-09-10-kh-merge-design.md` §3 L5 + §1.1 第 7/16 项 + §2 D2/D13
- **★★★ 侦察 digest（实现者必读，含 KH 全部原文与逐条差异）**：`.superpowers/sdd/l5-recon.md`（654 行，按节查）。下文所有「§N」均指该文件。
- **KH 来源快照（只读）**：`E:\Workspace\godot\the-cyancular-ruins-KH_v1_1_3`（记作 `$KH`）
- **Godot**：`"D:\Program Files\Godot_v4.7.1-stable_win64\Godot_v4.7.1-stable_win64_console.exe"`（记作 `$GODOT`，不在 PATH）
- **路径映射**：`Globals/*.gd`→`core/*.gd`；`Scenes/Player/*`→`scenes/player/*`；`Scenes/Enemies/*`→`scenes/enemies/*`；`Scenes/Effects/*`→`scenes/effects/*`；`Scenes/hud.gd`→`ui/hud.gd`；`Scenes/*`→`scenes/*`；`Shaders/*`→`shaders/*`；`Tests/*`→`tests/*`；`map/*.cyrm`→`maps/*.cyrm`
- **★ 硬约束 1：C2 四条一个字都不许动**（§1.4）：`_physics_process` / `_broadcast_snapshot` / `_on_input` / COUNTDOWN 分支。`match_host.gd` 本层**只做追加**。
- **★ 硬约束 2：main 侧既有成果不许被 KH 覆盖**（§2.2 / §3.2 共 4 条）：`room_manager` 的 sweep 族 + `created_at` + `_kill_worker`；`_spawn_worker` 不许带回 `--log-file C:/Users/21559/...`；`server_main._kill_port_holder` 逐字用 main 的修正版。
- **★ 硬约束 3：字号必须是 16 的倍数**（L4 起的不变量）。换算规则固定为 **`roundi(v / 16.0) * 16`**，不靠"就近取"的主观判断。新文件的字号必须一次到位，否则 L4 的全仓扫描探针会红。
- **★ 硬约束 4：UI 一律走 `UiFactory`**（用户 2026-09-11 裁定）。`UiFactory.label(text, size, color)` / `UiFactory.button(text, size, min_size := Vector2(420, 64))` / `UiFactory.style_control(c, size)` / `UiFactory.pixel_font()`。**不许**新写第 5/第 6 份「load ttf + 三件套 + add_theme_font_size_override」抄本。
- **★ 硬约束 5：大乱斗客户端走 `server_rendered`**（规格 D2，临时方案），且**「AI 补位」不接界面**（D13）——服务端 handler 与 `--ai-roles` 留，客户端按钮删。
- **★ 硬约束 6：`beam_fired` 必须走 `NetBus`**，不得启用 `NetBusExt.beam_fired`（规格 §3 L6 明文；`match_host.gd:391` 发送端就在 `NetBus` 上）。
- **提交纪律**：**每个任务结束各做一次本地提交**；`git add` 逐个点名文件；**严禁** `git add -A`/`git add .`——工作区里有**用户既有的未提交改动**（`core/net_bus.gd`、`docs/pvp-networking.md`），绝不能被卷入。全程**不 `git push`**。
- **提交信息用单行 `git commit -m "..."`**；本环境是 git-bash，**不要**用 PowerShell here-string。
- **GDScript 坑**：`if` 块里只剩注释 = 空缩进块 → **整个文件静默解析失败**（假红/假绿）。临时停写用 `pass`。
- **探针跑法**：首行 `extends SceneTree` 的用 `-s`；首行 `extends Node` 的**必须**用同名 `.tscn` + `--quit-after`（**单位是帧不是秒**）。`-s` 脚本**不得静态引用 autoload 标识符**。
- **新 `class_name` 必须刷缓存**：L5 新增 4 个全局类（`AIInputSource`/`AINavigator`/`RoyaleHost`/`RoyaleHud`）→ 每落一个都要跑项目级 `--import`，否则引用处 Parse Error。

## ★ 三个用户裁定（2026-09-11，直接写进本层）

| # | 裁定 | 落地位置 |
|---|---|---|
| **1** | `RoyaleHost` 的击杀归因时效窗口 **改读 `CombatFeedback.ATTRIB_WINDOW_MS`（3000ms）**，不再用 KH 自己的 10000ms | T3（`royale_host.gd` 的 `ATTRIB_WINDOW`）——**这是行为变更**（更严格：打一枪后 4~10s 内的溺水/坠落不再算击杀），注释须写明 |
| **2** | `server_main` 的 `_print_local_ips` / `_write_ip_file` / `_fetch_public_ip` / `_priv_score`（KH:64-122）**搬入** | T5 |
| **3** | `royale_lobby` / `royale_hud` 的 UI **走 `UiFactory`** | T6 / T8 |

## ★ 本层必须守住的 main 既有成果（8 条 KH 改坏 + 2 条 KH 带进来的必须删）

| # | 文件 | KH 干了什么 | 不保住的后果 |
|---|---|---|---|
| 1 | `server/match_host.gd` | C2-A：整队列一次性应用（`for pkt in q` + `q.clear()`）替代「每 tick 1 包」 | rollback 锚点崩塌，客户端画面被反复拉回（§1.4-A） |
| 2 | `server/match_host.gd` | C2-B：快照段挪到 `_match_round_tick` 之后 | ack 领先状态一拍 → 移动中每次快照都误判分歧（§1.4-B） |
| 3 | `server/match_host.gd` | C2-C：快照只发散字段，**丢 `ack_seq` + `c2`** | 客户端 rollback 无从比对整态（§1.4-C） |
| 4 | `server/match_host.gd` | C2-D：COUNTDOWN 只 `q.clear()`，**丢 `reset_state()`** | 倒计时里服务器玩家按上一包方向漂移，开局即分歧（§1.4-D） |
| 5 | `server/room_manager.gd` | 删掉整族僵尸房清理（`SWEEP_INTERVAL`/`MAX_ROOM_AGE`/`_sweep_acc`/`_process`/`_sweep_stale_rooms`/`_kill_worker`） | 客户端僵死不断开的老房间**永不释放 worker 端口**，500 端口池被慢速耗尽（§2.2-1） |
| 6 | `server/room_manager.gd` | `Room` 无 `created_at` | `_sweep_stale_rooms` 读 `room.created_at` → 运行期 `Invalid access`，且每 600s 才触发 → **静默潜伏**（§2.2-2） |
| 7 | `server/room_manager.gd` | `_spawn_worker` 加 `--log-file C:/Users/21559/worker_debug.log`（KH 开发机绝对路径） | 异机/导出后 worker 启动异常或日志写失败（§2.2-3） |
| 8 | `server/server_main.gd` | `_kill_port_holder` **回退成坏版本**（`% OwningProcess` 取不到属性 + `OS.execute` 末参 false） | 7777 被僵尸服务端占着 → 新实例 bind 失败 → **双击 exe 闪退**（§3.2） |
| 9 | `scenes/royale_game.gd` | 带进 `Level0.menu_demo = false`（演示世界残留） | main 的 `Level0` 无此静态 → `Invalid set index`（§4.2.1-1） |
| 10 | `scenes/royale_game.gd` | 带进 `CombatFeedback.spawn(self)`（第二个挂载点） | 破坏 L4「唯一挂载点 = `level_0.gd:_ready` 顶部」的不变量与探针（§4.2.1-2） |

**另 1 条实现型缺口（KH 自带）**：`royale_game.gd` **未订阅 `NetBus.local_beam_fired`** → 大乱斗非射手端看不到激光视觉副本。本层补（§4.2.2）。

## ★ 本层的两条 carry-forward（前层遗留，本层必须结清）

1. **`match_host._respawn_player` 的 `equip("1")` → `equip(default_slot())`**（L4 计划缺口 #4 记过、未落地）：大乱斗禁武器后固定回槽 1 会踩禁用槽。1 行 + 注释（§1.2-⑦）。
2. **`royale_game` 里 L2 搬入却未接线的三个视觉件**（`Minimap.setup_multi` / `EnemyHpBar` / `BulletTrail`）：KH 的 `royale_game.gd` **已经把三处全接好了**（调用点见 §6-6），本层照搬含路径改写即自动结清，**零新增逻辑**。

## 执行顺序

```
T1 AI 两个新类(core/ai_input_source.gd + server/ai_player.gd) + --import
 → T2 match_host 扩展点(①~⑦,只追加;C2 四条禁动)
 → T3 server/royale_host.gd(新文件;归因窗口改读 ATTRIB_WINDOW_MS)
 → T4 server/room_manager.gd(main sweep 族 ∪ KH royale 注册表)
 → T5 server/server_main.gd(main _kill_port_holder ∪ KH 分支 + IP 族)
 → T6 scenes/royale_hud.gd(新文件;UiFactory + 字号)
 → T7 scenes/royale_game.gd + .tscn(新文件;2 删 + 1 补 + 路径改写)
 → T8 scenes/royale_lobby.gd + .tscn(新文件;AI 按钮 3 删 + UiFactory + 字号)
 → T9 scenes/main_menu.gd 加「大乱斗」按钮(必须在 T8 之后)
 → T10 tests/royale_probe.*(搬入 + 路径改写)+ L5 验收断言
 → T11 全量回归 + CLAUDE.md 更新 + 交付说明
```

**为什么 T1 在 T2 之前**：T2 的 AI 生成块静态引用 `AIInputSource`/`AINavigator` 两个新全局类，缺一即 Parse Error（§4.5 ⚠️）。先建类 + 刷缓存，T2 才能引用。
**为什么 T4/T5 相邻**：两者的命令行契约耦合——T4 的 `_spawn_royale_worker` 生成的 `--royale --players N --ai-roles r,r` 必须与 T5 的解析逐字一致（§8.2-7）。

---

### Task 1: AI 补位两个新类

**Files:**
- Create: `core/ai_input_source.gd`（+ `.uid`）
- Create: `server/ai_player.gd`（+ `.uid`）
- Test: `tests/ai_input_source_smoke.gd`（+ `.uid`）

**Interfaces:**
- Produces: `class_name AIInputSource extends InputSource`（覆写 `is_network_driven() -> bool` 返回 `true`）；`class_name AINavigator extends Node`（成员 `host: Node` / `role: int` / `src: AIInputSource`）
- Consumes: `core/input_source.gd` 的基类；`MazeGenerator.toroidal_delta_px` / `has_line_of_sight`；`GameParameters.MAP_WIDTH|HEIGHT|TILE_SIZE`

- [ ] **Step 1: 写失败测试（新类还不存在）**

创建 `tests/ai_input_source_smoke.gd`：

```gdscript
extends SceneTree

# AIInputSource 契约冒烟:①是 InputSource 子类 ②is_network_driven() 必须为 true
# ③基类所有读口都真被覆写(不会被基类默认实现悄悄接管)
# 为什么钉死第 ② 条:main 的 WeaponBase.reload_active() 第二判据是 player.input_is_network()
# → 若 AI 被判成"本地单机",AI 打空弹夹后会进换弹、静默停火 reload_time 秒(霰弹 2.2s/榴弹 2.8s)。
# 无报错、无客户端分歧,只是 AI 手感莫名变差 —— 只有断言能拦住这种静默退化。

var _fail := 0


func _initialize() -> void:
	# -s 阶段 autoload 未实例化 → 这里只 load 不静态引用任何 autoload 标识符
	var ai_script: GDScript = load("res://core/ai_input_source.gd")
	var base_script: GDScript = load("res://core/input_source.gd")
	_check(ai_script != null, "ai_input_source.gd 可加载")
	if ai_script == null:
		print("AI INPUT SMOKE: FAIL")
		quit(1)
		return
	var src: InputSource = ai_script.new()

	_check(src is InputSource, "AIInputSource 是 InputSource 的子类")

	# ★ 核心:必须为 true。基类 InputSource.is_network_driven() 返回 false,而 main 的
	# WeaponBase.reload_active() 第二判据正是 player.input_is_network() → 若这里为 false,
	# 服务器侧 AI 会被判成"本地单机",打空弹夹后进换弹、静默停火 reload_time 秒
	# (霰弹 2.2s / 榴弹 2.8s)。无报错、无客户端分歧,只有断言能拦住这种静默退化。
	_check(src.is_network_driven(), "★ is_network_driven() 必须为 true(否则 AI 会静默进换弹)")

	# 覆写的行为断言(逐条对基类可区分):基类会把读口委托给真实 Input,headless 下恒为
	# 中性值 → 下面每条"写进去再读出来"都能把"未覆写"照出来。
	# ⚠ 不要改回 "GDScript.has_method(m)" 那种写法:对 **脚本资源** 调用 has_method 只报
	#   GDScript 类自身的 ClassDB 方法与 static func,不报脚本的实例方法 → 该写法恒为 false,
	#   任何断言都过不了(已实测)。也不能改用 get_script_method_list():子类会列出**继承来的**
	#   全部基类方法 → 有无覆写都通过,属空转(已实测)。
	src.aim = Vector2.UP
	_check(src.get_aim_dir_override() == Vector2.UP,
			"get_aim_dir_override() 回放写入的 aim(基类返回 ZERO)")
	src.axis = -1.0
	_check(is_equal_approx(src.get_axis("left", "right"), -1.0),
			"get_axis('left') 回放写入的 axis(基类读真实 Input,headless 恒 0)")
	src.fire = true
	_check(src.is_attack_pressed(), "is_attack_pressed() 跟随 fire(基类读真实 Input,恒 false)")
	_check(src.is_attack_just_pressed(), "is_attack_just_pressed() 跟随 fire")
	src.press_jump()
	_check(src.is_action_just_pressed("up"), "press_jump() 后 just_pressed('up') 为真")
	_check(not src.is_action_just_pressed("up"), "跳跃是**边沿**不是电平(第二次读为假)")
	# 不逐条断言那些"在基类与覆写里都是同一个常量"的读口(is_action_pressed 恒 false、
	# is_action_just_released 恒 false、is_attack_just_released 恒 false、
	# get_weapon_slot_pressed 恒 0)—— 那种断言无论覆写与否都通过,是空转。

	# AINavigator:只建实例断言成员与接口(不入树 → _physics_process/_ready 都不会跑,
	# 故读 host 的那行不会被触发;T2 的 AI 生成块按这几个成员名赋值,名错即静默失效)
	var nav_script: GDScript = load("res://server/ai_player.gd")
	_check(nav_script != null, "server/ai_player.gd 可加载")
	if nav_script != null:
		var nav: Node = nav_script.new()
		var props: Array = []
		for p in nav.get_property_list():
			props.append(p["name"])
		for m in ["host", "role", "src"]:
			_check(props.has(m), "AINavigator 有成员 %s" % m)
		_check(nav.has_method("_physics_process"), "AINavigator 有 _physics_process")
		nav.free()

	if _fail == 0:
		print("AI INPUT SMOKE: OK")
		quit(0)
	else:
		print("AI INPUT SMOKE: FAIL(%d 条)" % _fail)
		quit(1)


func _check(ok: bool, what: String) -> void:
	if ok:
		print("  ok  " + what)
	else:
		_fail += 1
		print("  FAIL " + what)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
"$GODOT" --headless --path . -s res://tests/ai_input_source_smoke.gd
```
Expected: FAIL（`res://core/ai_input_source.gd` 不存在 → `load()` 返回 null → 打印 `FAIL ai_input_source.gd 可加载`，退出码 1）

- [ ] **Step 3: 搬 `core/ai_input_source.gd` 并追加覆写**

从 `$KH/Globals/ai_input_source.gd`（45 行）整文件照搬，**零路径改写**（它只引用基类 `InputSource`）。然后在文件末尾（或紧邻其他覆写之后）追加：

```gdscript
# ★ L5 必加(规格 §3 L5 明文) —— 不加以致静默手感退化:
# main 的 WeaponBase.reload_active()(scenes/weapons/weapon_base.gd)第二判据是
#   player.input_is_network() → input_source.is_network_driven()
# 基类 InputSource.is_network_driven() 返回 false,而本类不覆写 →
#   服务器侧 AI 会被判成"本地单机" → 打空弹夹后进换弹、静默停火 reload_time 秒
#   (霰弹 2.2s / 榴弹 2.8s)。AI 没有预测端,不会造成客户端分歧 ——
#   只是 AI 手感莫名变差且无任何报错。
# 附带正向影响:weapon_base._aim_world_dir() 在 override == ZERO 时对
#   input_is_network()==true 的玩家永不读宿主 OS 鼠标、改用朝向兜底;
#   AI 的 aim 恒非零(AINavigator 每帧写 src.aim),两分支都安全但语义更正确。
func is_network_driven() -> bool:
	return true
```

- [ ] **Step 4: 搬 `server/ai_player.gd`**

从 `$KH/server/ai_player.gd`（139 行）**整文件照搬**，零路径改写（只引用 `MazeGenerator.toroidal_delta_px` / `has_line_of_sight` / `GameParameters.MAP_WIDTH|HEIGHT|TILE_SIZE`，main 全有）。

⚠️ **`class_name` 保持 `AINavigator`**（不是 `AIPlayer`）——文件名 `ai_player.gd` 与类名不同是 KH 既有状态，**不要"顺手改名"**（改了会让 T2 的静态引用对不上）。

⚠️ 该文件里 `host.RoundState.PLAYING`（经实例访问嵌套 enum）是**动态查找**，无单元测试覆盖且只在 `--ai-roles` 开局时走到 → T11 的 AI 实机复测里必须确认这一行不报 `Invalid get index 'RoundState'`。

- [ ] **Step 5: 刷全局类缓存**

```bash
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|SCRIPT ERROR|Parse Error'
```
Expected: `0`
（**必须做**：`AIInputSource`/`AINavigator` 是新全局类，不刷缓存则 T2 引用处 Parse Error。）

- [ ] **Step 6: 跑测试确认通过**

```bash
"$GODOT" --headless --path . -s res://tests/ai_input_source_smoke.gd
```
Expected: `AI INPUT SMOKE: OK`，退出码 0

- [ ] **Step 7: 冒烟 + 启动无错误**

```bash
"$GODOT" --headless --path . -s res://tests/enemy_logic_smoke.gd 2>&1 | grep -E 'SMOKE OK|FAIL'
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'    # 0
```

- [ ] **Step 8: 提交**

```bash
git add core/ai_input_source.gd core/ai_input_source.gd.uid server/ai_player.gd server/ai_player.gd.uid tests/ai_input_source_smoke.gd tests/ai_input_source_smoke.gd.uid
git commit -m "feat(L5): 搬入 AI 补位两个新类(core/ai_input_source.gd + server/ai_player.gd 的 AINavigator);AIInputSource 补 is_network_driven()=true 覆写(不加以致 AI 被判本地单机→打空弹夹静默停火);新增契约冒烟钉死该覆写"
```

---

### Task 2: `match_host.gd` 扩展点（★ 本层最高风险，只追加）

**Files:**
- Modify: `server/match_host.gd`（`_init` 签名 + `_ready` + 新增 2 个方法 + `_on_bullet_hit` / `_respawn_player` 两处追加）
- Test: 无（本任务不加测试；机械断言探针 `tests/kh_l5_probe.*` 的整体归 **T10**，本任务**不要**建骨架 —— 11 个 Step 里没有一条描述它的内容）

**Interfaces:**
- Produces: `MatchHost._init(map_path: String, role_peers: Dictionary, options := {}, ai_roles := [])`；`MatchHost._broadcast_match_options() -> void`；`MatchHost.notify_direct_hit(shooter: Node, victim: Node) -> void`；成员 `_options` / `_round_full_heal` / `_disabled_weapons: Array[int]` / `_ai_roles`
- Consumes: `AIInputSource`、`AINavigator`(T1)；`NetBusExt.rpc_id(..., "match_options", ...)`；`NetBusExt.rpc_id(..., "hit_confirm", ...)`；`Player.set_input_source`

> **⚠️ 本任务的验收线**：`git diff server/match_host.gd` 里**不得出现**对 `_physics_process` / `_broadcast_snapshot` / `_on_input` / COUNTDOWN 分支的任何改动（§1.4）。做完 Step 10 必须目视核一遍。

> **★★ 本任务范围的一处更正（控制者已核 KH 源码，覆盖侦察件 §6-1 的说法）**：侦察 §6-1 把 `set_display_names` / `mark_disconnected` 也列进「main 必须新开」。**这是错的** —— 它们是 `RoyaleHost` 的**自有方法**（`mark_disconnected` 读写 `_left` 成员并调 `_finish_match()`，都是 RoyaleHost 的东西），**随 T3 的整体照搬自动到位**，搬进 `MatchHost` 只会引入未定义符号。
> 同理 `request_suicide_role`（RoyaleHost 自有，T3 带来）**也不要**加到 `MatchHost`。
> 已验证**真正**只存在于 KH `match_host.gd`、必须由本任务补的是且仅是两个：**`_broadcast_match_options`** 与 **`notify_direct_hit`**。
> `_attributed_killer` / `_finish_match` 同属 RoyaleHost 自有（侦察 §7-3 已澄清）→ **不要为它们开空方法**。

- [ ] **Step 1: 改 `_init` 签名，追加三个选项成员与 `_ai_roles`**

`server/match_host.gd:48` 现为：

```gdscript
func _init(map_path: String, role_peers: Dictionary) -> void:
```

改为（**新增参数必须有默认值**，否则 `RoomManager` 的旧调用点会编译失败）：

```gdscript
# ── 对局选项(房主 role1 下发,经 claim_role 携带;进局时广播生效值)──
var _options: Dictionary = {}
var _round_full_heal := false        # 每回合开始双方回满血
var _disabled_weapons: Array[int] = []   # 禁用的武器槽位(双方一致)
var _ai_roles: Array = []            # AI 补位的 role 列表;这些 role 无网络 peer

func _init(map_path: String, role_peers: Dictionary, options: Dictionary = {},
		ai_roles: Array = []) -> void:
	_options = options
	_round_full_heal = bool(options.get("round_full_heal", false))
	var raw_disabled: Array = options.get("disabled_weapons", [])
	for v in raw_disabled:
		_disabled_weapons.append(int(v))
	_ai_roles = ai_roles
```

（4 个 `var` 声明放在 `_init` 之前、紧邻其他成员声明处；**新增参数必须有默认值**，否则 `RoomManager` 的旧调用点会编译失败。`_disabled_weapons` 保持 `Array[int]` 类型化，与 KH 一致。）

- [ ] **Step 2: 在 `_init` 的既有玩家循环**之后**追加 AI 生成块**

在 `server/match_host.gd:75`（`print("MatchHost: 角色 %d 出生点 %s" ...)`）之后、`_enter_tree` 之前插入。**既有循环体一字不动**：

```gdscript
	# AI 补位(实验性):同一 Player.tscn,输入源换 AIInputSource,由 AINavigator 驱动;
	# 快照/命中裁决/计分/复活全部按 players 迭代 → 客户端副本零改动。
	# ★ 不入 input_sources(不走网络包);入 players 即自动获得快照/裁决/计分/复活覆盖。
	# D13:代码就位,不接界面(客户端按钮已删)。
	for ai_role in _ai_roles:
		var role := int(ai_role)
		var p: Node2D = preload("res://scenes/player/Player.tscn").instantiate()
		var src := AIInputSource.new()
		p.set_input_source(src)
		add_child(p)
		p.collision_mask |= 2
		players[role] = p
		var nav := AINavigator.new()
		nav.host = self
		nav.role = role
		nav.src = src
		p.add_child(nav)
		var spawn := _spawn_cell(role)
		var ts := GameParameters.TILE_SIZE
		p.global_position = Vector2(spawn.x * ts + ts * 0.5, spawn.y * ts + ts * 0.5)
		print("MatchHost: AI 角色 %d 出生点 %s" % [role, spawn])
```

（KH 原文的 `nav.host/role/src` 赋值在 `p.add_child(nav)` **之前**，保持该顺序即可，功能等价。）

> **时序提醒（§1.3）**：`_init` 里的 `_spawn_cell(role)` 是**虚调**，`RoyaleHost` 覆写了它并要求「先算散点、后调 `super._init`」。本步骤只加 AI 块，**不改既有循环的相对顺序**。

- [ ] **Step 3: `_broadcast_match_options()` 新方法**

追加在 `_ready()` 之后（KH 原文逐字）：

```gdscript
# 生效选项广播:客户端据此同步禁武器(本地切枪同样被挡)。服务器权威,进局发一次。
# ★ 必须有:大乱斗的禁武器闸门靠它同步,且 tests/royale_probe 硬断言"未收到 match_options = FAIL"。
func _broadcast_match_options() -> void:
	var opts := {"disabled_weapons": _disabled_weapons, "round_full_heal": _round_full_heal}
	for role in peer_by_role:
		NetBusExt.rpc_id(peer_by_role[role], "match_options", opts)
```

- [ ] **Step 4: `_ready()` 加两段**

`server/match_host.gd:83-93` 的既有内容全部保留。按 KH 原文的位置插入（**注意顺序与 KH 一致**）：

```gdscript
func _ready() -> void:
	# 开局回合:玩家已在 _init 摆位,进 COUNTDOWN
	_round_state = RoundState.COUNTDOWN
	_round_timer = COUNTDOWN_TIME
	# 禁用武器槽位:_init 时玩家 @onready 未就绪(不能碰 weapons),进树后应用
	for role in players:
		(players[role] as Node).weapons.set_enabled_slots(_disabled_weapons)
	# 受击反馈:任意来源(子弹/鸟接触/鸟弹/爆炸)实际扣血 → combat.took_hit → 广播 hit_event
	for role in players:
		var combat = (players[role] as Node).get("combat")
		if combat != null and combat.has_signal("took_hit"):
			combat.took_hit.connect(_on_player_hit.bind(role))
	_spawn_round_birds()  # 内部按 ENABLE_BIRDS 守卫,关闭时开局/换局都不刷
	_broadcast_round_state()
	_broadcast_match_options()   # ← 新增:末行
```

⚠️ `set_enabled_slots` 段必须在**受击反馈连接循环之前**、`_broadcast_match_options()` 必须在 **`_broadcast_round_state()` 之后**（与 KH 逐行同序）。

- [ ] **Step 5: ★ 核对——以下方法**不**归本任务（防止照侦察 §6-1 的错误说法去加）**

**本步骤不改任何代码**，只做一次确认：`set_display_names` / `mark_disconnected` / `request_suicide_role` **在 KH 里属于 `server/royale_host.gd`**（已核源码），随 T3 整体照搬自动到位。**不要在 `match_host.gd` 里再写一份**——`mark_disconnected` 读写 `_left` 与 `_finish_match()`，两者都是 `RoyaleHost` 的成员，搬进基类即未定义符号。

```bash
grep -nE '^func (set_display_names|mark_disconnected|request_suicide_role|_finish_match|_attributed_killer)' "$KH/server/royale_host.gd"
```
Expected: 5 行全部命中，**全在 `royale_host.gd`**（这证明它们随 T3 到位）。

```bash
grep -nE '^func (notify_direct_hit|_broadcast_match_options)' "$KH/server/match_host.gd"
```
Expected: 2 行命中（**只有这两个**在 KH 的 `match_host.gd` 里 → 只有它们要由本任务补）。

- [ ] **Step 6: `notify_direct_hit`（KH `match_host.gd:420-430` 逐字）**

追加在 `_on_bullet_hit` 之前：

```gdscript
# 即时光束武器(激光)的直击命中:服务器结算后把 X 标记(hit_confirm)发给射手本人,
# 与子弹 _on_bullet_hit 的 hit_confirm 同链路(爆炸 AoE 不发——伤害方不明确,激光方向明确可发)。
func notify_direct_hit(shooter: Node, victim: Node) -> void:
	var shooter_role := -1
	var victim_role := -1
	for role in players:
		if players[role] == shooter:
			shooter_role = int(role)
		if players[role] == victim:
			victim_role = int(role)
	if shooter_role < 0 or victim_role < 0 or shooter_role == victim_role:
		return
	if peer_by_role.has(shooter_role):
		NetBusExt.rpc_id(peer_by_role[shooter_role], "hit_confirm", shooter_role, victim_role)
```

> **本层无调用者**：调用点在 `scenes/weapons/laser_weapon_base.gd`（KH `:239-240`），main 现无 → **归 L6**（规格 §3 L6 补一行）。本层只让方法就位（`royale_probe` 不需要它）。

- [ ] **Step 7: `_on_bullet_hit` 追加 hit_confirm 段（`take_hit` 与注释一字不动）**

`server/match_host.gd:393-397` 改为（**只追加**，`take_hit` 行与其注释原样保留；追加段用 KH `match_host.gd:433-446` 的逐字版本。注意方法签名保留 main 的 `_victim_role` 下划线前缀）：

```gdscript
func _on_bullet_hit(bullet: CharacterBody2D, victim: Node2D, _victim_role: int) -> void:
	if victim.has_method("take_hit"):
		# 受击反馈广播统一走 combat.took_hit → _on_player_hit(子弹/鸟/爆炸同源,避免重复)
		victim.take_hit(bullet.global_position, bullet.hit_damage, false, bullet.hit_impact)
	# 命中确认(NetBusExt):告诉射手"你打中了"→ 客户端屏幕中心 X 标记。只发射手本人;
	# RoyaleHost 覆写先写归因 meta 再 super 到这里,大乱斗同样生效。
	var shooter_role := 0
	for r in players:
		if players[r] == bullet.shooter:
			shooter_role = int(r)
			break
	if shooter_role != 0 and peer_by_role.has(shooter_role) \
			and multiplayer.get_peers().has(peer_by_role[shooter_role]):
		NetBusExt.rpc_id(peer_by_role[shooter_role], "hit_confirm", shooter_role, _victim_role)
	bullet.queue_free()
```

> ⚠️ 追加段用 `bullet.shooter`（`scenes/weapons/bullet_base.gd:18` 的 `var shooter: Node`，已核存在），**不是** `_victim_role`。KH 在 `_on_bullet_hit` 里**没有** `shooter != victim` 守卫（自伤守卫在 `RoyaleHost` 的覆写里）—— 保持 KH 原样，不要自己加。

- [ ] **Step 8: `_respawn_player` 的 `equip("1")` → `equip(default_slot())`**

`server/match_host.gd:481-482`：

```gdscript
	if p.weapons != null and p.weapons.has_method("equip"):
		p.weapons.equip(p.weapons.default_slot())   # 禁用武器闸门下回槽 1 会踩禁用槽(原为 equip("1"))
```

- [ ] **Step 9: `_round_full_heal` 回满血**

`server/match_host.gd:448-450` 的 COUNTDOWN 归零分支，在 `_round_state = RoundState.PLAYING` 与 `_broadcast_round_state()` **之间**插入：

```gdscript
			if _round_timer <= 0.0:
				_round_state = RoundState.PLAYING
				# 选项:回合开始双方回满血(倒计时结束时生效,含换局后的第一拍)
				if _round_full_heal:
					for heal_role in players:
						(players[heal_role] as Node).apply_authoritative_state(
								(players[heal_role] as Node).max_hp,
								(players[heal_role] as Node).max_waterproof, false)
				_broadcast_round_state()
```

（KH `match_host.gd:494-505` 逐字；不加 `has_method` 守卫 —— `Player` 确有该方法，忠实照搬即可。）

- [ ] **Step 10: ★ 目视核 C2 四条零改动**

```bash
git diff server/match_host.gd | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' > /tmp/l5_mh.diff
grep -nE '_ack_seq|q\.pop_front|q\.clear|reset_state|_snapshot_accum|_broadcast_snapshot' /tmp/l5_mh.diff
```
Expected: **只应命中你新增的 AI 块/闸门块里没有的行** —— 具体地，`q.pop_front` / `_ack_seq[role] = int(pkt.get(...))` / `_snapshot_accum` / `src.reset_state()` 这四类**不得出现在 diff 的 `-`（删除）侧**。任何一处出现在 `-` 侧 = C2 被改坏，**停下来报告**。

```bash
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'    # 0
"$GODOT" --headless --path . -s res://tests/enemy_logic_smoke.gd 2>&1 | grep -E 'SMOKE OK|FAIL'
```

- [ ] **Step 11: 提交**

```bash
git add server/match_host.gd
git commit -m "feat(L5): match_host 开 7 个 RoyaleHost 扩展点(只追加,不碰 C2 四条)——_init 加 options/ai_roles 4 参签名+选项成员+AI 生成块;_ready 加服务器侧禁武器闸门 + 末行 _broadcast_match_options 下发房主选项;新增 notify_direct_hit;_on_bullet_hit 追加 hit_confirm;_round_full_heal 回合回满血;_respawn_player 改 equip(default_slot()) 防踩禁用槽"
```

---

### Task 3: `server/royale_host.gd`（新文件）

**Files:**
- Create: `server/royale_host.gd`（+ `.uid`）

**Interfaces:**
- Produces: `class_name RoyaleHost extends MatchHost`；`static RoyaleHost.start_on(role_peers: Dictionary, map_path: String, options := {}, ai_roles := []) -> Node`；`const MATCH_TIME`；`plan_spawns(roles) -> Dictionary`
- Consumes: T2 的全部扩展点；`CombatFeedback.ATTRIB_WINDOW_MS`

- [ ] **Step 1: 整文件照搬**

从 `$KH/server/royale_host.gd`（451 行）整文件搬入 `server/royale_host.gd`，**零路径改写**（依赖项 T1/T2 已就位）。

- [ ] **Step 2: ★ 归因窗口改读 `CombatFeedback.ATTRIB_WINDOW_MS`（用户裁定 1）**

`royale_host.gd:310` 现为 `const ATTRIB_WINDOW := 10000   # ms`（已核），改为：

```gdscript
# 击杀归因时效窗口(ms)。★ 有意与 main 的单一来源对齐(用户 2026-09-11 裁定):
# 原 KH 值 10000ms 已弃用 —— 现读 CombatFeedback.ATTRIB_WINDOW_MS(3000ms)。
# 背景:两处读的是同一个 last_damager_time meta,但读端对象不重叠 ——
#   CombatFeedback 读「敌人」(决定播不播「击杀 XXX」),本文件读「玩家」(决定谁算击杀)。
# 所以这不是"两套 bug",只是口径选择;选 3s 的理由是与单机播报口径一致。
# 行为变更(有意):打一枪后 4~10s 内的溺水/坠落死亡,现在不再算作你的击杀。
const ATTRIB_WINDOW := CombatFeedback.ATTRIB_WINDOW_MS
```

唯一读点是 `_attributed_killer`（`royale_host.gd:320` 的 `if age > ATTRIB_WINDOW:`）→ 改常量即覆盖，**不需要动读点**。

- [ ] **Step 3: `_on_bullet_hit` 的 meta 写入改走 `CombatFeedback.attribute`**

`royale_host.gd:423-426` 现为裸 `set_meta("last_damager", ...)`。改为：

```gdscript
	# 归因写入统一走 main 的单一入口(它同时写 last_damager + last_damager_time)。
	# 本覆写不可省:服务器子弹撞玩家时掩码不含玩家层,只经 _adjudicate_bullets 到这里,
	# bullet_base._register_player_hit 不会跑 → 必须由本处写 meta,否则击杀归因丢失。
	CombatFeedback.attribute(victim, bullet.shooter)
```

⚠️ `attribute(victim: Node, attacker: Node)` 内含 `attacker == victim` 自伤守卫（与 KH 原有的 `bullet.shooter != victim` 守卫等价），且它是**静态**函数、`current == null` 时照常写 meta → 服务器进程安全。

- [ ] **Step 4: ★ `_init` 函数体顺序逐字保留**

`RoyaleHost._init` 必须**先**算 `_round_spawns`、**后**调 `super._init(...)`（§1.3）。顺序反了 → 首次摆位拿 `(-1,-1)` 并被 `_spawned_once` 闩锁 → 全体挤到地图回卷角落。**照搬时不要"整理"这个顺序。**

- [ ] **Step 5: 刷缓存 + 验证**

```bash
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|SCRIPT ERROR|Parse Error'   # 0
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'                        # 0
"$GODOT" --headless --path . -s res://tests/enemy_logic_smoke.gd 2>&1 | grep -E 'SMOKE OK|FAIL'
```

- [ ] **Step 6: 提交**

```bash
git add server/royale_host.gd server/royale_host.gd.uid
git commit -m "feat(L5): 新增 server/royale_host.gd(RoyaleHost extends MatchHost:散点规划 plan_spawns/大乱斗回合状态机/淘汰与观战/5 分钟限时);归因写入改走 CombatFeedback.attribute,时效窗口改读 ATTRIB_WINDOW_MS(3000ms,弃用 KH 的 10000ms——用户裁定,统一单机播报口径)"
```

---

### Task 4: `server/room_manager.gd`（main sweep 族 ∪ KH royale 注册表）

**Files:**
- Modify: `server/room_manager.gd`

**Interfaces:**
- Produces: `RoomManager.start_match_on(role_peers: Dictionary, map_path := PVP_MAP, options := {}, ai_roles := []) -> Node`；`RoomManager.royale_*` 六个 handler；`class RoyaleRoom`；`ROYALE_MIN_PLAYERS`/`ROYALE_MAX_PLAYERS`/`ROYALE_DEFAULT_MAX`/`ROYALE_PORT_REUSE_DELAY`；`_release_port_later(port, delay)`
- Consumes: `NetBusExt` 的 7 个 `*_royale_*_requested` 转交信号；T2/T3 的 4 参 `MatchHost` / `RoyaleHost.start_on`

- [ ] **Step 1: 加常量与 `RoyaleRoom` 类**

从 `$KH/server/room_manager.gd:20-23`（两个端口延迟常量）、`:39-41`（ROYALE_MIN/MAX/DEFAULT）、`:43-56`（`class RoyaleRoom`）搬入。另加 `var royale_rooms: Dictionary = {}`。

⚠️ **`RoyaleRoom` 是独立类、不是 `Room`** —— main 的 `Room`（含 `created_at`）**必须原样保留**，不许被 KH 的 `Room` 替换（§2.2-2）。

- [ ] **Step 2: `_enter_tree`/`_exit_tree` 增连 7 条 `NetBusExt.*_royale_*_requested`**

从 `$KH/server/room_manager.gd:63-69`（connect）/ `:77-83`（disconnect）搬入。main 的 `core/net_bus_ext.gd:55-61` 这 7 个信号全有 ✅。

- [ ] **Step 3: 搬入新增方法族**

从 `$KH/server/room_manager.gd` 整段搬入（路径零改写）：`_broadcast_royale_state`(:184-197)、`_royale_room_of`(:199-203)、`_in_1v1_room`(:206-210)、`royale_create`(:212-240)、`royale_join`(:242-269)、`royale_leave`(:271-283)、`royale_list`(:286-297)、`royale_start`(:300-328)、`ai_duel`(:333-355)、`royale_start_ai`(:358-389)、`_spawn_royale_worker`(:392-411)、`_release_port_later`(:446-450)。

> `ai_duel` / `royale_start_ai` 属 D13「服务端就位、不接界面」→ **handler 与信号连接都留**，只是没有客户端调用点。

- [ ] **Step 4: 改造既有方法（逐条，注意不许丢 main 侧语义）**

| 方法 | 改法 |
|---|---|
| `create_room` | 开头加 `if _royale_room_of(caller) != null:` 拒绝（KH:106-108） |
| `join_room` | 加 `if room.started:` 拒绝（KH:125-128）+ `_royale_room_of(caller)` 拒绝（KH:132-134） |
| `on_peer_left` | 1v1 分支改为「空房 **或 `room.started`** → 关房」；**`_worker_ports.erase` 立即归还改为 `_release_port_later(...)`**（KH:141-160）；**追加**大乱斗房分支（KH:161-178，含房主转移 + `ROYALE_PORT_REUSE_DELAY`） |
| `_start_match` | 开头 `room.started = true`；两条失败路径改为「通知双方 + 关房 + `room.worker_port = 0`」；`await` 后加 `if not rooms.has(room.code): return`（KH:414-442） |
| `_spawn_worker` | 加 `ai_roles` 参数透传 `--ai-roles`（KH:468-485）。★ **删掉 KH 的 `--log-file "C:/Users/21559/worker_debug.log"` 项**（硬约束 2 / §2.2-3）；KH 的 `or OS.has_feature("template_debug")` 判据**可以留**（可移植改进） |
| `start_match_on` | 签名改 `(role_peers, map_path := PVP_MAP, options := {}, ai_roles := [])`，末行改 `MatchHost.new(map_path, role_peers, options, ai_roles)`（KH:491-504） |
| `PVP_MAP` | **保持 main 的小写路径** `"res://maps/factory1v1.cyrm"`（KH 是 `res://map/...`） |

⚠️ `_release_port_later` 是**协程**（内含 `await`），而 `on_peer_left` 是信号回调 → 调用处**不能** `await`，fire-and-forget 裸调（KH 就是这么写的）。

- [ ] **Step 5: ★ 守住 main 的 sweep 族（零改动）**

```bash
grep -c 'func _sweep_stale_rooms' server/room_manager.gd        # 期望 1
grep -c 'func _kill_worker' server/room_manager.gd              # 期望 1
grep -c 'created_at' server/room_manager.gd                     # 期望 >= 2
grep -c 'Select -ExpandProperty OwningProcess -Unique' server/room_manager.gd   # 期望 1(修正版 PowerShell)
grep -c '21559' server/room_manager.gd                          # 期望 0
```

- [ ] **Step 6: 刷缓存 + 启动验证**

```bash
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|SCRIPT ERROR|Parse Error'   # 0
"$GODOT" --headless --path . --quit-after 120 res://server/server_main.tscn 2>&1 | grep -icE 'error|parse'   # 0(T5 之前 server_main 还没接 royale,此处只验它没被 T4 弄坏)
```

- [ ] **Step 7: 提交**

```bash
git add server/room_manager.gd
git commit -m "feat(L5): room_manager 合入大乱斗——main 的 sweep 族/created_at/_kill_worker 原样保留,+ KH 的 RoyaleRoom 注册表/royale_create|join|leave|list|start/ai_duel/royale_start_ai/_spawn_royale_worker/_release_port_later(延迟归还端口)/start_match_on 4 参签名/1v1与大乱斗互斥;删 KH 的 --log-file 私有调试残留"
```

---

### Task 5: `server/server_main.gd`（main `_kill_port_holder` ∪ KH 分支 + IP 族）

**Files:**
- Modify: `server/server_main.gd`

**Interfaces:**
- Produces: worker 命令行支持 `--royale` / `--players N` / `--ai-roles r,r`；`_begin_match()`
- Consumes: T3 的 `RoyaleHost.start_on`；T4 的 `RoomManager.start_match_on`(4 参) 与 `RoomManager.PVP_MAP`

- [ ] **Step 1: 加三个命令行分支 + 成员**

`server/server_main.gd:14-23` 的 `match args[i]` 里加（KH:24-48，**位置在 `is_worker` 判据之前**）：

```gdscript
			"--royale":
				_royale = true
			"--players":
				if i + 1 < args.size():
					_expected_players = clampi(int(args[i + 1]), 2, 8)
			"--ai-roles":
				if i + 1 < args.size():
					for tok in str(args[i + 1]).split(","):
						var r := int(tok.strip_edges())
						if r >= 1 and r <= 8:
							_ai_roles.append(r)
```

新成员：`_royale := false` / `_expected_players := 2` / `_ai_roles: Array = []` / `_claim_wait := 0.0` / `_understaffed_wait := 0.0` / `_match_started := false`（KH:19-22 + :24-48 用到的）。

- [ ] **Step 2: 加 `_process(delta)` 两条超时梯**

main 的 `server_main.gd` **完全没有 `_process`** → 新增（KH:148-161）：

```gdscript
func _process(delta: float) -> void:
	# 大乱斗:有人报到但 20s 仍未收齐 → 按已到人数(≥2)直接开局(缺席角色不入局)
	if _royale and not _match_started and _host == null and _claims.size() >= 2:
		_claim_wait += delta
		if _claim_wait > 20.0:
			_begin_match()
	# 大乱斗:可用玩家不足 2 人(开局前全部掉线)→ 宽限后退出释放端口
	elif _royale and not _match_started and _host == null and _claims.size() < 2:
		_understaffed_wait += delta
		if _understaffed_wait > 10.0:
			get_tree().quit(0)
```

- [ ] **Step 3: `_run_worker` 增连两个信号 + 调 `_print_local_ips()`**

`NetBusExt.player_options_received.connect(_on_player_options)` + `NetBusExt.suicide_requested.connect(_on_suicide_request)`（两信号 main 已有 ✅）。并按用户裁定 2 在 worker 启动时报本机 IP。

- [ ] **Step 4: 搬 IP 族（用户裁定 2）**

从 `$KH/server/server_main.gd:64-80`（`_print_local_ips`）、`:82-93`（`_priv_score`）、`:96-105`（`_write_ip_file`）、`:108-122`（`_fetch_public_ip`）四函数**整段搬入**。零路径改写。

- [ ] **Step 5: `_on_player_options` / `_on_suicide_request`**

从 `$KH/server/server_main.gd:164-168` / `:171-178` 搬入。后者带 `has_method` 守卫。

- [ ] **Step 6: 重写 `_on_role_claimed` + 新增 `_begin_match`**

从 `$KH/server/server_main.gd:180-202`（`_on_role_claimed` 加串线防护 + 收齐判据）与 `:204-226`（`_begin_match`）搬入，并按 main 的路径/命名核对：
- `_begin_match` 里 `_royale` 分支调 `RoyaleHost.start_on(_claims, RoomManager.PVP_MAP, _claim_opts.get(1, {}), _ai_roles)`
- 非 `_royale` 分支调 `RoomManager.start_match_on(_claims, RoomManager.PVP_MAP, _claim_opts.get(1, {}), _ai_roles)`
- 保留 main 的 `_kill_port_holder` 调用时机不变

- [ ] **Step 7: `_claim_hues()` + `_on_peer_left` 重写**

从 `$KH/server/server_main.gd:229-233` 与 `:235-277` 搬入（大乱斗单掉线 = `mark_disconnected(role)` 继续；全员走光才 `quit(0)`）。

- [ ] **Step 8: ★ 守住 main 的 `_kill_port_holder`（零改动）**

```bash
grep -c 'Select -ExpandProperty OwningProcess -Unique' server/server_main.gd   # 期望 1(修正版的判据)
grep -c '% OwningProcess' server/server_main.gd                                # ★ 期望 1,不是 0
grep -n 'OS.execute("powershell.exe"' server/server_main.gd                    # 末参必须是 true
```

★ **更正（T5 实现时发现，计划原先写「期望 0」是错的）**：`% OwningProcess` 的**唯一命中是 main 自己那条解释坏写法的注释**（`server/server_main.gd:125`），合并前的 HEAD 同样返回 1。所以判据不是"零命中"，而是：**`-Unique` 恰 1 处 + `OS.execute` 末参为 `true` + `% OwningProcess` 只出现在注释里**。不要为了凑 grep 去改 main 那条注释。
`_kill_port_holder` 整个函数体**逐字用 main 的**（§3.2）。这是 main 已修、KH 从未修的真回归。

- [ ] **Step 9: 验证**

```bash
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|SCRIPT ERROR|Parse Error'   # 0
"$GODOT" --headless --path . --quit-after 120 res://server/server_main.tscn 2>&1 | grep -icE 'error|parse'   # 0
```

- [ ] **Step 10: 提交**

```bash
git add server/server_main.gd
git commit -m "feat(L5): server_main 合入大乱斗——--royale/--players/--ai-roles 解析 + _process 两条超时梯(报到 20s 收不齐按已到人数开局/不足 2 人宽限退出)+ _begin_match 抽函数 + 报到串线防护 + player_options/suicide 接线 + 服主开服报本机 IP 族(_print_local_ips/_write_ip_file/_fetch_public_ip/_priv_score);_kill_port_holder 逐字保持 main 的修正版"
```

---

### Task 6: `scenes/royale_hud.gd`（新文件）

**Files:**
- Create: `scenes/royale_hud.gd`（+ `.uid`）

**Interfaces:**
- Produces: `class_name RoyaleHud`（`const LAYER := 130`）
- Consumes: `UiFactory.label/style_control`；`PvpSession.player_name`；`NetBus.local_round_state`；`NetBus.ping_updated`

- [ ] **Step 1: 整文件搬入**

从 `$KH/Scenes/royale_hud.gd`（219 行）搬入 `scenes/royale_hud.gd`，**零路径改写**（只 `load("res://assets/fonts/less_perfect_dos_vga.ttf")`）。

- [ ] **Step 2: ★ 字号换算（硬约束 3）**

`_make_label(size, color)` 的实参按 **`roundi(v/16.0)*16`** 换算：

| 行 | 原值 | 新值 |
|---|---|---|
| `:55` `_board_title` | 30 | **32** |
| `:58` `_timer_label` | 26 | **32** |
| `:75` `_big` | 150 | **144** |
| `:76` `_sub` | 64 | 64（已合规，不动） |
| `:83` `_ping_label` | 24 | **32** |
| `:99` hint | 20 | **16** |
| `:182` row | 24 | **32** |

报告里列出你实际扫到的全部「原值→新值」。

- [ ] **Step 3: ★ 字体来源改走 `UiFactory`（硬约束 4）**

`:111` 的 `l.add_theme_font_size_override("font_size", size)` + 文件里的 `load(ttf)` 三件套 → 删掉本地 `load`，`_make_label` 改走 `UiFactory.style_control(l, size)`（`UiFactory` 内部转调 `core/pixel_font.gd` 的 `PixelFont.shared()`，是字体配置的唯一来源）。

⚠️ 若该 Label 是 **RichTextLabel** 则**不要**走 `style_control`（它读 `normal_font`/`normal_font_size` 键，硬编码 `font`/`font_size` 会**静默失败**，见 `ui/ui_factory.gd:33-37` 的 caveat）。以你读到的实际控件类型为准。

- [ ] **Step 4: 验证**

```bash
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|SCRIPT ERROR|Parse Error'   # 0
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'                        # 0
"$GODOT" --headless --path . --quit-after 600 res://tests/kh_l4_probe.tscn 2>&1 | grep -E '字号规范|ALL-OK'
```
Expected: `[L4] 字号规范:扫 N 个源文件,违例 0 处`

- [ ] **Step 5: 提交**

```bash
git add scenes/royale_hud.gd scenes/royale_hud.gd.uid
git commit -m "feat(L5): 新增 scenes/royale_hud.gd(RoyaleHud,layer 130:击杀榜/限时/大字提示/ping);字号按 16 倍数归一(30/26/24/20→32/32/32/16,150→144),字体改走 UiFactory 不再自建抄本"
```

---

### Task 7: `scenes/royale_game.gd` + `.tscn`（新文件）

**Files:**
- Create: `scenes/royale_game.gd`（+ `.uid`）、`scenes/royale_game.tscn`

**Interfaces:**
- Consumes: `NetBus.local_*` 全部信号（含 `local_beam_fired`）；`NetBusExt.local_match_options`/`peer_hues`/`hit_confirm`；`Level0.safe_change_scene`；`core/laser_visual.gd`（**无 class_name，必须 preload**）

- [ ] **Step 1: 整文件搬入 + 路径改写**

从 `$KH/Scenes/royale_game.gd`（382 行）搬入。按 §4.2.3 的逐处改写：

| KH | main |
|---|---|
| `res://Scenes/Effects/tile_hit_fx.gd` | `res://scenes/effects/tile_hit_fx.gd` |
| `res://Scenes/Level0.tscn` | `res://scenes/Level0.tscn` |
| `res://Scenes/Player/player_replica.tscn` | `res://scenes/player/player_replica.tscn` |
| `res://Scenes/Player/world_label.gd` | `res://scenes/player/world_label.gd`（**无 class_name，必须 `load()`**） |
| `res://Scenes/Player/player_p2_hue.gdshader` | `res://scenes/player/player_p2_hue.gdshader`（**不是** `res://shaders/`） |
| `res://Scenes/Enemies/enemy_replica.gd` | `res://scenes/enemies/enemy_replica.gd` |
| `Level0.safe_change_scene(tree, "res://Scenes/main_menu.tscn")` | `res://scenes/main_menu.tscn` |

- [ ] **Step 2: ★ 删 2 处（硬约束 5 / 硬约束「唯一挂载点」）**

| # | KH 行 | 处置 |
|---|---|---|
| 1 | `:31` `Level0.menu_demo = false` | **删整行**（main 的 `Level0` 没有 `menu_demo` 静态 → 照抄即 `Invalid set index`） |
| 2 | `:79` `CombatFeedback.spawn(self)`（连同那行「打击反馈层」注释） | **删整段**（唯一挂载点是 `scenes/level_0.gd:_ready` 顶部；本场景里 `level0` 已是子节点 → Level0 的 `_ready` 已经 spawn 过） |

> 删了**不会**丢反馈层：`royale_game.gd:34-36` 先 `Level0.pvp_mode = true` 再 `add_child(level0)`；Level0 的 `_ready` 里 `CombatFeedback.spawn(self)` 在 `if pvp_mode: return` **之前** → 照常挂上。

- [ ] **Step 3: ★ 补 1 处：订阅 `local_beam_fired`（§4.2.2）**

在 `:50-61` 的信号块里加一行：

```gdscript
	NetBus.local_beam_fired.connect(_on_beam_fired)   # 大乱斗非射手端激光视觉副本(与 pvp_client 同款)
```

并在文件顶部加 `const LaserVisual := preload("res://core/laser_visual.gd")`（与 `scenes/pvp_client.gd:5` 同款，该脚本**无 `class_name`**，必须 preload、不能裸写标识符），然后新增 handler：

```gdscript
# 服务器权威开火(激光):逐点锚到**射手副本**当前渲染位置再整条画。
# 与 pvp_client._on_beam_fired 的唯一差别:大乱斗有 N 个副本,锚点按 shooter_role 取。
# ★ 必须走 NetBus(不是 NetBusExt):发送端 server/match_host.gd 用的是 NetBus.rpc_id(...);
#   收在 NetBusExt 上会静默 no-op(main 的 core/net_bus_ext.gd 那个同名 RPC 是 KH 遗留重复)。
func _on_beam_fired(data: Dictionary) -> void:
	if _world == null:
		return
	var shooter := int(data.get("shooter_role", 0))
	if shooter == PvpSession.role:
		return                                   # 自己那发已本地预测画过,再收会双光束
	var replica: Node2D = _replicas.get(shooter)
	if replica == null or not is_instance_valid(replica):
		return                                   # 射手副本还没建(快照未到)→ 丢本发
	var raw: PackedVector2Array = data.get("pts", PackedVector2Array())
	if raw.is_empty():
		return
	var anchor: Vector2 = replica.global_position
	var w := GameParameters.MAP_WIDTH
	var h := GameParameters.MAP_HEIGHT
	var pts := PackedVector2Array()
	for p in raw:
		pts.append(MazeGenerator.anchor_to_nearest(p, anchor, w, h))
	var color: Color = data.get("color", Color(0.1, 0.35, 1.0, 1.0))
	var half_width := float(data.get("half_width", 2.0))
	var lifetime := float(data.get("lifetime", 0.25))
	LaserVisual.spawn_muzzle_orb(_world, pts[0], color, half_width, lifetime)
	LaserVisual.spawn_beam(_world, pts, half_width, color, lifetime, int(data.get("style", 0)))
```

⚠️ 若 `_replicas` 的实际键类型/字段名与你读到的不同（例如是按 role 的 Dictionary），按实际改写；**若与本节描述不符，停下来报告。**

- [ ] **Step 4: 建 `.tscn`**

```ini
[gd_scene format=3 uid="uid://royalegame000001"]

[ext_resource type="Script" path="res://scenes/royale_game.gd" id="1"]

[node name="RoyaleGame" type="Node2D"]
script = ExtResource("1")
```

（`uid` 沿用 KH 的 `uid://royalegame000001` 以免与其他场景撞号。）

- [ ] **Step 5: 验证**

```bash
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|SCRIPT ERROR|Parse Error'   # 0
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'                        # 0
grep -cE 'menu_demo' scenes/royale_game.gd                       # 0
grep -c 'CombatFeedback.spawn' scenes/royale_game.gd             # 0
grep -c 'local_beam_fired' scenes/royale_game.gd                 # 1
grep -c 'NetBusExt.*beam_fired' scenes/royale_game.gd            # 0
grep -c 'set_server_rendered(true)' scenes/royale_game.gd        # 1(硬约束 5:大乱斗本地玩家走服务器渲染,不接 C2)
grep -c 'reconcile\|note_post_step' scenes/royale_game.gd        # 0(C2 rollback 只属 pvp_client)
```

- [ ] **Step 6: 提交**

```bash
git add scenes/royale_game.gd scenes/royale_game.gd.uid scenes/royale_game.tscn
git commit -m "feat(L5): 新增 scenes/royale_game.gd+tscn(大乱斗对局场景:Level0 世界+PostProcess+N 个 PlayerReplica/RoyaleHud/Minimap/血条/拖尾);删 KH 带进的演示世界残留 Level0.menu_demo 与第二个 CombatFeedback.spawn 挂载点;补 NetBus.local_beam_fired 订阅(大乱斗非射手端此前看不到激光)"
```

---

### Task 8: `scenes/royale_lobby.gd` + `.tscn`（新文件）

**Files:**
- Create: `scenes/royale_lobby.gd`（+ `.uid`）、`scenes/royale_lobby.tscn`

**Interfaces:**
- Consumes: `NetBusExt` 大乱斗 RPC + 信号；`NetBus.local_go_match`/`local_match_start`/`local_server_message`；`LocalServer`；`UiFactory`
- Produces: `user://` 场景切换入口由 `main_menu` 调（T9）

- [ ] **Step 1: 整文件搬入 + 路径改写**

从 `$KH/Scenes/royale_lobby.gd`（575 行）搬入。按 §4.1.1：

| KH | main |
|---|---|
| `preload("res://Globals/local_server.gd")` | `res://core/local_server.gd`（**保留 `preload` 写法**，见 `scenes/matchmaking.gd:11` 同款） |
| `change_scene_to_file("res://Scenes/main_menu.tscn")` | `res://scenes/main_menu.tscn` |
| `call_deferred("change_scene_to_file", "res://Scenes/royale_game.tscn")` | `res://scenes/royale_game.tscn`（**保留 `call_deferred`**：RPC 在 `NetBus.poll` 栈内到达，同栈切场景会偶发原生段错误） |

- [ ] **Step 2: ★ 删 AI 补位按钮族 3 处（D13）**

| # | KH 行 | 处置 |
|---|---|---|
| 1 | `:41` `var _ai_fill_btn: Button` | 删字段 |
| 2 | `:499-507` `_build_wait_panel()` 里的 `_ai_fill_btn = Button.new()` + `"AI 补位开局(实验性)"` + `pressed.connect(→ NetBusExt.rpc_id(1, "royale_start_ai"))` + `vb.add_child(_ai_fill_btn)` | 整段删 |
| 3 | `:450-451` `_on_room_state` 里 `if _ai_fill_btn != null:` / `_ai_fill_btn.visible = ...` | 两行删 |

三处**必须一起删**（同 L4 对 `matchmaking._ai_duel_btn` 的处理）——只删字段会留空引用/编译错。

- [ ] **Step 3: ★ 字号换算（硬约束 3）**

按 **`roundi(v/16.0)*16`**：

| 行 | 原值 | 新值 |
|---|---|---|
| `:72` `addr_hint` | 18 | **16** |
| `:79` `_ip_label` | 20 | **16** |
| `:84` cap | 26 | **32** |
| `:102` `_status` | 24 | **32** |
| `:137` 创建房标题 | 34 | **32** |
| `:142` `_public_check` | 24 | **32** |
| `:156`/`:166`/`:173`/`:181`/`:188`/`:211` 各 `_label` | 24 | **32** |
| `:231` 说明 | 20 | **16** |
| `:236` create 按钮 | 28 | **32** |
| `:405` 空列表提示 | 24 | **32** |

报告里列出你实际扫到的全部「原值→新值」。

- [ ] **Step 4: ★ UI 改走 `UiFactory`（硬约束 4）**

- 删本地 `_label(text, size, color)` → 改调 `UiFactory.label(text, size, color)`
- 删本地的按钮工厂 → 改调 `UiFactory.button(text, size, min_size)`（按钮的 `custom_minimum_size` 需求**传 `min_size` 参数**，不要事后覆写）
  - ⚠️ **更正（T8 实现时发现）**：计划原写「删本地 `_pixel_button(...)`」，但 `royale_lobby.gd` 里**没有** `_pixel_button` —— 它的按钮工厂叫 `_make_button`（另有 `_make_line_edit`）。**以实际存在的名字为准**；若与本节描述不符，按实际改写并在报告里写明。
- `_apply_pixel_font` **只剩一种用途时保留**：给 `WeaponComponent.make_weapon_check` 造的剪影格兜底 —— 它内层的 `Label` 只设了字号、没设字体，而 `UiFactory.style_control` 不递归穿透容器 → **递归给容器子里所有 Label 套字体是必要的**（同 L4 T6 对 `matchmaking` 的处理）。若该函数在删完 AI 按钮后已无调用点 → 一并删。
- `LocalServer` 的「启动/重启本机服务器」按钮族（`:77`/`:79-82`/`:356-367`/`:434-435`）**保留**（L4 已在 matchmaking 落地同款）。

- [ ] **Step 5: 建 `.tscn`**

```ini
[gd_scene format=3 uid="uid://royalelobby00001"]

[ext_resource type="Script" path="res://scenes/royale_lobby.gd" id="1"]

[node name="RoyaleLobby" type="Control"]
script = ExtResource("1")
```

- [ ] **Step 6: 验证**

```bash
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|SCRIPT ERROR|Parse Error'   # 0
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'                        # 0
"$GODOT" --headless --path . --quit-after 300 res://scenes/royale_lobby.tscn 2>&1 | grep -icE 'error|parse'   # 0
grep -c '_ai_fill_btn' scenes/royale_lobby.gd          # 0
grep -c 'royale_start_ai' scenes/royale_lobby.gd       # 0(客户端零调用点;服务端 handler 仍在 room_manager)
```

- [ ] **Step 7: 提交**

```bash
git add scenes/royale_lobby.gd scenes/royale_lobby.gd.uid scenes/royale_lobby.tscn
git commit -m "feat(L5): 新增 scenes/royale_lobby.gd+tscn(大乱斗大厅:公开/私密房、邀请码、人数与限时、禁用武器与角色色、房间列表自动拉取、一键起本服);删 AI 补位按钮族 3 处(D13 服务端就位不接界面);UI 走 UiFactory 且字号 16 倍数归一"
```

---

### Task 9: `scenes/main_menu.gd` 加「大乱斗」按钮

**Files:**
- Modify: `scenes/main_menu.gd`

**Interfaces:**
- Consumes: `scenes/royale_lobby.tscn`（T8 必须已存在，否则是悬空引用——L4 硬约束 2）

- [ ] **Step 1: 加按钮（在 `multi_btn` 之后）**

`scenes/main_menu.gd:163-167` 的 `multi_btn` 块之后插入：

```gdscript
	var royale_btn := UiFactory.button("大 乱 斗", 32)
	royale_btn.pressed.connect(func() -> void:
		Sfx.play("ui")
		PvpSession.reset()
		PvpSession.royale = true
		get_tree().change_scene_to_file("res://scenes/royale_lobby.tscn"))
```

- [ ] **Step 2: ★ 同步改两处按钮数组**

`scenes/main_menu.gd:178` 与 `:187`（**两处都要改**，这是个容易漏的点）：

```gdscript
	for b in [start_btn, multi_btn, royale_btn, settings_btn, ver_btn, quit_btn]:
```

- [ ] **Step 3: 验证**

```bash
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'    # 0
"$GODOT" --headless --path . --quit-after 600 res://scenes/main_menu.tscn -- --autotest-sp 2>&1 | grep -E 'AUTOTEST\[sp\]'
```
Expected: `AUTOTEST[sp]: 当前场景 = res://scenes/Level0.tscn` 且 `DONE`（加按钮**不改既有文案**，故 autotest 仍按文本匹配得到；`_press_by_text` 去空格前缀匹配不受插入按钮影响）
```bash
"$GODOT" --headless --path . --quit-after 600 res://tests/kh_l4_probe.tscn 2>&1 | grep -E '大乱斗入口|ALL-OK'
```
Expected（**改动前**）: `[L4] 主菜单大乱斗入口:零命中` → **这条探针会变红**（L4 的断言是「无 `royale` 字样」，L5 正是要加它）。**改动后**应打印「命中 1 处」并 `ALL-OK`。

⚠️ **必须一并更新 `tests/kh_l4_probe.gd` 的该条断言**：L4 约束 2 的原文是「L4 加了就是悬空引用，L5 连场景一起加」——现在 L5 加了，断言得从「零命中」改为「命中 1 处且指向 `royale_lobby.tscn`」。改这条断言属本任务范围（同一个提交）。

⚠️ **只改「数出现次数」是不够的（T9 评审发现，已补）**：数到路径字符串只能证明**源码里有这行字**，证明不了**那个场景真的存在** —— 而「悬空引用」正是 L4 原规则存在的全部理由。若有人删/改了 `royale_lobby.tscn`，数数依旧绿。故该断言**必须同时断言该路径可被解析**（探针同文件里已有 `ResourceLoader.exists(...)` 的惯用法，照抄其形式即可）。**反证要求**：临时把一个不存在的路径喂给这条存在性检查，探针**必须**红。

- [ ] **Step 4: 提交**

```bash
git add scenes/main_menu.gd tests/kh_l4_probe.gd
git commit -m "feat(L5): 主菜单加「大乱斗」按钮(PvpSession.royale=true → royale_lobby.tscn,纯 change_scene);两处按钮数组同步;kh_l4_probe 的「无 royale 入口」断言改为「恰 1 处且指向 royale_lobby.tscn」(L4 硬约束 2 的到期日)"
```

---

### Task 10: `tests/royale_probe.*` 搬入 + L5 验收探针

**Files:**
- Create: `tests/royale_probe.gd`（+ `.uid`）、`tests/royale_probe.tscn`
- Create: `tests/kh_l5_probe.gd`（+ `.uid`）、`tests/kh_l5_probe.tscn`

**Interfaces:**
- Consumes: T1–T9 的全部产物

- [ ] **Step 1: 搬 `royale_probe`（规格 §6 的 L5 验收门，main 现无 → §7-1）**

从 `$KH/Tests/royale_probe.gd`（198 行）+ `Tests/royale_probe.tscn`（6 行）搬入 `tests/`。**路径改写 2 处**：
- `res://Tests/royale_probe.tscn` → `res://tests/royale_probe.tscn`（出现在 `OS.create_process` 的实参与用法注释里）
- `.tscn` 里 `path="res://Tests/royale_probe.gd"` → `res://tests/royale_probe.gd`；`uid` 沿用 `uid://royaleprobe00001`

它起 **2 个子进程**（c1/c2）经 `user://royale_probe_room.txt` 传房号，断言三条：`round_state` 带 `names`（→ T2 的 `set_display_names`）、`match_options`（→ T2 的 `_broadcast_match_options`）、`match_start` 的 `spawn.x >= 0`（→ T3 的 `plan_spawns`）+ `≥30` 快照/3s。

- [ ] **Step 2: 跑 `royale_probe`**

```bash
"$GODOT" --headless --path . res://tests/royale_probe.tscn 2>&1 | tail -5
```
Expected: `PROBE: ALL-OK`

⚠️ **跑之前确认 7777 空闲**（项目坑：僵尸 headless Godot 会占着 7777 → 探针必红）。收尾按项目约定 `taskkill` + `kill_port`。
⚠️ 第一次跑若 `FAIL`，先看是不是 `--ai-roles` 的 `host.RoundState.PLAYING` 动态查找（T1 Step 4 的 ⚠️）——那是本层唯一无单测覆盖的动态访问。

- [ ] **Step 3: 写 L5 机械断言探针**

创建 `tests/kh_l5_probe.gd`（`extends Node`，场景式）+ `tests/kh_l5_probe.tscn`（照 `tests/kh_l4_probe.tscn` 的 6 行壳，`uid` 自拟一个未占用的）。断言（全部**源码级**，碎片拼接自伤防护照 L4 探针的既有做法）：

1. **C2 四条在位**：`server/match_host.gd` 含 `q.pop_front()`、`_ack_seq[role] = int(pkt.get(`、`src.reset_state()`、`_snapshot_accum` 各 ≥1 次
2. **main 既有成果在位**：`server/room_manager.gd` 含 `func _sweep_stale_rooms`、`func _kill_worker`、`created_at`、`Select -ExpandProperty OwningProcess -Unique`；且 `21559` **零命中**（KH 私有路径残留）
3. **`server/server_main.gd`**：`Select -ExpandProperty OwningProcess -Unique` ≥1，且 **`% OwningProcess` 在「去注释视图」上零命中**
   - ⚠️ **不能直接在原文上要求零命中（T10 实现时发现，此条已改）**：`server_main.gd` **自己那条解释坏写法的注释就含该串**，原文上 `grep -c` 恒为 1（合并前的 HEAD 亦然）。判据是「该串只出现在注释里」→ 必须**剥掉注释再判**。同类坑在 T5 段已记过一次，此处同步修正。
4. **零演示残留**：全仓生产路径扫 `menu_demo` / `_demo_level0` / `enter_game_staged` / `build_permanent_region` **零命中**
5. **`CombatFeedback.spawn` 唯一挂载点**：全仓生产路径恰 **1** 处，且在 `scenes/level_0.gd`
6. **大乱斗非射手端激光**：`scenes/royale_game.gd` 含 `local_beam_fired` 且 `NetBusExt` 的 beam_fired **零命中**
7. **AI 手感闸**：`core/ai_input_source.gd` 含 `func is_network_driven` 且其返回 `true`
8. **字号规范**：全仓字号载体非 16 倍数 **零违例** —— ★ **但照抄 L4 探针的扫描模式是不够的（T6 实测证明）**：`kh_l4_probe.gd` 的扫描只覆盖 `UiFactory.label(` / `UiFactory.button(` / `style_control(` 这几类**载体**，而字号实参**经过变量/参数**传递时（如 `royale_hud.gd` 的 `_make_label(30, color)` 私有 helper）按规则被跳过 → 实测把某处字号临时改成 `20` 后**探针依然全绿**。
   本探针必须补上这一类：扫描**已知字号 helper 的调用实参**中的整数字面量，以及**helper 体内 `style_control`/`add_theme_font_size_override` 的字号实参**。本层新文件里要覆盖的 helper 至少包括：`royale_hud.gd` 的 `_make_label(size, …)`；`royale_lobby.gd` 的 `_make_button` / `_make_line_edit`（其字号写在**体内**而非参数上）；以及 `UiFactory.label/button/style_control` 的直接调用点。并把"本层新文件的每一个字号都逐个进过断言"写进探针输出。
   **判据是"能挡住把 20 写进 helper 实参"**——反证：临时改一个 16 倍数实参为非 16 倍数，探针**必须**红。
   ⚠️ **造探针时注意两类假通过（T4 清扫探针实测撞见过）**：断言的字符串可能被**注释**或**日志行**满足 → 断言要收窄到目标代码行本身，并在**去注释**的视图上匹配。
9. **新接口在位，且归属正确**（★ T2 评审发现的计划自相矛盾，此处已修正）：
   - 在 `server/match_host.gd` 里断言存在：`_broadcast_match_options` / `notify_direct_hit`
   - 在 `server/royale_host.gd` 里断言存在：`start_on` / `set_display_names` / `mark_disconnected` / `request_suicide_role`
   - **★ 反向断言**：`server/match_host.gd` **不得**含 `set_display_names` / `mark_disconnected` / `request_suicide_role` / `_finish_match` / `_attributed_killer` —— 它们全属 `RoyaleHost`（`mark_disconnected` 读写 `_left` 并调 `_finish_match()`，放进基类即未定义符号）。
   - 另断言 `scenes/royale_hud.gd` 有 `class_name RoyaleHud`、`server/room_manager.gd` 有 `func royale_create`
10. **`_round_full_heal` 真的回满血**（★ T2 评审：该分支在 T2 的 diff 里零覆盖——只有一次性探针验过）：在 `tests/royale_probe` 或 `pvp_match_smoke` 里加一条端到端断言（开局倒计时归零后双方 `hp == max_hp`），或在本探针里用 `MatchHost.new(..., {"round_full_heal": true})` 起一局把倒计时推完再断。**不要**只靠"代码看起来对"。

- [ ] **Step 4: 跑 L5 探针**

```bash
"$GODOT" --headless --path . --quit-after 600 res://tests/kh_l5_probe.tscn 2>&1 | grep -E '\[L5\]|ALL-OK|FAIL'
```
Expected: 每条 `[L5] ...` 通过，末行 `KH L5 PROBE: ALL-OK`，退出码 0

- [ ] **Step 5: 各断言的反证（至少做 C2 那条）**

临时把 `server/match_host.gd` 的 `q.pop_front()` 改成 `q.pop_back()` → 探针**必须**报 FAIL 且退出码 1。验完改回并复跑确认 ALL-OK。

- [ ] **Step 6: 提交**

```bash
git add tests/royale_probe.gd tests/royale_probe.gd.uid tests/royale_probe.tscn tests/kh_l5_probe.gd tests/kh_l5_probe.gd.uid tests/kh_l5_probe.tscn
git commit -m "test(L5): 搬入 royale_probe(规格 §6 的 L5 验收门,main 此前没有)+ 新增 kh_l5_probe 机械断言探针(C2 四条在位/main sweep 族与 _kill_port_holder 修正版/零演示残留/CombatFeedback 唯一挂载点/大乱斗激光走 NetBus/AI is_network_driven/字号 16 倍数/七个新接口在位)"
```

---

### Task 11: 全量回归 + 文档 + 交付说明

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/superpowers/specs/2026-09-10-kh-merge-design.md`（更正 §3 L5 的两处误写）
- Create: `.superpowers/sdd/l5-delivery.md`

- [ ] **Step 1: 全量回归（规格 §6）**

```bash
# ① 导入零异常
"$GODOT" --headless --path . --import 2>&1 | grep -icE 'case mismatch|hides|SCRIPT ERROR|Parse Error'   # 0
# ② 启动
"$GODOT" --headless --path . --quit-after 90 2>&1 | grep -icE 'error|parse'   # 0
# ③ 敌人/武器/手感冒烟
"$GODOT" --headless --path . -s res://tests/enemy_logic_smoke.gd 2>&1 | grep -E 'SMOKE OK|FAIL'
"$GODOT" --headless --path . --quit-after 600 res://tests/move_feel_smoke.tscn 2>&1 | grep -E 'OK|FAIL'
# ④ ★★ C2 未被破坏的唯一机器证据（三条都要绿）
bash tests/pvp_match_smoke.sh
bash tests/pvp_reconcile_smoke.sh
bash tests/pvp_twin_smoke.sh
# ⑤ L1~L5 探针
"$GODOT" --headless --path . --quit-after 600 res://tests/kh_l1_probe.tscn 2>&1 | grep -E 'ALL-OK|FAIL'
"$GODOT" --headless --path . --quit-after 600 res://tests/kh_l3_probe.tscn 2>&1 | grep -E 'ALL-OK|FAIL'
"$GODOT" --headless --path . --quit-after 600 res://tests/kh_l4_probe.tscn 2>&1 | grep -E 'ALL-OK|FAIL'
"$GODOT" --headless --path . --quit-after 600 res://tests/kh_l5_probe.tscn 2>&1 | grep -E 'ALL-OK|FAIL'
# ⑥ 大乱斗端到端
"$GODOT" --headless --path . res://tests/royale_probe.tscn 2>&1 | tail -3
```

- [ ] **Step 2: ★ C2 无损的最终目视核**

```bash
# L5 开工基线 = 本计划提交 32db65b(固定锚点,不要用 main~N —— L5 本身会加十几个提交)
git diff 32db65b -- server/match_host.gd | grep -E '^-' | grep -vE '^---'
```
Expected: 删除侧**只应有** `equip("1")` → `equip(default_slot())` 那一行（以及可能的签名行）。任何 `q.clear()` / `_snapshot_accum` / `reset_state` / `_ack_seq` 出现在删除侧 = **停下来报告**。

- [ ] **Step 3: 更正规格 §3 L5 的两处误写**

规格 §3 L5 里那份 `match_host` 扩展点清单是**照侦察的错误说法写的**（本计划的 T2 已按 KH 源码更正）。在规格里改掉，免得 L6/L7 照它办事：

1. 清单里的 `_attributed_killer` / `_finish_match` —— 它们是 **`RoyaleHost` 自有**方法，`MatchHost` 两版都没有也不需要 → 删掉这两项（侦察 §7-3 已记）。
2. 清单里未列、但侦察 §6-1 声称「main 必须开」的 `set_display_names` / `mark_disconnected` —— 它们**也在 `royale_host.gd` 里**，随 L5 的整体照搬到位 → 在规格里注明「随 `royale_host.gd` 搬入，非 `match_host` 扩展点」。
3. 真正需要 `match_host` 补的只有 **`_broadcast_match_options`** 与 **`notify_direct_hit`** 两个方法，其余是 `_init` 签名/`_ready`/`_on_bullet_hit`/`_round_full_heal`/`_respawn_player` 的就地改动 → 照本计划 T2 的清单重写这一节。
4. 同时更正 §3 L5 对 `room_manager` / `server_main` 的措辞（侦察 §7-4）：「端口 30s 延迟归还」与 `started` 标志**是 KH 的**（main 没有）；「僵尸房清理 `_sweep_stale_rooms`」**是 main 独有的**（KH 没有）。

- [ ] **Step 3b: 更新 `CLAUDE.md`**

- autoload 数量（若 T5 未新增则不变，仍 4 个）
- 「大乱斗走 `server_rendered`」标为**临时方案**（规格 D2）
- `restart_single` 语义、`cover_multiplier` 不变量已在 L4 写入 → 核对在位即可
- 新增段落：大乱斗架构（大厅 worker 拉起 / `--royale --players N --ai-roles` / `RoyaleHost` 覆写 `MatchHost` 的哪些方法 / AI 补位「代码就位不接界面」）
- **★ 本层新引入的已知风险**（规格 §6-2）：大乱斗快照带 `c2` 整态 → 8 人 = 8 份整态 60Hz `unreliable`，体积 ×N；且 main 每 tick 只消费 1 包（KH 整队列清空）→ N 人 60Hz 上行 + 抖动理论上有**输入队列积压**风险（无长度上限/丢弃策略）。两者都写进 CLAUDE.md 标为「已知，实测观察」。

- [ ] **Step 4: 写交付说明 `.superpowers/sdd/l5-delivery.md`**

必含：
- 三条用户裁定的落地位置（归因窗口 3s / IP 族搬入 / UiFactory）
- **有意偏离 KH 的每一处 + 理由**：`--log-file` 删除、`_kill_port_holder` 用 main 修正版、`ATTRIB_WINDOW` 改 3s、AI 按钮删除（D13）、`menu_demo` 与二次 `spawn` 删除、字号换算、`equip(default_slot())`
- **8 条「KH 改坏 main」的逐条守住证据**（配 grep 输出）
- 两个 HUD 同层 130 是**有意**（互斥场景，§8.2-6）
- `start_server.bat` **不需要改**（它只起大厅；大乱斗 worker 由大厅按需拉起，§8.2-7）
- 未做项：`notify_direct_hit` 无调用者（归 L6）、AI 补位无界面入口（D13）
- **需用户真机验收**：2~4 人建房/加入/开局/死斗/复活/自杀脱困；8 人快照与输入手感；「一键起本机服务器」后进大乱斗；**AI 补位首次实跑**（确认 `host.RoundState.PLAYING` 动态查找不报错）

- [ ] **Step 5: 提交**

```bash
git add CLAUDE.md docs/superpowers/specs/2026-09-10-kh-merge-design.md
git commit -m "docs(L5): CLAUDE.md 补大乱斗架构与两条已知风险(快照带 c2 整态体积×N、每 tick 1 包的输入队列积压在 N 人下未经压测);更正规格 §3 L5 的扩展点清单(set_display_names/mark_disconnected/request_suicide_role/_finish_match/_attributed_killer 均属 royale_host 自有,随搬入到位,非 match_host 扩展点)与 room_manager/server_main 的措辞(延迟归还与 started 是 KH 的;sweep 族是 main 独有的)"
```

---

## 附：给执行者的关键提醒

1. **本层最大的坑是 `match_host.gd`**——只追加，不重写。`_physics_process`/`_broadcast_snapshot`/`_on_input`/COUNTDOWN 分支一行都不许动。做完 T2 一定要 `git diff` 目视核（Step 10）。
2. **T1 必须在 T2 之前**——`AIInputSource`/`AINavigator` 是新全局类，不先建 + 刷缓存，T2 引用处 Parse Error。
3. **每个新 `class_name` 落地后都要 `--import`**（项目 memory 有这条），否则后续引用处 Parse Error。
4. **`RoyaleHost._init` 的顺序不能"整理"**——先算散点、后 `super._init`，反了全体挤到回卷角落。
5. **`Room` 是 main 的字段集 + `started`**——不许被 KH 的 `Room` 替换，否则 `_sweep_stale_rooms` 读 `created_at` 运行期报错且 600s 才触发（静默潜伏）。
6. **`--log-file C:/Users/21559/...` 绝不带走**；`_kill_port_holder` 逐字用 main 的修正版。
7. **大乱斗的激光必须走 `NetBus`**，`NetBusExt.beam_fired` 是 KH 遗留重复，收上去静默 no-op。
8. **`ui/pvp_hud.tscn` 一行都不用改**（规格 D9：保持 main 的声明式布局）。
9. **`royale_probe` 跑之前先确认 7777 空闲**（僵尸 headless Godot 占端口 → 必红）。
10. **真机部分的验收代理做不了**——联机手感、8 人压力、AI 补位实跑必须交给用户。
